-- =============================================================================
-- V8: 日志留存清理配置种子（R58，50 阶段步骤 1b 前置开发）
-- 需求规格 v1.1：操作日志保留 2 年（默认 730 天），阈值经全局配置可调。
-- =============================================================================

INSERT INTO sys_config (config_key, config_value, description) VALUES
('log.retention_days', '730', '操作日志保留天数（R58：到期自动清理并通知超级管理员；测试可短时调低，验后必须恢复默认）');
