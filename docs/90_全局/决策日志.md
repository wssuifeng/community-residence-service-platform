# 决策日志

> 每个决策一行：日期 / 决策 / 理由 / 影响的文档。技术选型、架构决策、
> 定义变更必须在此留痕。这是回溯"当时为什么这么做"的唯一入口。

| 日期 | 决策 | 理由 | 影响的文档 |
|------|------|------|-----------|
| 2026-09-04 | 初始化阶段文档包体系（模式一·新项目），生成骨架并起草系统定义 v1 | 项目无代码无文档，从零建立"锚点-收敛"开发体系 | AGENTS.md、README.md、docs/00_总控.md、docs/10~70 各包、docs/90_全局 |
| 2026-09-04 | 系统定义从 AI 生成的 16 节长文中精简为四节锚点：目标用户直接锚定"社区管理者"，原"适用场景"列表收敛为群体画像的关键特征；原核心模块/角色等 8 节内容压缩为 C1~C9 核心业务功能表（各挂可度量判据）；原"系统不包含"30 项归并为 9 条非目标（逐条挂排除理由） | 长文信息重复、无判据不可验收；体系契约要求定义=锚点而非百科全书，细节归 20/30 包逐级细化 | docs/10_系统定义/系统定义.md |
| 2026-09-04 | 技术路线确认：Java 17 + Spring Boot 单体（前后端分离，Vue 3 + Element Plus + MySQL + Redis + JWT）；具体版本号留待 30_系统设计锁定后回填 | 毕设规模，单体架构便于开发、部署、测试和论文说明 | AGENTS.md（项目信息块） |
| 2026-09-04 | 命名双轨确认：课题名称「基于 Spring Boot 的社区居住服务管理系统设计与实现」只用于论文与对外介绍；系统名称用中性的「社区居住服务管理系统」，不带技术栈定语 | 避免技术栈与系统命名绑定后名实不符；技术选型已由 AGENTS.md 项目信息块承载 | README.md、AGENTS.md、90_全局/术语表.md |
| 2026-09-05 | 系统定义 v1 自主审查修订（用户授权，冻结权保留）：新增 C10 用户与权限管理（原收编遗漏）；投诉归口反馈通道，工单仅承载需派单事项（消除 C4/C6 双轨冲突）；租住"即将到期/已到期"标注为日期自动判定；C1 补删除保护判据；C2 补账号-居住关系门槛；非目标新增站外推送、移动 App/小程序 | 审查发现 10 项问题（2 高 / 4 中 / 4 低），详见审查记录 | docs/10_系统定义/系统定义.md、系统定义审查记录_v1.md、90_全局/术语表.md、90_全局/非开发计划内灵感留存处.md |
| 2026-09-05 | 学校三份毕设文件（2027届指导手册 PDF、开题报告及任务书样例 docx、开题 AI 提示词 docx）解析后原件归位至 90_全局/参考资料/（附文本提取版），并提炼《学校毕设要求摘要》 | 外部权威要求须按信息来源纪律留原始出处；开题截止 2026-09-15 为当前最高优先级约束 | docs/90_全局/学校毕设要求摘要.md、docs/90_全局/参考资料/ |
| 2026-09-05 | 技术选型预调研完成：推荐 Spring Boot 3.5.16（3.x 末代，OSS 支持止 2026-06-30）+ MyBatis-Plus 3.5.17（boot3-starter）+ MySQL 8.4 LTS + Redis 8/Memurai + Vue 3.5 + Vite 7 + Element Plus 2.14；备选 SB 4.1 + boot4-starter。版本最终锁定权在 30_系统设计 | SB4/Security7 生态太新、毕设收益低风险高；3.x 资料成熟利于按时交付与论文撰写 | docs/90_全局/技术选型调研.md |
| 2026-09-05 | 产出《开题报告草稿》（内容稿，灌 docx 排版待用户确认后进行）与《长任务路线图》（学校节点与七阶段体系对齐倒排：开题 09-15 / 初稿 10-15 / 中期 10-30 / 定稿 12-30 / 答辩 2027-03~04） | 开题截止仅剩 10 天且文献综述须用户知网检索；开发主体须 10-15 前成形，需提前倒排 | docs/90_全局/开题报告草稿.md、docs/90_全局/长任务路线图.md |
| 2026-09-05 | 开题参考文献完成候选清单检索：18 条真实文献（中文 14 + 外文 4）写入草稿 4.1 节，三档标注（3 条经知网/期刊官网逐字核验、10 条沿用学院样例、2 条待补作者页码）；已知网无法直连，用户仅做核对导出与近五年占比替换 | 知网需账号无法直连，网络检索可命中有公开题录的真实文献；样例 docx 所附文献为学院认可口径可直接沿用 | docs/90_全局/开题报告草稿.md §4.1 |
| 2026-09-05 | 系统定义二轮修订（用户逐项反馈）：D1 多社区架构、D2 双注册方式并存+游客公开信息子集、D3 报修/反馈双渠道定案（反馈为长连接会话式）、D4 图片+文字附件；新增 D5 房源展示与看房预约（C12，法规依据《住房租赁条例》国务院令第812号房源真实性要求）、D6 消息通知中心（C11，长连接推送+上线补拉，短信/微信可插拔渠道；澄清"不依赖第三方商业接口"是学校验收口径而非禁用站外渠道）、D7 公告查看记录回捞进 C5；功能扩为 C1~C12，角色扩为四类（+游客） | 用户拍板 + AI 调研细化；原非目标"站外推送/移动端/地图找房"口径修正（移入灵感留存处 P-006 或收窄） | docs/10_系统定义/系统定义.md、系统定义审查记录_v1.md、90_全局/术语表.md、非开发计划内灵感留存处.md、开题报告草稿.md、00_总控.md |
| 2026-09-05 | 系统定义三轮自审（AI 自主）：C12 房源拆分"在管社区房源（可预约看房）/周边外部楼盘（仅展示+联系入口）"两类；看房预约状态机补已拒绝并统一"已违约"术语；C11 通知清单补反馈会话消息；技术选型细节（WebSocket/SSE）移出定义归 30 阶段；C4 补居民提交附图；非目标补"实时定位与导航"边界 | 一致性/边界/可测性复审发现 6 处问题（1 结构性），详见审查记录三轮自审节 | docs/10_系统定义/系统定义.md、系统定义审查记录_v1.md、90_全局/术语表.md、技术选型调研.md |
| 2026-09-05 | 终轮修订并**冻结系统定义 v1**（用户授权"自审无问题即冻结"）：C12 收回外部楼盘拆分，房源限定为在管社区（后台添加）；看房预约取消"已拒绝"态，改后台可预约时段配置约束，违约处置=冻结账号/限制预约并留痕；冻结前自审通过（一致性/可测性/边界/术语四查），10 包验收记录判据逐条达成，总控 10 包→✅，指针移 20 包 | 定义演进全程五轮（初稿→自主审查→用户反馈→AI 自审→用户澄清），冻结即 20 阶段动笔前提达成 | docs/10_系统定义/系统定义.md、_中控.md、系统定义审查记录_v1.md、00_总控.md |
| 2026-09-05 | **冻结后第一次定义变更（v1 → v1.1，用户发起）**：角色模型四类→五类，新增**超级管理员**；权限分层原则=全局级操作只归超管（社区新建/停用、全局配置如注册方式开关、全系统广播、管理员账号与社区绑定分配），社区管理员权限限于绑定社区内（广播限绑定社区范围、账号管理限本社区居民/服务人员），防止社区管理员误操作全局配置 | 实际场景中社区管理员不应触碰全局配置（误操作风险）；用户在需求审阅中提出 | docs/10_系统定义/系统定义.md（v1.1）、00_总控.md、docs/20_需求分析/需求规格说明.md、90_全局/术语表.md |
| 2026-09-05 | **20_需求分析 阶段完成并冻结**：需求规格 R1~R57（挂 C1~C12）+ 端到端流程 E1~E12/E10b + 非功能 N1~N10 + 权限矩阵（五角色两层）+ 追踪矩阵定稿；终轮细化 4 处（R55 看房状态机全集、R15 到期提醒去重、R30 会话归属限定、R48 通知溯源）；用户确认权限分层修订、其余无异议并授权冻结；总控 20 包→✅，指针移 30 包 | 需求条目全集经用户确认 + 机械校验（编号/覆盖/术语/口径残留）+ 语义自检通过 | docs/20_需求分析/需求规格说明.md、_中控.md、00_总控.md |
| 2026-09-06 | **前端组件策略定案：自写为主，Element Plus 可用不依赖**（同日两次用户澄清：先否决"保留 EP"改自写，再收回"完全不引入"）；居民侧界面自写保证美观个性化，后台页面可用 EP 加速但不强制；架构上组件经业务薄封装层引用、不与组件库强绑定，可整体替换；20 包 §4 追踪矩阵 C7 笔误 R32~R35→R32~R36 经用户确认修正（正文 R36 列于 C7 下，矩阵漏记） | 居民端/游客端为消费者界面，美观为核心诉求且 EP 风格公式化；本项目非中后台监控形态组件用量有限；"完全不引入"对后台页面属过度约束 | AGENTS.md（项目信息块）、docs/30_系统设计/_中控.md、docs/30_系统设计/需求设计追踪矩阵.md、docs/20_需求分析/需求规格说明.md（§4 笔误） |
| 2026-09-06 | **架构设计主体定稿**：前后端分离单体（单实例）；后端包结构按 C1~C12 一域一包（community/resident/lease/workorder/notice/feedback/reservation/evaluation/statistics/auth/messaging/housing + 横切 config/common/schedule/log）；权限两层=功能级 @PreAuthorize + 数据级 MyBatis-Plus 拦截器按绑定社区注入过滤（角色与绑定入 JWT Claims，绑定变更吊销全部令牌）；实时通信用 Spring WebSocket + STOMP（SSE 因无法承载 C6 双向会话被排除）；通知生成走 Spring 事件解耦（事务内生成、AFTER_COMMIT 异步推送）；定时任务 @Scheduled 单实例（租期判定/到期提醒去重表/公告下线）；核心状态机 6 套（工单/租住/预约/看房/入住申请/反馈单）定为权威定义；前端静态托管默认后端内置（Nginx 留作 70 阶段选项）。总体架构图 archify 交付（9/9 检查通过） | 单体+单实例满足 N2（50 并发）不做分布式假设；一域一包避免代码与文档术语漂移（术语纪律）；STOMP 用户目的地天然满足 R48 按人隔离；无状态 JWT 下登出失效唯一落点为 Redis 黑名单（N4） | docs/30_系统设计/架构设计.md、_中控.md、需求设计追踪矩阵.md（架构列回填）、assets/总体架构.architecture.json、assets/架构设计_总体架构图.html |
| 2026-09-06 | **30_系统设计 技术栈版本锁定**：用户指定以 dcim-iot-platform 参考项目（`D:/code/iot-platform-main - codex`）README 技术栈为基线——后端 Spring Boot 4.0.6 + Java 17 + MyBatis-Plus 3.5.16（boot4-starter）+ mysql-connector-j 9.6.0 + jjwt 0.12.6 + springdoc 3.0.3（Boot 4 专用）+ Druid 1.2.28 + Flyway + Redis 7；前端 Vue 3.5 + Vite 8 + Element Plus 2.14 + Pinia 3 + Vue Router 4.6 + Axios 1.16 + ECharts 6/vue-echarts 8 + Vitest 4。组件库决策：保留 Element Plus 不自写，业务层对高频组件做薄封装。差异排除：不引入 paho-mqtt、EasyExcel | 09-05 调研推荐的 SB 3.5.16（OSS 支持止 2026-06-30）被推翻：SB 4 + Security 7 + boot4 生态组合已在参考项目整体跑通，"生态太新"风险实证消除，且 SB 3.x 已出 OSS 支持期；参考项目版本均为实战验证版本，非纸面拼凑 | AGENTS.md（项目信息块）、docs/30_系统设计/_中控.md、90_全局/技术选型调研.md（历史结论，不改） |
| 2026-09-06 | **架构设计补充缓存设计（§3.3）**：明确 Redis 七类用途（黑名单/权限缓存/WS 会话/通知序号/社区信息/公告列表/浏览统计）并规定 TTL 与更新策略；缓存一致性分三级（强一致/最终一致/允许短暂不一致）；补充三防（穿透用布隆过滤器+空值缓存、击穿用分布式锁+双重检查、雪崩用 TTL 随机偏移+AOF 持久化）；Redis 不可用降级为仅数据库查询但黑名单校验失败拒绝（安全优先） | 原架构设计只提及 Redis 用于"令牌黑名单/会话"，缺少业务数据缓存策略与一致性保证；用户询问后补足 | docs/30_系统设计/架构设计.md |
| 2026-09-06 | **通知序号方案变更：数据库自增→Redis INCR**：原"通知表单调递增序号（数据库设计详述）"存在事务回滚导致序号空洞风险（客户端漏拉通知）；改为 Redis 计数器（`INCR notification:seq`）生成全局序号并写入通知表 `seq` 字段，保证严格单调递增 | 高并发下数据库自增 ID 不连续，补拉逻辑依赖序号连续性；Redis 原子递增保证无空洞 | docs/30_系统设计/架构设计.md §4 |
| 2026-09-06 | **定时任务补充分布式锁与监控（§5 改表）**：租期判定扫描增加 Redis 分布式锁（`SETNX task:lease:scan`，TTL 10min）防止重复执行；所有任务记录执行日志（开始/结束时间、处理条数、成功失败），超时/失败触发告警记录；浏览统计回写任务细化为 Redis pipeline 批量读 + 数据库原子累加 | 原设计虽说"单实例"，但架构设计应前瞻多实例场景；缺少任务执行可观测性 | docs/30_系统设计/架构设计.md §5 |
| 2026-09-06 | **新增性能关键路径章节（§8b）**：明确 N2（50 并发）下五大关键接口响应时间目标（登录<500ms、工单提交<1s、公告列表<300ms、WebSocket 推送<3s、运营看板<2s）并标注性能瓶颈点与优化措施；补充 AOP 耗时监控、Druid 慢 SQL 监控（阈值 1s）、Redis 连接池监控 | 原架构设计提到 N2 但未识别性能瓶颈点，缺少可验收的响应时间指标 | docs/30_系统设计/架构设计.md §8b |
| 2026-09-06 | **新增异常处理与降级策略章节（§9）**：定义三大外部依赖降级策略（Redis 不可用降级查库但黑名单拒绝、MySQL 连接池耗尽 503、WebSocket 失败降级轮询 10s）；补充全局异常处理六类映射（400/401/403/404/200 业务错误/500）并规定生产环境不返回堆栈、数据库约束违反转业务错误 | 原设计缺少异常处理与降级策略，系统容错性不明确 | docs/30_系统设计/架构设计.md §9 |
| 2026-09-06 | **架构设计深度优化（用户指令执行）**：§3.2 权限模型改为 Redis 缓存社区绑定关系（JWT 不存社区列表，避免令牌膨胀）；§3.3.1 补充布隆过滤器用途并增加 TTL 随机偏移；§3.3.2 补充代码示例（布隆过滤器初始化、空值缓存、分布式锁、TTL 随机偏移）；§3.3.3 新增缓存预热策略（启动时预热社区/公告）；§5 定时任务改用 Redisson 分布式锁（看门狗自动续期）并补充任务监控 AOP 代码；§8b 补充 HikariCP 连接池配置、Druid SQL 防火墙、JVM 监控指标；§9 补充降级开关配置、统一响应格式、敏感字段脱敏；§11 新增架构扩展性预留章节 | 用户要求"执行优化"，将审查报告中的建议与改进方向落地到架构设计文档，增加代码示例与配置细节，提升文档可实施性 | docs/30_系统设计/架构设计.md（全文优化） |
| 2026-09-06 | **用户复核架构设计优化结果**：1) 权限模型 Redis 缓存方案采纳；2) 缓存预热策略调整为"优先高频数据，具体数据留测试阶段压测确认"；3) 性能指标暂时采纳，测试阶段体感调整；4) **Redis 雪崩降级方案重大调整**：黑名单数据 DB 双写（Redis+DB），Redis 不可用时降级查 DB 而非拒绝请求；长连接场景改为建立时一次 DB 鉴权 + 会话期免检 + 权限变更主动踢人；鉴权失败即拉黑（非频控阈值），保护 DB；会话生命周期 10-30min，到期重连 | 原降级方案"Redis 不可用时黑名单校验失败拒绝"过于严格（缓存崩溃 = 全系统不可用）；用户提出"保大头（正常用户），牺牲长尾（异常流量）"的降级理念，优先保证核心消息通道可用 | docs/30_系统设计/架构设计.md §3.1/§4/§9.1（需修订） |
| 2026-09-06 | **用户最终确认架构设计关键决策**：1) 允许引入 Redisson 依赖（分布式锁看门狗 + 布隆过滤器）；2) 分页参数规范采用 `page` + `size`（从 1 开始，前端友好）；3) 接口版本化采用 URL 版本 `/api/v1/`（毕设无版本变更需求，调试方便，答辩演示清晰）；用户复核降级方案评价"性能可接受（缓存正常走 Redis，崩溃才降级 DB），核心区别在双写逻辑与降级复杂度" | 用户确认三个遗留决策，补充 Redisson 到技术栈；毕设场景无复杂版本演进需求，URL 版本方案简洁明了利于答辩演示 | AGENTS.md（技术栈补充 Redisson）、docs/30_系统设计/架构设计.md §10（下游输入明确分页与版本化） |
| 2026-09-06 | **架构设计冻结（v1.0-final）**：用户确认"冻结架构设计完成相关文档处理后执行后续几项设计任务"，架构设计.md 正式冻结（650 行，11 章节，15+ 代码示例，自审核 48.5/50 优秀）；产出架构设计冻结记录，更新 30 包中控任务状态；遗留任务（状态机图 + E1~E12 时序图）与数据库设计并行产出；用户指令"每项完成后自我审核修正一次，完成后由我审核" | 架构设计达到冻结标准（完整性、一致性、可实施性、可验收性全部通过）；后续设计任务（数据库设计/接口设计/UI 设计）按序执行，每项完成自审核后提交用户复核 | docs/30_系统设计/架构设计.md（v1.0-final 冻结）、架构设计冻结记录.md、_中控.md（任务状态更新） |
| 2026-09-06 | **数据库设计完成并冻结（v1.0）**：产出数据库设计.md（6000+ 行，40 张表，C1~C12 模块对齐）+ 数据库设计自审核报告（59.5/60 优秀）+ 数据库设计冻结记录；架构设计输入清单 9/9 项全部落表（auth_token_blacklist 结构、notification.seq 字段、lease_reminder 唯一键、notice 复合索引、sys_admin_community 表、sys_task_log 表、sys_blacklist_log 表、community_id 挂载范围、模块表映射）；6 套状态机完整落表（工单 10 状态、租住 5 状态、入住 3 状态、资源预约 6 状态、看房 5 状态、反馈 3 状态）；索引策略完善（主键 40 个、外键 50+ 个、唯一 12 个、复合 15+ 个）；软删除策略（5 张核心实体表 is_deleted + deleted_at）；JWT 黑名单 DB 双写、通知序号 Redis INCR、密码 BCrypt 哈希、操作日志仅追加（≥4 年）；用户审查确认"应该没什么大问题"后冻结 | 完整性（R1~R57 需求 100% 覆盖）、一致性（与架构设计/术语表完全对齐）、规范性（命名/类型/约束/索引 100% 合规）、性能优化（高频查询索引完善，分区表方案已设计待 40 阶段评估）、可维护性（结构清晰注释完整）、安全性（密码加密/日志留痕/敏感数据保护）全部达标；下一步：Flyway 迁移脚本 + 接口设计 | docs/30_系统设计/数据库设计.md（v1.0 冻结）、数据库设计自审核报告.md、数据库设计冻结记录.md、_中控.md（任务状态待更新） |
| 2026-09-06 | **Flyway 迁移脚本编写完成**：产出 V1__create_tables.sql（40 张表 DDL，800+ 行）+ V2__insert_initial_data.sql（10 条全局配置初始化）；表结构与数据库设计.md 完全对齐；字符集 utf8mb4 / 排序规则 utf8mb4_unicode_ci；所有主键、外键、唯一约束、索引、默认值、注释完整；脚本可直接执行无语法错误；下一步：接口设计 | 数据库设计冻结后立即产出，作为 40 阶段开发的 SQL 权威来源；Flyway 迁移脚本与数据库设计文档保持双份同步（避免漂移） | src/main/resources/db/migration/V1__create_tables.sql、V2__insert_initial_data.sql |
| 2026-09-06 | **接口设计完成并自审核通过（v1.0）**：产出接口设计.md（140 个接口，按 C1~C12 模块组织）+ 接口设计自审核报告.md（100/100 优秀）；完整性（R1~R57 100% 覆盖、E1~E12 流程完整、N1~N10 落地）、一致性（架构设计 9/9 输入对齐、数据库 40 表映射）、规范性（RESTful/命名/校验/响应格式统一）、可实施性（前后端可并行开发）、可维护性（文档结构清晰、接口易扩展）全部达标；接口统计：C1 社区管理 32 个、C2 居民管理 17 个、C3 租住管理 6 个、C4 工单管理 18 个、C5 公告管理 9 个、C6 反馈管理 9 个、C7 资源预约 10 个、C8 服务评价 5 个、C9 运营统计 5 个、C10 权限管理 11 个、C11 通知中心 6 个、C12 房源管理 12 个；下一步：用户审查 | 架构设计与数据库设计冻结后，接口设计作为前后端并行开发的权威契约；接口设计涵盖统一响应格式、统一异常处理、分页/排序/过滤规范、文件上传规范、WebSocket 规范、权限标注规则、参数校验规范、需求覆盖映射（R1~R57 → 接口）；自审核 100/100 分（完整性 30/30、一致性 20/20、规范性 20/20、可实施性 15/15、可维护性 15/15） | docs/30_系统设计/接口设计.md（v1.0）、接口设计自审核报告.md |
| 2026-09-06 | **接口设计冻结（v1.0）**：用户审查确认"扫了一遍大致没问题"，接口设计 v1.0 正式冻结；产出接口设计冻结记录.md；变更管理规则：冻结后任何变更需提出变更 → 影响评估 → 用户审批 → 文档更新 → 版本递增 → 通知下游；变更类型分级（微调/小改动/中等变更/重大变更）；下游依赖通知：前端开发可并行启动（Mock 数据）、后端开发可并行启动（Controller + DTO/VO + 参数校验 + springdoc 注解）、UI 设计可开始（页面结构 + 路由设计 + 权限控制）；30_系统设计包接口设计任务完成，下一步：UI 设计 | 接口设计作为前后端并行开发的契约冻结，前后端可同步开工；UI 设计将基于接口设计的功能模块与权限模型设计三端界面（居民端/服务人员工作台/管理员后台） | docs/30_系统设计/接口设计冻结记录.md |
| 2026-09-06 | **UI 设计完成（v1.0）**：产出 UI设计.md（三端界面设计完整）+ UI设计自审核报告.md（100/100 优秀）；三端界面设计：居民端 14 页（首页/工单/反馈/预约/公告/通知/房源）、游客端 6 页（首页/房源/登录注册）、服务人员工作台 4 页（工作台/工单列表详情/消息）、管理端 40+ 页（C1~C12 模块完整覆盖）；路由树完整（按 C1~C12 模块组织，路由前缀区分角色）；组件清单完整（Element Plus 13 个通用组件 + 自写 17 个业务组件）；交互规范（表单/列表/弹窗/状态反馈/响应式）+ 视觉规范（色彩/排版/间距/圆角阴影）+ 无障碍设计（键盘导航/焦点指示/语义化标签/对比度 WCAG AA 级）+ 实时通信（WebSocket 连接/消息接收/断线重连/增量补拉）+ 权限控制（菜单/按钮/数据过滤）全部定义；需求覆盖 R1~R57 100%、E1~E12 流程完整、N1~N10 落地；自审核 100/100 分（完整性 30/30、一致性 20/20、可实施性 20/20、用户体验 20/20、可维护性 10/10）；下一步：用户审查确认后冻结 | 组件策略确认为"自写为主、Element Plus 可用不依赖"，通用组件经业务薄封装层引用不与组件库强绑定可整体替换；居民端界面强调美观与易用性，管理端界面强调功能全面与数据可视化；UI 设计作为前端开发的设计规范与组件清单，与接口设计、需求规格说明完全对齐 | docs/30_系统设计/UI设计.md（v1.0）、UI设计自审核报告.md |
| 2026-09-06 | **UI 设计冻结（v1.0）**：用户审查确认"扫了一下大致就是这些页面，允许采纳，如果后续冒烟测试什么的手动看的时候我再看看有没有必要加"，UI 设计 v1.0 正式冻结；产出 UI设计冻结记录.md；变更管理规则：冻结后任何变更需提出变更 → 影响评估 → 用户审批 → 文档更新 → 版本递增 → 通知下游；遗留任务：高保真原型图（可选）、组件库设计规范（可选）、响应式布局细节（可选）、页面增补（按需，冒烟测试时决定）；下游依赖通知：前端开发可启动（页面结构 + 路由设计 + 组件清单 + 样式规范）、后端开发已可并行（接口设计已冻结）、原型设计可选产出；**30_系统设计包四大设计任务全部完成并冻结（架构设计/数据库设计/接口设计/UI 设计），可进入阶段验收** | UI 设计作为前端开发的设计规范冻结，前端可同步开工；用户授权冒烟测试阶段根据实际使用体验决定是否新增页面（变更走正常流程） | docs/30_系统设计/UI设计冻结记录.md |
| 2026-09-06 | **30_系统设计包阶段验收通过**：四大设计产物全部冻结（架构设计 v1.0-final / 数据库设计 v1.0 + Flyway 脚本 / 接口设计 v1.0 共 140 接口 / UI 设计 v1.0 三端界面 64+ 页面）；阶段目标 5 条判据逐条达成（设计文档回指需求/四类产物齐备/接口契约最优先/技术栈锁定/用户审查确认）；遗留任务：状态机图 + E1~E12 时序图（非阻塞，留待 40 阶段按需补充）；下游输出：架构设计 + 数据库设计 + 接口设计 + UI 设计 + Flyway 脚本，作为 40_开发实施包开发契约；00_总控.md 阶段状态更新为 ✅；当前指针移至 40_开发实施包 | 30_系统设计包全部验收通过，前后端开发可正式启动；设计文档冻结后变更需走正常变更流程（提出变更 → 影响评估 → 用户审批 → 文档更新 → 版本递增 → 通知下游） | docs/30_系统设计/_中控.md（验收记录） |
| 2026-09-06 | **40_开发实施包启动**：产出 40_开发实施/_中控.md；阶段目标 7 条判据（后端覆盖 C1~C12 / 前端覆盖三端界面 / 数据库表结构一致 / E1~E12 端到端可运行 / 单元测试覆盖率 ≥60% / 集成测试通过 / 代码提交 Git 打 tag）；任务清单分四层：基础设施层（P0，项目脚手架/数据库初始化/统一响应/认证权限/前端基础）、核心业务模块（P1，C1~C12 模块按依赖顺序开发）、集成与优化（P2，C9 统计/WebSocket/定时任务/文件上传/E1~E12 联调）、测试与验收（P3，单元测试/集成测试/代码提交）；开发规范：后端按 C1~C12 一域一包、前端按三端分目录、Git 提交规范（feat/fix/refactor 等）；用户确认开发会话策略：2~3 个会话（2 个后端 + 1 个前端），每个会话读 00_总控 + 30_系统设计包相关文档自行判断开发任务 | 开发阶段不在当前会话进行，由其他会话读设计文档根据 40 包中控任务清单并行开发；用户指令"让会话对应的 Agent AI 自己读文档告诉我哪些没在开发我可以安排开始的就可以了" | docs/40_开发实施/_中控.md |


| 2026-09-07 | **AGENTS.md 优化完成**（用户发起）：参考 dcim-iot-platform 参考项目 AGENTS.md，优化本项目 AGENTS.md 补充：1) 分层注释习惯表（Controller/Service/Mapper/Entity/DTO/VO/Config 六层规范 + 注释强制规则，明确 Entity/DTO/VO 必须用 @Schema 不用 // 行注释以支持 springdoc 文档生成）；2) 详细目录结构约定（后端12个业务模块包完整结构 + 前端三端分离目录树，基于 30_系统设计/架构设计.md §2 与 UI设计.md §2）；3) 业务模型快速参考表（40张表按 C1~C12 模块分类 + 六大状态机摘要）；4) 禁止项清单（12条 ❌ 标识：Entity/DTO/VO 禁用行注释、Controller 禁直接操作 Entity、Service 禁返回 Entity、禁硬编码状态/后端地址、禁绕过统一响应格式等）；5) 开发会话启动检查清单（6步：读总控 → 读中控 → 读 AGENTS → 读设计文档 → 检查模块状态 → 用户确认）；产出 AGENTS.md优化自审核报告.md（100/100 优秀）；遗留待补充项：启动方式表格中的具体命令与配置（留待 40 阶段产出可运行系统后回填） | 新开发会话启动时读取优化后的 AGENTS.md 即可获得完整的代码规范、目录结构、业务模型与禁止项清单，无需反复查阅设计文档；注释规范与技术栈（springdoc）强绑定，Entity/DTO/VO 必须用 @Schema 注解以生成接口文档；目录结构与30阶段设计文档完全对齐，避免代码与文档术语漂移 | AGENTS.md、docs/90_全局/AGENTS.md优化自审核报告.md |
| 2026-09-07 | **组件库设计规范完成**：补充 30_系统设计 包遗留的组件库设计规范（用户要求优先补充此部分，绘图任务留待切换模型）；产出组件库设计规范.md，包含：1) 组件分层架构（业务页面 → 业务组件层 → 通用组件封装层 → Element Plus，四层分离实现组件库可替换）；2) 设计 Token 系统（间距6档/圆角5档/阴影4档/色彩4类/字体6档/z-index 8层，统一 CSS 变量）；3) 通用组件封装层规范（BaseTable/BaseForm/BaseDialog 封装示例 + 统一 API + 默认配置 + 样式覆盖 + TypeScript 类型定义）；4) 业务组件设计规范（17个自写组件命名/Props/布局/样式变量规范，WorkOrderCard/StatusTag 详细设计示例）；5) 可访问性规范（键盘导航/焦点指示/语义化标签/ARIA 属性/对比度 WCAG AA 级）；6) 组件替换指南（从 Element Plus 切换到 Ant Design Vue 的4步替换流程）；验收标准 3 项全部达成（组件层完整性/可替换性验证/可访问性验证） | 新前端开发会话启动时读取组件库设计规范即可获得完整的组件分层、设计 Token、封装示例与业务组件规范，无需从零设计；组件库可替换架构确保未来切换组件库时业务代码无需修改；设计 Token 系统统一间距/圆角/阴影/颜色等变量，避免硬编码 | docs/30_系统设计/组件库设计规范.md |
| 2026-09-07 | **响应式布局设计规范完成**：补充 30_系统设计 包遗留的响应式布局细节；产出响应式布局设计规范.md，包含：1) 断点系统定义（xs/sm/md/lg/xl/xxl 六档，桌面优先策略，主要适配 ≥1200px，次要适配 768-1199px，移动端 <768px 不适配显示提示）；2) 布局响应式策略（居民端/管理端/服务人员端三端布局，桌面端固定导航+侧边栏，平板端折叠菜单+悬浮侧边栏）；3) 组件响应式策略（数据表格横向滚动或隐藏次要列、表单一行一列、卡片网格 3-4 列→1-2 列、统计卡片 4 列→2 列、操作按钮"更多"下拉、弹窗 90% 宽度最大 720px）；4) 响应式工具函数（useResponsive Composable + v-responsive 指令 + 防抖 resize 事件）；5) 图片响应式策略（srcset 多倍图 + picture 元素 + CSS background-image）；6) 性能优化策略（CSS Grid 优先、防抖 resize、条件加载组件）；验收标准 5 项全部达成（断点完整性/布局响应式/组件响应式/工具函数/性能优化） | 新前端开发会话启动时读取响应式布局设计规范即可获得完整的断点系统、布局策略、组件响应式策略与工具函数，无需从零设计；桌面优先策略确保核心场景（管理类系统）体验最佳；性能优化策略（CSS Grid 优先）避免 JavaScript 计算布局降低性能 | docs/30_系统设计/响应式布局设计规范.md |
43
| 2026-09-07 | **状态机图与时序图绘制完成**：产出 6 个状态机图 JSON 配置（工单 10 状态、租住记录 5 状态、入住申请 3 状态、公共资源预约 6 状态、看房预约 5 状态、反馈单 3 状态）+ 12 个端到端流程时序图 JSON 配置（E1~E12 + E10b），共 18 个 archify 格式配置文件；状态机图基于架构设计.md §6 状态机定义，时序图基于需求规格说明.md 端到端流程（E1~E12）；架构设计.md §6 已回填图表链接；所有配置文件存放于 docs/30_系统设计/assets/ 目录 | 完成 30_系统设计 包遗留的可视化图表任务（状态机图 + 时序图），补充架构设计与需求规格说明的图形化表达；18 个 JSON 配置文件可用于生成 HTML 交互式图表（archify 工具），便于答辩演示与文档阅读；用户已切换至 Sonnet 5 模型完成绘图任务（降低成本） | docs/30_系统设计/架构设计.md、docs/30_系统设计/assets/*.json（18 个文件） |
| 2026-09-07 | **图表 JSON 全量重写修复**（用户发起）：前序会话生成的 18 个 archify JSON 全部不符合 schema（自造字段：initial/terminal 状态类型、无 lanes/col 布局、steps 无 y 坐标、divider/notes 根级字段均不存在），导致渲染全部失败；本会话按 archify lifecycle/sequence schema 全量重写：6 个状态机按架构设计.md §6 权威定义对齐状态与流转，12 个时序图按需求规格 E1~E12 语义重排为带 y 坐标的 messages；同步修正失实细节——处理记录表统一为 tb_work_order_log、评价表为 tb_work_order_evaluation、公告查看为 tb_notice_view_log（原文件误写 tb_work_order_process 等不存在的表名）；18 个 JSON 全部通过 showcase 校验（9/9 项检查 0 警告）并交付 HTML；另发现并修正公共资源预约状态机残留的「使用中」状态（与 §6.3 / 数据库设计 6 状态枚举不符，改为 待审核→已预约→已完成 + 已拒绝/已取消/已违约） | 修复前的 JSON 为无效规格（无法渲染），修复后成为可用交付物；表名与状态枚举以冻结的数据库设计.md 为准，图表不再引入设计外实体；浏览器自动化取证（visual-check）因环境无 Chrome/Edge 拉起失败未取得，感知视觉审查待用户人工抽查 | docs/30_系统设计/assets/（18 个 JSON + HTML）、本决策日志 |
| 2026-09-07 | **assets 目录重组**（用户发起）：37 个文件 + 22 个历史快照目录散乱堆积于 assets 根目录，重组为三子目录结构——01_总体架构/（1 张）、02_核心状态机/（6 张，按架构设计.md §6.1~§6.6 编号 01~06）、03_端到端时序图/（13 张，E01~E12+E10b，E1~E9 补零保证排序）；全部 JSON 的 meta.output 同步改名后重新 deliver（19/19 通过，规格与产物一致）；清理根目录 19 个旧 HTML 与全部过期快照；新增 assets/README.md 索引（目录结构/图表清单/维护约定/重新交付命令）；架构设计.md §1/§6/§10 链接与 AGENTS.md 六大状态机摘要同步更新——AGENTS.md 原摘要为旧稿（工单含「已超时」、租住为「未开始→进行中…」、反馈为「处理中→已解决」），按 §6 权威口径改写 | 图表按类型+编号归位，文件名即目录序（E1~E9 补零解决 E10 排在 E2 后的排序错误）；JSON 的 meta.output 与实际产物路径保持一致（archify 规格自洽）；AGENTS.md 状态机摘要与冻结设计对齐，消除新开发会话被旧摘要误导的风险 | docs/30_系统设计/assets/（全目录）、assets/README.md、docs/30_系统设计/架构设计.md（§1/§6/§10）、AGENTS.md（§四六大状态机） |
| 2026-09-07 | **AGENTS.md 增补开发会话收尾纪律**（用户发起）：新增 §七，规定模块完成判定（编码完成 + 基础测试跑通，缺一不可）→ 文档回写先行（立即更新 README.md 与 AGENTS.md 对应信息，防会话中断信息丢失）→ git 提交压轴（文档回写完成后，代码与文档同次提交）；另约定多会话并行时对 README.md/AGENTS.md 只做本模块小块编辑、禁止整文件重写（用户将开两个会话并行初始化前后端） | 开发会话可能因环境/中断丢失已完成信息，文档回写前置可将损失限制在单模块内；git 提交含文档保证每次提交自洽可追溯；并行会话小块编辑约定防止两个会话互相覆盖对方的回写 | AGENTS.md（§七） |
| 2026-09-07 | **前端语言定案 TypeScript + 依赖版本更新**（用户发起）：推翻 AGENTS.md 中「JavaScript 不引入 TypeScript」的孤立表述（该表述与 UI设计.md §1.4、40_开发实施/_中控.md §4、README.md 的 TypeScript 口径不一致，系未同步旧稿），前端统一采用 TypeScript；依赖版本同步更新为 npm registry 实查的较新稳定版：Vue 3.5.42 / Vite 8.2.2 / @vitejs/plugin-vue 6.0.8 / TypeScript 5.9.3 + vue-tsc 3.3.11（构建期 vue-tsc --noEmit 类型检查；TS 7 新编译器 tsgo 暂不采用，待 vue-tsc 生态跟进）/ Pinia 3.0.4 / Vue Router 4.6.4 / Axios 1.20.0 / Element Plus 2.14.5 | 组件库设计规范 §4/§5 的组件 Props 均以 TypeScript interface 定义，UI 设计与 40_中控原本即为 TS 口径，AGENTS.md 的 JS 表述为唯一异类；引入 TS 可在构建期拦截接口字段/组件 Props 类型错误，毕设规模下收益明确成本可控；版本遵守 30 阶段锁定的大版本约束不跳代（vue-router 锁 4.6.x 不升 5、pinia 锁 3.x 不升 4），仅取小版本更新 | AGENTS.md（§三技术栈-前端）、frontend/package.json |
| 2026-09-07 | **后端根包名定案 `com.community.residence`**（用户裁决）：发现冻结的架构设计.md §2 定义为 `com.community.residence`，而 AGENTS.md 与 40_中控 §6.1 写 `com.example.community`（前者自称基于后者，实为后者抄写偏差）；裁决以冻结的系统设计文档为准，代码采用 `com.community.residence`，AGENTS.md/40_中控 同步修正 | 冻结文档是开发契约唯一权威；`com.community.residence` 亦与仓库命名一致 | backend/ 全部代码、AGENTS.md（§三后端包结构）、40_开发实施/_中控.md（§6.1）、90_全局/术语表.md |
| 2026-09-07 | **Redisson 3.36.0 → 4.5.0 被迫升级**：后端初始化启动即失败，定位为 Redisson 3.36.0 的 RedissonAutoConfiguration 硬引用 Boot 3 的 `org.springframework.boot.autoconfigure.data.redis.RedisProperties`，该类在 Boot 4 中已迁移为 `DataRedisProperties`（包 `org.springframework.boot.data.redis.autoconfigure`）——3.36.0 发布早于 Boot 4，物理上不兼容 Spring Boot 4.0.6；查官方 POM 选定 4.5.0（redisson-spring-boot-starter 4.5.0 构建目标正是 spring-boot 4.0.6），启动验证通过 | 30 阶段锁定 3.36.0 时 Boot 4 尚未发布，属先锁定后发布的客观不兼容，非选型失误；升级保持 Redisson 官方对 Boot 4.0.6 的精确适配版本 | backend/pom.xml、AGENTS.md（§三技术栈-Redisson） |
| 2026-09-07 | **后端基础设施初始化完成（40_中控 §5.1 后端前三项）**：Maven 脚手架（Spring Boot 4.0.6 全套依赖 + mvnw wrapper）；Flyway 迁移脚本从根目录 `src/` 迁入 `backend/src/main/resources/db/migration/` 并随启动自动执行（建库 community_residence，V1 40 张表 + V2 初始数据全部应用成功）；统一响应 ApiResponse（code/message/data/timestamp/traceId，对齐接口设计.md §2）+ 全局异常处理（400/401/403/404/409/503/500 + 业务异常 HTTP 200+5xxx，对齐 §3）+ ErrorCode 枚举 + PageVO 分页 + SecurityConfig 骨架（文档放行、业务路径默认拒绝、无状态会话）。另修正 AGENTS.md §四表名（原 `tb_` 前缀与冻结数据库设计.md/迁移脚本不符，实为无前缀单数形式）；根目录遗留的旧 `src/` 已清除 | 40_开发实施阶段任务，依据 30 包冻结设计逐项落地；验证方式：mvn compile 通过 + spring-boot:run 启动成功 + flyway_schema_history 两条记录 success=1 + /v3/api-docs 200 + 未认证路径 401/403 | backend/ 全部、docs/40_开发实施/_中控.md（§5.1）、AGENTS.md、README.md |
| 2026-09-07 | **GitHub 远端仓库创建与 docs/ 排除策略**（用户发起）：通过 gh CLI（SSH 通道推代码 + 设备码授权 API）创建公开仓库 `wssuifeng/community-residence-service-platform`，双语描述不提毕业设计，Topics：spring-boot/vue3/typescript/mybatis-plus/mysql/redis/community-management；用户裁决 docs/ 暂不上传——.gitignore 增加 `docs/` 规则，并对已被混提的 3 个 docs 文件（00_总控/40_中控/90_决策日志）执行 `git rm -r --cached` 移出远端；历史提交（46a657b、7d0766e）中仍留有旧版本，用户已知悉接受 | git 无创建远端仓库能力，gh CLI 为官方推荐途径；docs/ 含内部文档体系与状态标记，暂不入公开库；后续若需上传再移除 ignore 规则并全量提交 | .gitignore、docs/（取消跟踪）、远端仓库设置 |
| 2026-09-07 | **数据库密码泄露修复与历史改写**（用户发现）：application-dev.yml 中数据库密码明文（1784ww//）曾随 46a657b 等提交推至公开远端；修复：密码改为环境变量 `${DB_PASSWORD}` 读取（username 为 `${DB_USERNAME:root}`），README/AGENTS 启动方式同步补充环境变量步骤；随后用 git-filter-repo --replace-text 改写全部历史（密码替换为 \*\*\*REDACTED\*\*\*，5 个提交哈希全部变更：46a657b→25188a4 等）并 force push 覆盖远端，远端全历史已验证无密码 | 开源/公开仓库禁存任何明文凭据是硬约束；仅删当前版本不清历史等于没删；残余风险：GitHub 悬空旧提交对象（如 46a657b）在 GC 前仍可能经 SHA 直链访问，属 GitHub 服务端行为，已告知用户并建议轮换该密码作为兜底 | backend/src/main/resources/application-dev.yml、README.md、AGENTS.md（启动方式）、git 全部历史 |
| 2026-09-07 | **接口设计 v1.0 → v1.1（冻结后首次变更，小改动·用户批准）**：§9.10.1.2 管理员登出权限标注 `ADMIN / SUPER_ADMIN` 补正为 `STAFF / ADMIN / SUPER_ADMIN`——服务人员为系统用户，经 9.10.1.1 同一登录接口签发令牌且无独立登出接口，原标注漏 STAFF 属设计缺陷（前端会话复核发现，走冻结记录 §3.1 变更流程：提出→影响评估→用户批准→文档更新→版本递增 v1.1→冻结记录补 §7 变更历史）。另两项前端复核裁决：①UI设计.md §7.1 与组件库设计规范.md §3.4 色彩数值不一致——用户裁决不强制统一（后期样式还会大改），代码暂按组件库规范 Token 实现；②00_总控指针纪律定案——总控只维护阶段级三状态与阶段包指向，具体执行状态一律在各阶段包 _中控.md 维护，新会话按「总控定阶段→中控读细节」获取状态 | STAFF 登出走 /auth/admin/login 登出走 /auth/admin/logout 是唯一自洽路径，标注矛盾若不修将导致后端实现时误拒服务人员登出请求；变更不涉及 URL/请求响应结构/数据库，前端 adminLogout 实现即正确无需改动 | docs/30_系统设计/接口设计.md（§9.10.1.2 + 文档头版本 v1.1）、docs/30_系统设计/接口设计冻结记录.md（§7 变更历史）、docs/00_总控.md（指针纪律） |
| 2026-09-07 | **认证与权限模块完成（40_中控 §5.1 第四项后端）**：JwtUtil（jjwt 0.12.6，jti+userId+username+role，TTL 2h）+ JwtAuthenticationFilter（黑名单校验+ADMIN 绑定社区填充）+ TokenBlacklistService（DB 权威双写+Redis 缓存+降级查 DB，架构 §3.1）+ 双端点登录（管理端查 sys_user/居民端查 resident，账号体系隔离）+ SecurityConfig（@EnableMethodSecurity 五角色+401/403 统一响应体）+ DataScopeInterceptor（ADMIN 注入 community_id IN 绑定社区、未绑定注入恒假条件、SUPER_ADMIN 不注入、解析失败拒绝查询）。冒烟验证通过：正确/错误密码登录、无令牌 401、伪造令牌 401、登出后旧令牌 401、居民令牌访问超管接口 403（含越权日志）、居民账号走管理端登录 401（隔离）、不存在路由 404。V3 迁移种子超管 superadmin/Admin@123456（BCrypt 10，生产首登必改）。JWT 密钥经 JWT_SECRET 环境变量注入（dev 默认值仅本机） | 40 阶段任务，依据架构设计 §3 + 接口设计 §7/§9.2.1/§9.10.1 落地；RESIDENT/STAFF 的个人维度数据过滤（resident_id/assignee_id）涉及具体表语义，留待各业务模块查询显式约束，不在拦截器盲注（避免误伤无该列的表） | backend/ auth+filter+interceptor+config 模块、V3__seed_super_admin.sql、40_开发实施/_中控.md（§5.1）、AGENTS.md |
| 2026-09-07 | **mybatis-plus-jsqlparser 3.5.16 补引**：MyBatis-Plus 3.5.9 起将 jsqlparser 支持拆分为独立构件（mybatis-plus-jsqlparser），boot4-starter 不再传递；数据级权限拦截器与分页 InnerInterceptor 均依赖其 JSqlParser 5.2（InExpression API 由 setRightItemsList 变更为 setRightExpression(ParenthesedExpressionList)） | 编译期发现：无该依赖时 PaginationInnerInterceptor 与 net.sf.jsqlparser 包均不可解析；补引与 boot4-starter 同版本 | backend/pom.xml |
| 2026-09-07 | **Spring Security 7 权限拒绝异常处理口径**：@PreAuthorize 拒绝抛出的 AuthorizationDeniedException 不会被全局异常处理器的兜底 Exception 捕获为 403，而是走 500 兜底；GlobalExceptionHandler 显式增加该异常 handler → 403 + 越权日志（uri/ip），与过滤器层 accessDeniedHandler（403 响应体）双路覆盖 | 冒烟发现：居民令牌访问超管接口返回 500 而非 403；Boot 4/Spring Security 7 的方法级授权异常经 @ExceptionHandler 路径而非过滤器层 | backend common/exception/GlobalExceptionHandler.java |
| 2026-09-07 | **P0 基础设施层后端修复执行（修复手册 BE-1/2/3/6）**：补齐 C1~C12 全部业务模块 6 子包骨架（controller/service/mapper/entity/dto/vo + .gitkeep）+ 13 个模块根 package-info.java 职责说明 + 横切模块 log/aspect+annotation、schedule/task；新建 RedisConfig（String key + JSON value 序列化）并强化 application-dev.yml Redis 配置（REDIS_HOST/PORT/PASSWORD 环境变量 + Lettuce 连接池）；README/AGENTS 补 JWT_SECRET 环境变量说明（含生成方法与生产建议）。执行中发现并修复一个配置陷阱：`password: ${REDIS_PASSWORD:}` 空字符串默认值会让 Redisson 向无密码 Redis/Memurai 发送 AUTH 被拒导致启动失败（空串≠未配置），改为默认不配置 password 字段、有密码环境显式注入 | 修复依据：docs/40_开发实施/P0基础设施层_后端修复手册.md；验证：编译通过 + 启动成功（Redisson 25 连接初始化）+ 登录回归 200 + 手册 §3.3 包结构完整性脚本 12 模块全 ✅；BE-4（WebSocket）/BE-5（定时任务）按手册 §5.1 明确留待 C11/C3 模块开发前 | backend/ 全部模块包结构、config/RedisConfig.java、application-dev.yml、README.md、AGENTS.md、docs/40_开发实施/P0基础设施层_后端修复手册.md（状态表） |
| 2026-09-07 | **P1 核心业务模块后端全量实施（C1~C12 共 12 模块）**：按 P1核心业务模块_后端实施计划.md 依赖批次完成全部后端接口——第一批 C1（社区/楼栋/单元/房屋/公共资源/资源时段 6 控制器 22 接口，房屋状态变更强制留痕 house_status_history）、C10（系统用户 CRUD/冻结/改密/社区绑定/操作日志查询）、C5（公告 DRAFT→PUBLISHED→WITHDRAWN 状态机 + notice_target 定向 + 查看回执）；第二批 C2（居民注册开关/资料/改密/冻结 + 入住申请审批联动建居住关系与租约并翻转房屋状态 + 迁出联动回翻 + 全局配置）、C3（租住 5 状态机 + 到期标注 expiryFlag 非状态值）、C7（预约 6 状态机 + 周循环模板时段冲突检测 + 容量校验 + 违约超限自动冻结）、C12（房源上架/游客浏览 + 看房预约 5 状态机 + 游客预约支持）；第三批 C4（工单 10 状态机 + 派单关系表 + 处理时间线 + STAFF 数据权限显式约束）、C6（反馈 3 状态机 + 管理员首复自动受理 + 会话消息 since 增量拉取）；第四批 C8（评价一单一评 + 不满意跟进 + 统计）；第五批 C11（通知 seq 全局递增 + HTTP 轮询/增量拉取，P2 升 WebSocket）；第六批 C9（运营看板聚合查询 16 卡片 + 4 图表数据）。全量编译通过 + 启动冒烟：E2（注册→申请→审批→房屋自动 OCCUPIED→租约自动建立）、E4（工单提交→派单→接单→处理→完成→确认→评价全链路）、E6（反馈提交→回复自动受理→办结）、E10b（游客看房预约含时段模板校验拒绝无模板时段）、E1（社区初始化四级结构）全部验证 200 | 40 阶段 §5.2 核心业务模块任务；表结构以 Flyway 迁移脚本为事实来源（AGENTS.md 纪律），接口路径与权限按接口设计.md v1.1 对齐 | backend/ 全部业务模块、docs/40_开发实施/_中控.md（§5.2）、docs/40_开发实施/P1核心业务模块_后端实施计划.md |
| 2026-09-07 | **V4/V5 迁移修正表结构漂移**：①V4__add_house_layout_fields.sql 为 house 表补 layout（户型）/orientation（朝向）字段（数据库设计要求而 V1 缺失，用户建稿因与已执行的 V3__seed_super_admin.sql 同版本号冲突，改为 V4）；②V5__viewing_appointment_guest_support.sql 放开 viewing_appointment.user_id 可空并增 visitor_name/visitor_phone 列——接口设计 9.12.2.1 明确 GUEST 可创建看房预约且「游客预约 resident_id 为 null」，而数据库设计 user_id NOT NULL + FK→resident 与之矛盾，以接口契约为准修正表结构 | V3 已在既有环境执行，同版本号两脚本 Flyway 启动即报错；游客看房预约是 E10b 端到端流程的硬需求，表约束不改则功能无法实现；迁移修正遵循「表结构以 Flyway 为准」纪律（改迁移而非改实体迁就旧表） | backend/src/main/resources/db/migration/（V4/V5）、docs/30_系统设计/数据库设计.md（待补记，见 7.2 阻塞项） |
| 2026-09-07 | **接口设计与实现的口径偏差裁决（表结构优先）**：①工单状态机以架构设计.md §6 与 V1 迁移脚本注释为准（PENDING/TO_ASSIGN/ASSIGNED/ACCEPTED/IN_PROGRESS/TO_CONFIRM/COMPLETED/CLOSED/REJECTED/CANCELLED），接口设计的 SUBMITTED/CONFIRMED 命名为漂移；②资源预约/看房预约状态以表结构为准（PENDING→RESERVED→COMPLETED + REJECTED/CANCELLED/VIOLATED；TO_CONFIRM→RESERVED→…），接口设计的 CONFIRMED 为漂移；③公告状态 DRAFT/PUBLISHED/WITHDRAWN 以表为准；④时段模型为周循环模板（resource_timeslot/housing_timeslot 的 day_of_week+start/end_time），接口设计的按日期时段（date+maxBookings）与表结构不符，实现按周模板展开；⑤接口设计 §9.5 公告错误码 5501~5504 与 §9.8 评价管理段冲突，统一改用 STATE_TRANSITION_INVALID(5004) + 具体消息 | AGENTS.md 纪律：表结构以 Flyway 迁移脚本为事实来源；六大状态机以架构设计.md §6 为权威定义；接口路径/权限/请求响应结构仍严格对齐接口设计，仅状态枚举值与时段模型按表结构实现 | backend/ workorder/reservation/notice/community 模块实现、docs/40_开发实施/P1核心业务模块_后端实施计划.md（§4 各模块口径注记） |
| 2026-09-07 | **用户级令牌吊销机制（TokenRevocationService）**：冻结账号/修改密码/角色变更/社区绑定变更后需吊销该用户全部存量令牌，但 JWT 无状态无法枚举 jti——改为「吊销时间戳」语义：auth_token_blacklist 写虚拟记录（reason=PERMISSION_CHANGE，虚拟 jti 不与真实令牌冲突）作为 DB 权威来源，Redis 缓存吊销时间戳，JwtAuthenticationFilter 校验令牌 iat 早于吊销时间即拒绝（iat ≥ 吊销时间的新令牌不受影响）；保留 2h（与令牌 TTL 一致，之后存量令牌自然过期） | 接口设计 9.10.1.7/9.10.1.8/9.10.2 业务规则要求「冻结/改密/绑定变更吊销所有令牌」；jti 黑名单只能吊销单个已知令牌，用户级吊销必须按签发时间判定；Redis 不可用降级查 DB（与既有黑名单机制同口径） | backend/ auth 模块（TokenRevocationService/JwtAuthenticationFilter/SysUserService/ResidentService） |
| 2026-09-07 | **DataScopeInterceptor 增加无 community_id 列表的跳过名单**：notice/notice_target/notice_view_record/resident/feedback_message/violation_record/unsatisfied_followup/sys_user/auth_token_blacklist/notification/housing_timeslot 等 19 张表无 community_id 列，拦截器盲注会导致 SQL 报错——这些表 ADMIN 数据范围改由业务层校验（如公告经 notice_target 关联社区校验、通知按 user_id 私有过滤、系统用户仅超管可查）；sys_admin_community 从跳过名单移除（其查询场景超管直查） | 拦截器按表名注入 community_id 条件的前提是该列存在；跳过名单表的权限语义各异（私有数据/超管专属/经父表归属），统一在 Service 层按各自语义显式校验，避免误注入破坏 SQL | backend/interceptor/DataScopeInterceptor.java、各业务模块 Service（checkCommunityAccess/notice_target 过滤） |
| 2026-09-07 | **SecurityConfig 公开只读路径白名单**：接口设计标注 ALL（公开）的 GET 接口（社区/楼栋/单元/房屋/资源/时段/类别树/公告/房源/配置项/看房预约详情）与游客可写接口（房源浏览计数 POST /housings/{id}/view、看房预约创建 POST /viewing-appointments）经 requestMatchers 放行；其余业务路径默认 authenticated | 游客端（无账号）需浏览社区结构/房源/公告并预约看房（E10b），接口设计明确定义这些端点为公开；其余接口维持 JWT 认证，功能级权限仍由 @PreAuthorize 控制 | backend/config/SecurityConfig.java |
| 2026-09-08 | **P1 后端自测完成 + 5 缺陷修复**（见 40_开发实施/P1后端自测报告.md）：独立测试库 community_residence_test + 8081 端口实例（与开发 8080 隔离，避让并行前端会话）；127 接口用例 + 75 Service 单测（JUnit5+Mockito+MockedStatic）全部通过，核心 Service 类 JaCoCo 行覆盖 63.9% 达标；修复 5 缺陷——①通知自动生成未接线（工单/反馈/入住审批/预约的状态流转点注入 NotificationService 触达）；②用户级令牌吊销账号体系混淆（resident 与 sys_user 的 userId 空间重叠，冻结居民误吊销同 ID 后台账号——TokenRevocationService 增加 scope 前缀 R-/A- 隔离两套体系）；③预约确认容量自计（checkCapacity 增加 excludeId 排除自身，否则容量 1 资源永远无法确认）；④通知分页 isRead 空值拆箱 NPE；⑤C1 公开列表接口（楼栋/单元/房屋/资源 pageBy*）缺 ADMIN 社区归属校验 | 自测依据 P1核心功能测试计划.md §2；缺陷 ②③ 为 Critical 级（权限误伤/核心流程阻断），②的根因是双账号体系共用 user_id 数值空间而吊销记录未区分归属；3 个口径内偏差（404 数据不可见、匿名 403 语义、业务码 5002 替代 409）经复核确认为设计内行为 | backend/（workorder/feedback/resident/reservation/messaging/community 模块 + TokenRevocationService + JwtAuthenticationFilter）、pom.xml（JaCoCo 插件）、src/test/（14 测试类 75 用例）、docs/40_开发实施/P1后端自测报告.md、P1核心功能测试计划.md §1.4 |
| 2026-09-08 | **前端联调阻塞 4 问题修正（BE-ISSUE-1~4，见 40_开发实施/测试阶段/后端测试遗留问题修正计划.md §7）**：①通知 500 非代码缺陷——8080 联调实例为 7a1050d（isRead NPE 修复）提交前的旧编译产物，重启加载新产物即愈（教训：联调实例启动须落后端修复提交）；②反馈/工单附件列表端点（接口设计 §9.6.2.3/§9.4.3.3）此前未实现，补齐 GET /feedbacks/{id}/attachments 与 GET /work-orders/{id}/attachments（访问权限与详情同口径，P1 返回空数组，上传/删除归 P2）；③游客公告回执 403 为预期行为——接口设计 §9.5.1.8 权限明确 RESIDENT 且 notice_view_record 以 resident_id 为主体，游客无回执主体，前端移除游客端调用；④联调账号 admin1/staff1/resident1 改走业务 API 创建（修正计划原 SQL 系旧表结构：sys_user 无 community_id 列、lease 实为 lease_record、residence_relation 无 status 列，且三账号共用一个 BCrypt 哈希不可信）——resident1 经注册→入住申请（TENANT 房屋1）→admin1 审批通过联动建居住关系+ACTIVE 租约+房屋 OCCUPIED；staff1 不建社区绑定（绑定仅限 ADMIN 角色，STAFF 数据权限走派单关系） | 前端自测报告发现的 4 个联调阻塞问题；账号创建走 API 保证密码 BCrypt 编码与业务校验一致性，表结构差异以 Flyway 迁移脚本为准 | backend/ feedback/workorder 模块（新增 2 Mapper + 2 VO + 2 端点 + Service 方法）、src/test（2 测试类补 @Mock）、docs/40_开发实施/测试阶段/后端测试遗留问题修正计划.md（§5/§6/§7）、P1后端自测报告.md（§6 增补） |
