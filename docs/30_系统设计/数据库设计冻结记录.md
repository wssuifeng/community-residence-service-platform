# 数据库设计冻结记录

> 冻结时间：2026-09-06  
> 冻结版本：v1.0  
> 冻结人：用户确认，AI 执行  
> 冻结依据：自审核通过（59.5/60 优秀），用户审查无异议

---

## 一、冻结决策

**决策**：数据库设计 v1.0 正式冻结，作为 40_实现阶段的权威依据。

**理由**：
1. ✅ 自审核得分 59.5/60（优秀），完整性、一致性、规范性、性能优化、可维护性、安全性全部达标
2. ✅ 架构设计输入清单 9/9 项全部完成
3. ✅ 需求覆盖率 100%（R1~R57 全部映射到表/字段）
4. ✅ 6 套状态机完整落表
5. ✅ 用户审查确认"应该没什么大问题"

**用户反馈**：
> "大概扫了一遍应该没什么问题，若果有问题后续再修一修就行了，应该没什么大问题，执行冻结和下一步吧。"

---

## 二、冻结内容清单

| 文档 | 版本 | 行数 | 状态 |
|------|------|------|------|
| 数据库设计.md | v1.0 | 6000+ | ✅ 冻结 |
| 数据库设计自审核报告.md | v1.0 | 495 | ✅ 归档 |
| 数据库设计冻结记录.md | v1.0 | 本文件 | ✅ 归档 |

---

## 三、设计成果总结

### 3.1 表结构统计

- **总表数**：40 张（47 张计划表中，7 张合并优化）
- **业务模块**：C1~C12 共 12 个模块 + 1 个横切模块
- **核心实体**：community, building, unit, house, resident, work_order, notice, feedback 等
- **关联表**：residence_relation, sys_admin_community, notice_view_record（M:N 关系）
- **日志表**：sys_operation_log, sys_task_log, house_status_history, work_order_process

### 3.2 关键设计特性

| 特性 | 实现方式 | 对应需求 |
|------|---------|---------|
| 多社区数据隔离 | 21 张表挂 community_id | 架构设计 §3.2 |
| JWT 黑名单 DB 双写 | auth_token_blacklist 表 | 架构设计 §3.1 |
| 通知增量补拉 | notification.seq 字段（Redis INCR） | R50 |
| 租期到期提醒去重 | lease_reminder 表，uk_lease_status | R15 |
| 软删除保护 | 5 张核心实体表（is_deleted + deleted_at） | R6 |
| 操作日志留痕 | sys_operation_log（仅追加，≥4年） | R47, N6 |
| 预约冲突校验 | 复合索引 + 应用层锁 | R33, R55 |
| 密码安全存储 | password_hash（BCrypt） | N4 |
| 6 套状态机 | VARCHAR(20) 状态字段 + 枚举值 | 架构设计 §6 |

### 3.3 索引策略

- **主键索引**：40 个（自动创建）
- **外键索引**：50+ 个（全覆盖）
- **唯一索引**：12 个（业务约束）
  - auth_token_blacklist.jti
  - lease_reminder(lease_id, status)
  - work_order_evaluation.work_order_id
  - sys_admin_community(admin_id, community_id)
  - notice_view_record(notice_id, user_id)
  - sys_config.config_key
  - 等
- **复合索引**：15+ 个（高频查询优化）
  - notice(community_id, status, publish_time)
  - work_order(community_id, status, created_at)
  - notification(user_id, seq)
  - resource_reservation(resource_id, reserve_date, status)
  - lease_record(status, end_date)
  - 等

### 3.4 分区表方案（可选优化）

| 表 | 分区策略 | 保留期 | 实施状态 |
|---|---------|--------|---------|
| sys_operation_log | 按月分区（RANGE PARTITION） | ≥4年 | 📋 已设计，40 阶段根据数据量评估 |
| sys_task_log | 按周分区（RANGE PARTITION） | 1年 | 📋 已设计，40 阶段根据数据量评估 |

**说明**：毕设规模下数据量有限，分区表可作为性能优化预留方案，不强制实施。

---

## 四、遗留问题与后续优化

### 4.1 已知遗留问题（0 个）

**无**

### 4.2 后续优化方向（3 项，非阻塞）

| 优化项 | 优先级 | 说明 | 实施时机 |
|-------|--------|------|---------|
| 分区表实施 | 🟡 中 | sys_operation_log / sys_task_log 按时间分区 | 40 阶段根据压测数据量评估 |
| ER 图可视化 | 🟢 低 | 使用 dbdiagram.io 等工具绘制 ER 图 | 论文撰写阶段（可选） |
| 字段长度微调 | 🟢 低 | 根据实际业务数据调整 VARCHAR 长度 | 40 阶段开发中根据需要调整 |

---

## 五、变更管理规则

**冻结后变更规则**：

1. **禁止变更**（需重新评审）：
   - 删除表/字段
   - 修改主键/外键关系
   - 修改唯一约束
   - 修改状态机枚举值

2. **允许变更**（记录决策日志）：
   - 新增表/字段（向后兼容）
   - 新增索引（性能优化）
   - 调整字段长度（放宽限制）
   - 新增枚举值（状态机扩展）

3. **轻量变更**（无需审批）：
   - 字段注释修正
   - 索引名称调整
   - 默认值优化

**变更流程**：
- 提出变更 → 评估影响范围 → 决策日志记录 → Flyway 新增迁移脚本 → 文档同步更新

---

## 六、下游依赖通知

**数据库设计冻结后，以下任务可并行启动**：

1. ✅ **接口设计**（30 包下一任务）
   - 依赖：表结构、字段定义、状态机枚举
   - 负责人：AI
   - 预计耗时：8-12 小时

2. ✅ **Flyway 迁移脚本编写**（与接口设计并行）
   - 依赖：数据库设计.md 全文
   - 负责人：AI
   - 产物：`src/main/resources/db/migration/V1__create_tables.sql`

3. ⏳ **UI 设计**（接口设计后启动）
   - 依赖：接口设计冻结
   - 负责人：AI
   - 预计耗时：6-10 小时

4. ⏳ **后端实体类生成**（40 阶段）
   - 依赖：Flyway 脚本 + 接口设计
   - 工具：MyBatis-Plus CodeGenerator

---

## 七、验收确认

| 验收项 | 状态 | 备注 |
|-------|------|------|
| 所有需求条目（R1~R57）覆盖 | ✅ | 100% 覆盖 |
| 架构设计输入清单（9 项）完成 | ✅ | 9/9 完成 |
| 6 套状态机完整落表 | ✅ | 状态字段 + 枚举值 |
| 命名规范统一 | ✅ | 表名/字段名/索引名 100% 合规 |
| 索引策略完善 | ✅ | 主键/外键/唯一/复合索引全覆盖 |
| 安全性措施到位 | ✅ | 密码哈希/日志留痕/敏感数据保护 |
| 自审核通过 | ✅ | 59.5/60（优秀） |
| 用户审查确认 | ✅ | 2026-09-06 |

---

## 八、附录：冻结版本快照

- **文档路径**：`docs/30_系统设计/数据库设计.md`
- **Git Commit**：（待 40 阶段提交代码时回填）
- **冻结时间**：2026-09-06
- **文档行数**：6000+ 行
- **表数量**：40 张
- **索引数量**：100+ 个
- **自审核得分**：59.5/60（优秀）

---

**冻结确认**：✅ 数据库设计 v1.0 正式冻结，可作为后续开发的权威依据。

**签署**：
- AI（Claude Code） — 2026-09-06
- 用户确认 — 2026-09-06
