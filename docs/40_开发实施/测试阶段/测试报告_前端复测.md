# 测试报告 · 前端复测（后端遗留问题修复后）

- **日期**：2026-09-08
- **测试人**：前端会话（AI）
- **范围**：后端会话修复 BE-ISSUE-1~4 后的前端复测（复测计划见主仓库
  `docs/40_开发实施/测试阶段/给前端会话的状态通知.txt` §2）
- **环境**：前端 dev server（localhost:5173，Vite proxy → 8080）；
  后端为主仓库测试库实例（通知接口修复版，JWT 签名已由 HS384 换为 HS256）
- **产物**：`frontend/frontend-selftest-results.json`（脚本 `frontend/frontend_selftest.py`）

---

## 1. 后端修复确认（复测前置探测）

| 编号 | 问题 | 修复验证 | 结果 |
|------|------|---------|------|
| BE-ISSUE-1 | 通知接口 500 | superadmin token `GET /notifications` | 200 ✅ |
| BE-ISSUE-2 | 反馈附件接口 404 | `GET /feedbacks/2/attachments` | 200 ✅ |
| BE-ISSUE-3 | 游客公告回执 403/401 | 后端澄清为**设计如此**（接口设计 §9.5.1.8 权限 RESIDENT，notice_view_record 以 resident_id 为主体，游客无回执主体） | 按澄清结论前端适配 ✅ |
| BE-ISSUE-4 | 测试账号缺失 | admin1 / staff1 / resident1 登录 | 全部 200 ✅ |

## 2. 前端适配（BE-ISSUE-3 澄清结论）

`frontend/src/views/guest/NoticeDetailView.vue`：移除游客端 `recordNoticeView`
调用（游客公告详情走公开 GET，不依赖回执接口）；居民端
`resident/NoticeDetailView.vue` 保留回执调用（RESIDENT 权限，符合设计）。

## 3. 自测脚本升级

`frontend/frontend_selftest.py` 本次升级：
- 多角色权限测试由「superadmin 真实 token + 客户端改角色」升级为
  **admin1 / staff1 / resident1 真实登录**（后端 BE-ISSUE-4 已种入账号），
  守卫与 v-permission 走完全真实的代码路径；
- 64 页路由扫描按端分组、以对应角色真实会话访问（此前全部用 superadmin
  会话扫 64 页，管理端会话访问居民/服务人员页面属越权场景）；
- 新增 BE-ISSUE-1/2/3 复测专项断言（三端通知页接口状态、附件接口 200、
  游客详情无回执调用）与全量 4xx/5xx API 响应记录（api_errors）。

## 4. 复测结果

**自动化 32/32 通过；64 页路由扫描 0 运行时错误；vue-tsc 0 错误；
vite build 成功。**

复测专项结论：

| 复测项 | 结果 |
|--------|------|
| 三端通知页加载与标记已读（BE-ISSUE-1） | 管理端/居民端/服务人员端通知接口全 200；居民端「全部已读」PATCH read-all 200，未读徽标正常 |
| 反馈详情页附件区（BE-ISSUE-2） | `/feedbacks/2/attachments` 200（空数组正常渲染），无运行时错误 |
| 游客公告详情回执（BE-ISSUE-3） | 游客详情页正常渲染且**零回执接口调用**（网络层验证） |
| 多角色真实登录与权限（BE-ISSUE-4） | resident1/staff1/admin1 登录、角色首页重定向、跨端 403 拦截全部通过；admin1 隐藏超管专属按钮（新建社区）、保留普通按钮；superadmin 全按钮可见 |

## 5. 残留 API 4xx 归类（12 条，全部非前端缺陷）

| 现象 | 归类 | 判定 |
|------|------|------|
| `GET /notices/1` 404（游客/居民） | 公告 1 未发布/已过期，后端按「普通用户仅可见已发布未过期」过滤；superadmin 访问 200 | 预期行为 |
| `work-orders/1`、`feedbacks/1` 及其子资源 403（resident1/staff1 会话） | 固定 ID 扫描跨用户数据，数据级权限正确拦截；superadmin 访问全 200 | 预期行为 |
| `GET /housings/1/available-slots` 404（superadmin 亦 404） | **后端未实现接口设计 9.12.2.8**（仅有 9.12.3.4 原始时段配置接口 `/timeslots`，两者语义不同：available-slots 返回含 currentBookings/maxBookings 的可约状态） | **新登记 BE-ISSUE-5，转后端会话** |

BE-ISSUE-5 影响：居民端看房预约页（`resident/HousingDetailView.vue`）时段
选择为空态（已优雅降级，不阻塞详情展示）。前端保持与接口文档一致，
待后端补齐后直接生效。

## 6. 结论

自测 + 复测范围内全部通过。BE-ISSUE-1/2/4 验证修复有效，BE-ISSUE-3 按
后端澄清完成前端适配；新发现 BE-ISSUE-5（看房可约时段接口缺失）转后端。
前端侧无遗留缺陷，frontend 分支具备合并到 main 的条件。
