# 缺陷跟踪与回归（步骤 8）

> 编写时点：步骤 7 发现缺陷起持续维护至全部关闭。
> 缺陷编号 `DEF-NNN` 全生命周期不改号；40 阶段 BE-ISSUE-N 仅作为
> TR-SP-06 回归基线引用，新缺陷一律用 DEF 编号。

## 1. 缺陷生命周期定义（新建 → 打开 → 修复 → 验证 → 关闭 / 拒绝 / 延期，
各状态流转责任方与入口）

| 状态 | 含义 | 责任方 |
|------|------|--------|
| 新建 | 执行会话发现并登记（现象/步骤/期望/实际 + 关联 TR/TC） | 主测试会话 |
| 打开 | 经复核确认为系统缺陷，待修复（裁决严重度） | 主测试会话 |
| 修复 | 修复会话提交修复（登记 commit） | 后端/前端修复会话 |
| 验证 | 回归用例重测通过 | 主测试会话 |
| 关闭 | 验证通过且无新问题引入 | 主测试会话 |
| 拒绝 | 经裁决非缺陷（口径符合/设计如此/环境问题），写明理由 | 主测试会话（严重裁决需用户确认） |
| 延期 | 严重度不足以阻塞出口准则，经评估接受遗留（写入报告） | 用户确认 |

## 2. 缺陷清单

| 编号 | 发现轮次 | 关联 TR/TC | 描述（现象/步骤/期望/实际） | 严重度（致命/严重/一般/轻微） | 状态 | 修复提交 | 回归结果 |
|------|---------|-----------|---------------------------|------------------------------|------|---------|---------|
| DEF-001 | 冒烟（步骤 6） | TR-C2-04 / TR-C4-01（TC 待 A 轨执行时对应） | 无居住关系的注册账号提交工单成功：新注册账号（未提交/未通过入住申请）携带任意社区的启用类别 ID 调 `POST /api/v1/work-orders` 返回 HTTP 200 并落库。期望：R9「申请通过后获得居民端功能入口」+ R12 居住私有数据口径，无关系账号应被拒。实际：通过。 | 一般 | 关闭（回归通过） | c946b69 | 2026-09-11 第二轮：无关系账号提单 code=403 零落库、在住居民放行（r2_def_regression.mjs） |
| DEF-002 | 冒烟（步骤 6） | TR-C7-02（R33，SP-01 关联） | 同天不同时段预约被误拒：同一居民对同一资源在同一天已有任一预约后，再预约该资源**不同时段**返回 code=5002「同一天已预约该资源，不可重复预约」。期望：接口设计 §9.7.1.1 冲突口径为「同一居民**同一时段**不可重复预约」，时段不重叠应放行（R33 同资源同时段重叠拦截针对并发抢占，非同天限次）。实际：按天拦截，更严。 | 一般 | 关闭（回归通过） | c946b69 | 2026-09-11 第二轮：同天不同时段放行 + 同时段 5002 拦（SP-01/冒烟/TC-C7-004 三处口径一致） |
| DEF-003 | 第一轮 A 轨（TC-SP-009/010） | TR-SP-02（R24/R25/R45/N5） | 公告跨社区读路径未做数据级过滤：①绑定社区 2 的管理员 `GET /api/v1/notices/1`（社区 1 公告）返回 200 全文（写路径 PUT/DELETE/publish 有 checkManageAccess 403，仅 GET 详情绕过）；②阳光花园居民公告列表可见清源里定向公告 2（列表接口对非管理端未按 notice_target 过滤展示范围，仅过滤了状态/有效期）。期望：管理员 GET 详情越绑定社区 404；居民列表仅含其社区定向公告（R25「范围外用户不可见」）。实际：放行/可见。 | 严重（R25 范围外不可见判据不达标） | 关闭（回归通过） | 见 §3.1（提交号由主会话回填） | 2026-09-11 第二轮：跨社区详情 404、范围外居民列表不可见、本社区居民可见 |
| DEF-004 | 第一轮 A 轨（TC-SP-005·5a） | TR-SP-02（R24 v1.2/N5） | 公告创建 communityId 单目标写法绕过越范围校验：`POST /api/v1/notices` 携带 `communityId: <非绑定社区>` 返回 200 并落库越权定向（实测 admin1 绑定社区 1 创建定向社区 2 的公告 #39 成功，notice_target 落 COMMUNITY/2）；`targets` 列表写法有 checkTargetAccess 校验（404 拒绝正常），单目标写法漏校验（NoticeService.resolveTargets 仅 targets 分支 peek 校验）。期望：R24「社区管理员选择绑定范围外的目标被拒绝并记录」。实际：放行落库（DRAFT 态，publish 时被 checkManageAccess 拦截，故未实际触达，但越权写入成立）。 | 严重（写路径越权） | 关闭（回归通过） | 见 §3.1（提交号由主会话回填） | 2026-09-11 第二轮：communityId 单目标越权 404 拒且零落库 |
| DEF-005 | 第一轮 A 轨（TC-SP-001，R33） | TR-SP-01（R33） | 资源预约并发竞态致容量突破：容量 1 的资源，10 路并发同时提交同资源同日期同起止时段，checkCapacity（select-then-insert，无锁无唯一约束）全部或部分通过——实测多轮 10/10 成功落库 10 条同槽 PENDING 预约（轮次间亦有波动：某轮 6 条落库+4 路 5401 拒绝，说明窗口存在但校验形同虚设）。期望：R33 同资源同时段重叠预约 100% 拦截，每轮仅 1 路成功。实际：10 条同槽并存（超容量 10 倍）。 | 致命（P0 出口准则 5：R33 拦截率 100% 未达标不得出口） | 关闭（回归通过） | 见 §3.1（提交号由主会话回填） | 2026-09-11 第二轮 SP-001 三轮 10 路并发全部 1/10 成功+落库 1（Redisson 锁+V10 约束） |
| DEF-006 | 第一轮 A 轨（TC-SP-002） | TR-SP-01（R33） | 资源预约半重叠时段不拦截：已存在 10:00-11:00 预约后，10:30-11:00、10:00-10:30、10:15-10:45（包含）三类重叠时段全部放行落库（checkCapacity 仅精确匹配 start_time+end_time 相等，不做区间重叠判定）。期望：R33「重叠预约 100% 拦截」。实际：仅拦截起止完全相同的预约，重叠判定缺失。 | 致命（同 DEF-005 判据） | 关闭（回归通过） | 见 §3.1（提交号由主会话回填） | 2026-09-11 第二轮 SP-002 六边界对全符（半重叠/包含/相同拦、首尾相接放行） |
| DEF-007 | 第一轮 A 轨（TC-SP-003，R55） | TR-SP-01（R55） | 看房预约并发竞态致同房源同时段多重预约：10 路并发同房源同日期同起止时段，全部 200 成功落库 10 条 TO_CONFIRM 预约（冲突检测 select-then-insert 无锁无唯一约束）。期望：R55 冲突校验复用 R33 机制，同房源同时段仅 1 条有效预约。实际：10 条并存。 | 致命（P0 出口准则 5：R55 拦截率 100% 未达标不得出口） | 关闭（回归通过） | 见 §3.1（提交号由主会话回填） | 2026-09-11 第二轮 SP-003 三轮 10 路并发全部 1/10 成功+落库 1 |
| DEF-008 | 第一轮 A 轨（TC-SP-004） | TR-SP-01（R55） | 看房预约半重叠时段不拦截：已存在 15:00-16:00 预约后，15:30-16:00（后重叠）、15:15-15:45（包含）放行落库；15:00-15:30（前重叠）因仅匹配 start_time 相同被拦，判定口径不一致且不完整（ViewingAppointmentService 冲突检测仅 eq start_time）。期望：R55 与 C7 同口径，重叠 100% 拦截。实际：仅拦截起始时间相同的预约。 | 致命（同 DEF-007 判据） | 关闭（回归通过） | 见 §3.1（提交号由主会话回填） | 2026-09-11 第二轮 SP-004 六边界对全符 |
| DEF-009 | 第一轮 A 轨（TC-C10-004/010） | TR-C10-01（R43）/TR-C10-03 | 用户级令牌吊销因秒级时间戳碰撞不生效：冻结账号、绑定/解绑社区变更后，接口设计口径为「吊销该用户全部存量令牌」（9.10.1.x 业务规则），实现为「iat < revokedAt 才失效」的吊销时间戳语义，但 revokedAt 取 `System.currentTimeMillis()/1000`、JWT iat 同为秒级——**冻结与登录发生在同一秒时 iat==revokedAt，判定为未吊销**，实测冻结 admin 账号后其令牌仍可访问全部接口（200）；绑变/解绑同口径失效。期望：冻结/权限变更后存量令牌立即 401。实际：同秒签发的令牌在冻结后继续有效 2h（直至自然过期）。 | 严重（冻结即时吊销判据不达标；跨秒签发的旧令牌可正常吊销，故非全量失效） | 关闭（回归通过） | 见 §3.1（提交号由主会话回填） | 2026-09-11 第二轮：冻结后存量令牌立即 401（含同秒签发；同秒重登被连带拦为已声明口径，脚本隔秒规避） |
| DEF-010 | 第一轮 A 轨（TC-C10-009） | TR-C10-03（R45/N5） | 居民账号数据未按绑定社区过滤：`GET /api/v1/residents`（列表）与 `GET /api/v1/residents/{id}`（详情）对 ADMIN 无社区过滤——resident 表在 DataScopeInterceptor 的 SKIP_TABLES（无 community_id 列），接口设计 9.2.1.7/9.2.1.8 明文「ADMIN 限绑定社区内居民（通过 residence_relation 关联）」但业务层未实现该校验。实测 admin1（绑定阳光花园）直查清源里居民 id=2 返回 200 全量资料（含手机号/证件号字段口径）；列表翻页同样可见全系统居民。期望：越绑定社区居民 403/404、列表自动注入社区过滤。实际：全量可见。 | 严重（两级数据授权 R45 判据不达标，跨社区居民敏感信息泄露） | 关闭（回归通过） | 见 §3.1（提交号由主会话回填） | 2026-09-11 第二轮：越社区详情 404、列表无跨社区泄露、追加绑定双社区均可见（行为正确） |
| DEF-011 | 第一轮 A 轨（TC-C4-011/012/014/016） | TR-C4-03/TR-C4-06（R19/R20/R22/R23） | 工单终态/同态重复动作被幂等放行并追加脏时间线：`transition()` 首行 `current.equals(target)` 时静默 return，重复同动作返回 200「成功」且 appendProcess 无条件执行。实测四组：①REJECTED→reject、CANCELLED→cancel 200（终态空集应 5302）；②ACCEPTED 态重复 accept 200（R20「接单后其他人不可再接」判据失守——同态重复也放行）；③COMPLETED 态重复 confirm 200（R22「已完成再确认被拒」失守）；④CLOSED→close 200。时间线在非法重复动作后 +1 条脏记录（违反架构 §6.1「非法流转不产生处理记录」）。期望：同态/终态重复动作一律 5302，时间线零新增。实际：200 + 脏记录。 | 一般→严重（升级理由：影响 R20/R22 两项需求验收判据，非仅留痕污染） | 关闭（回归通过） | 见 §3.1（提交号由主会话回填） | 2026-09-11 第二轮：同态 accept/终态 close 重复均 5302 且时间线零新增 |
| DEF-012 | 第一轮 A 轨（TC-C4-011） | TR-C4-03（R19）/TR-C4-06 | 工单取消与改派的合法流转集超宽：①cancel：实现 ALLOWED_TRANSITIONS 含 ASSIGNED→CANCELLED、ACCEPTED→CANCELLED，但架构 §6.1 状态机权威口径「居民取消」仅可自 PENDING/TO_ASSIGN 发起（R19 验收标准同口径：已取消仅可自待受理/待派单流转）——实测 ASSIGNED/ACCEPTED 态均被居民成功取消；②assign 改派：assign 允许 ASSIGNED 态改派（「支持改派」注释），且 ACCEPTED/COMPLETED/CLOSED→assign 实测 200 放行（回退已接单/已完成/已关闭工单到 ASSIGNED）。期望：取消限 PENDING/TO_ASSIGN；改派应限 ASSIGNED 态（已接单及之后不可改派）。实际：五类超权流转放行。 | 严重（状态机完整性 P0 判据；TO_CONFIRM/COMPLETED/IN_PROGRESS→cancel 同超集，本次矩阵 IN_PROGRESS/TO_CONFIRM/COMPLETED→cancel 亦 200 放行） | 关闭（回归通过） | 见 §3.1（提交号由主会话回填） | 2026-09-11 第二轮：ACCEPTED 取消/改派 5302、PENDING 取消放行；状态机矩阵 84/84 |
| DEF-013 | 第一轮 A 轨（TC-C3-003） | TR-C3-03（R16） | 租约跳级归档放行：ACTIVE→ARCHIVED 直接流转成功（跳过 MOVED_OUT）。实现 ALLOWED_TRANSITIONS 含 `ACTIVE→ARCHIVED`，但架构 §6.2 状态机为线性「已生效→已搬出→已归档」（R16「已生效租住经搬出登记流转已搬出→已归档」）。实测 PENDING→ACTIVE 合法流转后 ACTIVE→ARCHIVED 返回 200，状态变 ARCHIVED。期望：ACTIVE 仅可流转 MOVED_OUT。实际：跳级放行。 | 一般（状态机完整性；数据语义受损——未搬出即归档） | 关闭（回归通过） | c946b69 | 2026-09-11 第二轮：ACTIVE→ARCHIVED 5004 拒、线性两步达 ARCHIVED |
| DEF-014 | 第一轮 A 轨（TC-C1-001/023、TC-C10-007 等全域） | TR-C10-05（R47/N6）——影响全部功能域用例的「操作日志断言」项 | 操作日志 AOP 切面未实现，关键变更大面积零留痕：`log/aspect/` 目录仅有 .gitkeep 占位（AGENTS.md 后端包结构要求「log/ AOP 操作留痕切面」），全代码库仅 2 处手写落日志（社区级联删除、管理员代建居民）；实测 sys_operation_log 全库 0 条——社区创建/更新、楼栋/单元/房屋/资源/时段 CRUD、房屋状态变更、工单全链流转、预约审核、公告发布、入住审核、租约流转等关键操作均无日志。期望：R47「全部关键变更（新增/修改/删除/状态流转/审核/处置）可检索到对应日志」。实际：仅 2 个特例有日志，其余全缺。 | 严重（R47 判据大面积不达标；波及 C1~C12 各用例日志断言项与 R58 清理对象） | 关闭（回归通过） | 见 §3.1（提交号由主会话回填） | 2026-09-11 第二轮：多模块关键变更近 2 分钟 56 条落库（UPDATE/STATUS/REVIEW/DELETE）+ 检索端点 200；SP-02 越权日志断言新增 27 条 |
| DEF-015 | 第一轮 A 轨（TC-C1-022） | TR-C1-09（R6） | 资源时段删除无未完成预约引用保护：`DELETE /api/v1/timeslots/{id}` 在该时段存在未完成预约（待审核/已预约）时直接删除成功（ResourceTimeslotService.delete 无引用检查；对照：资源本体删除有 5105 保护）。期望：R6「已被业务数据引用的资源禁止物理删除」+ 用例预期 5106「时段有预约删除被拒」。实际：200 放行，预约悬空（模板删除后已建预约指向不存在的时段）。 | 一般（删除保护缺口；资源本体有保护，时段为旁路） | 关闭（回归通过） | c946b69 | 2026-09-11 第二轮：占用中删时段 5106 拒、释放后放行 |
| DEF-018 | 第一轮 A 轨（TC-C4-002） | TR-C4-01（R17） | 停用类别仍出现在提交选项数据源：类别树接口 `GET /api/v1/communities/{id}/service-categories` 未按 is_active 过滤，停用类别仍返回（前端提交页亦未过滤，靠后端提交时校验兜底拒绝）。期望：R17「停用类别不再出现在提交选项」。实际：树含停用项（提交兜底校验有效——用停用类别提交被 5003 类错误拒绝，但选项列表不净化）。 | 轻微（后端有兜底，用户可见选项列表含停用项的体验问题；预判清单项确认） | 延期（用户裁决 2026-09-11：随 C 轨 UI 批次处理展示净化，后端提交校验兜底有效） | — | — |
| DEF-019 | 第一轮 A 轨（TC-C4-015） | TR-C4-06（R22） | 工单「不满意退回处理中」未实现：R22「居民对结果不满意可退回处理中（注明原因）」，WorkOrderService 合法流转表 TO_CONFIRM 仅可达 COMPLETED，无 TO_CONFIRM→IN_PROGRESS 退回端点。期望：居民可自待确认发起退回且须填原因。实际：无入口（预判清单项确认）。 | 一般（R22 判据缺失：不满意场景无闭环，只能确认完成） | 关闭（回归通过） | c946b69 | 2026-09-11 第二轮：TO_CONFIRM 退回 IN_PROGRESS + 通知服务人员 + 非本人 403（PATCH /work-orders/{id}/return） |
| DEF-020 | 第一轮 A 轨（TC-C4-008） | TR-C4-09 联动（R48） | 工单驳回不发通知：reject 方法无 notificationService 调用，实测驳回后居民通知数为 0（其他流转节点——派单/接单/完成/确认——通知正常）。期望：R48「工单流转」事件全覆盖通知。实际：REJECTED 流转缺通知。 | 一般（R48 覆盖缺口单项） | 关闭（回归通过） | c946b69 | 2026-09-11 第二轮：驳回后居民通知 +1（「工单已驳回」） |
| DEF-021 | 第一轮 A 轨（TC-C5-007） | TR-C5-04（R26） | 公告时间字段时区偏移 8 小时：创建公告传入 ISO 无时区后缀的 `publishTime/endTime`（如本地 01:32），落库为偏移 -8h 的值（17:33）——LocalDateTime 反序列化按 UTC 解释、存储按 Asia/Shanghai 连接时区，两端口径不一致。影响：所有指定 endTime 的公告实际有效期偏移 8 小时（+2h 短效公告创建即已过期或延后 8h；publishTime 同偏移导致定时发布时刻错误）；缺省 30 天口径由服务端自算不受影响。期望：传入与落库一致。实际：恒差 8 小时。 | 严重（R26 有效期判据失真；影响定时发布与过期下线判定） | **拒绝（2026-09-11 主会话复核采纳：非系统缺陷，测试脚本时区口径问题，R26 实际达标）** | —（无代码改动，脚本修正归第二轮 TC-C5-007 重跑） | — |

> **DEF-021 复核记录（2026-09-11，后端修复会话）**：经独立实例实测（18080 端口 + 独立库，
> 未触碰 8080 SP-03 现场），**判定为非系统缺陷，根因在测试脚本的时区口径**：
> ① c05 脚本 `now()/iso()` 用 `toISOString()` 生成 **UTC 时刻**再截断后缀——本地 12:35
> 生成 `04:35`（UTC），后端 `LocalDateTime` 按无时区字面量原样接收存储，落库 `04:35`
> 与输入一致，「偏移 8 小时」实为脚本将 UTC 字面量传给了语义为本地时间的接口；
> ② 本地墙钟时间输入（如 `12:31:00`）实测落库 `12:31:00`，传入与落库完全一致；
> ③ 带 `Z` 后缀输入（`12:40:00Z`）实测同样一致存储（Jackson lenient 解析）；
> ④ notice 时间列为 DATETIME，连接串 `serverTimezone=Asia/Shanghai`，无隐式时区换算；
> Jackson 对 `LocalDateTime` 的解析不依赖 ObjectMapper 时区（javac 级验证）。
> **结论**：接口契约「ISO 无时区 = 本地时间」自洽，R26 判据实际达标；处置建议为
> **脚本修正**（`now()/iso()` 改本地时间生成）+ 本 DEF 关闭为拒绝（严重度裁决
> 涉及状态翻转，按生命周期定义需主测试会话/用户确认后正式落状态）。
| DEF-022 | 第一轮 A 轨（TC-C8-010） | TR-C8-03（R39） | 评价双维度聚合接口缺失：R39「按服务人员、服务类别聚合满意度」，实现仅有总量统计（`GET /api/v1/statistics/evaluations`：总数/平均分/满意率/分档），无按服务人员（经 work_order_assignment 关联）与按服务类别的分组聚合端点（by-staff/by-category 404）。期望：两个维度均可输出聚合结果。实际：仅总量单维度。 | 一般（R39 双维度判据缺失；看板 ratingDistribution 有分档但无人员/类别维度） | 关闭（回归通过） | c946b69 | 2026-09-11 第二轮：GET /statistics/evaluations/aggregation 返回 byStaff/byCategory 双维度 |
| DEF-023 | 第一轮 A 轨（TC-C12-006） | TR-C12-04（R55）——预判清单项确认 | 看房预约状态流转全链无通知：confirm/complete/violate/cancel 方法均无 notificationService 调用（ViewingAppointmentService 仅更新状态），预约人确认后通知数为 0。期望：R55「确认/取消均通知对方」+ R48 事件覆盖（审核结果-看房）。实际：看房预约四类流转零通知（对照：资源预约 confirm 有通知，实现不对称）。 | 一般（R48/R55 通知覆盖缺口；预判清单「看房确认通知未挂接」确认） | 关闭（回归通过） | c946b69 | 2026-09-11 第二轮：confirm/complete 各 +1 通知（「看房预约已确认/已完成」） |
| DEF-024 | 第一轮 A 轨（TC-SP-024） | TR-SP-05（上传边界） | 文档超限错误契约破损：`spring.servlet.multipart.max-file-size=10MB` 与业务文档上限相同，>10MB 文件在 multipart 层被 MaxUploadSizeExceededException 拦截，全局异常处理器无该异常映射 → 500「系统内部错误」；业务层「文档不能超过 10MB」400 提示对 >10MB 文件永不可达。功能上文件被拒绝且不落盘（安全面无缺口），但用户收到 500 而非明确提示。期望：400 + 「文档不能超过 10MB」。实际：500/系统内部错误。修复建议：GlobalExceptionHandler 增补 MaxUploadSizeExceededException 映射（400 + 上限提示）。 | 一般（功能拒绝生效、无落盘残留；错误契约与提示缺失） | 关闭（回归通过） | c946b69 | 2026-09-11 第二轮：11MB/13MB 均 400 + 「单个文件不能超过 10MB」（MaxUploadSizeExceededException 映射 + multipart buffer 12MB） |
| DEF-016 | 第一轮 A 轨（TC-C3-010） | TR-C3-03（R15） | 到期提醒无管理员手动重发入口：R15「可由管理员手动重发」，LeaseReminder 实体注释含「重发=删除旧记录后插入新记录」语义，但 lease 模块无重发端点（Controller 仅有 CRUD/status/renew/expiring）。期望：管理端可对指定租约重发提醒。实际：无触发入口。 | 一般（功能缺失；自动提醒主链路不受影响，重发为辅助能力） | 弃置（用户裁决 2026-09-11：自动提醒主链路正常，重发属辅助能力不实施，遗留评估入报告） | — | — |
| DEF-017 | 第一轮 A 轨（TC-C3-012） | TR-C3-04（R16） | 归档租约可修改业务字段：ARCHIVED 终态租约 `PUT /api/v1/leases/{id}` 修改租金/租期返回 200 成功（LeaseService.update 无状态校验，仅流转端点有状态机拦截）。期望：R16「归档后不可修改」。实际：终态数据可被改写（流转与续租已被正确拒绝，PUT 旁路）。 | 一般（数据完整性：归档语义失效） | 关闭（回归通过） | c946b69 | 2026-09-11 第二轮：ARCHIVED 修改 5004「已归档的租约不可修改」 |
| DEF-025 | 前端美化移交（2026-09-14） | R20（TR-C4 派单链路） | ADMIN 调 GET /sys-users 403 → 社区管理员无法派单：`GET /api/v1/sys-users`（列表）为 SUPER_ADMIN 专属，社区管理员打开派单对话框 403、人员下拉为空，R20 派单主链路断裂。期望：社区管理员可获取可派单服务人员选项。实际：403。 | 严重（R20 主链路断裂） | 关闭（回归通过） | 0a1935b | 2026-09-14 第三批专项：ADMIN 200 仅启用 STAFF 最小暴露面+熟手置前+角色 403（r3_batch3 B3-01） |
| DEF-026 | 前端美化移交（2026-09-14） | TR-C4（工单创建） | 工单号撞唯一键 409 无重试：generateOrderNo 为「WO+日期+4 位随机」，注释称「重复由唯一键兜底重试」但代码无重试，撞 uk_order_no 直接 409。期望：撞号自动重试。实际：直接 409。 | 一般 | 关闭（回归通过） | 0a1935b | 2026-09-14 第三批专项：20 单零 409+工单号 keyword 精确命中（r3_batch3 B3-02） |
| DEF-027 | 前端美化移交（2026-09-14） | TR-C6（R30/R48 联动） | 反馈办结不推 WS + 列表缺 keyword：close() 仅 sendMessage 落档不 convertAndSend，对端最长滞后一个轮询周期（STOMP 在线 30s）才感知办结；GET /feedbacks 无 keyword 参数（前端搜索仅覆盖已加载页）。期望：办结实时触达 + 服务端模糊过滤。实际：滞后/前端过滤。 | 轻微/一般 | 关闭（回归通过） | 0a1935b | 2026-09-14 第三批专项：keyword 库内一致+办结 FEEDBACK_STATUS 载荷在线送达（r3_batch3 B3-03/04） |
| DEF-028 | 前端美化移交（2026-09-14） | TR-C8（R40 跟进工作台） | unsatisfied 无 hasFollowup 参数：不满意评价列表不支持按是否已跟进过滤，前端工作台 N+1 请求逐条核实。期望：服务端过滤。实际：N+1 兜底。 | 一般 | 关闭（回归通过） | 0a1935b | 2026-09-14 第三批专项：hasFollowup true/false/null 三分支与库一致（r3_batch3 B3-05） |
| DEF-029 | 前端美化移交（2026-09-14） | TR-C4/TR-C9（服务人员端） | 统计无 STAFF 按日序列 + 工单列表无时间范围/多状态分页：StatisticsController 全端点 ADMIN+，服务人员端趋势卡 MOCK；GET /work-orders 仅单状态 status、无 startTime/endTime，「今日完成」实为累计完成。期望：STAFF 可取本人按日趋势；列表支持多状态与时间范围。实际：MOCK/口径不符。 | 一般 | 关闭（回归通过） | 0a1935b | 2026-09-14 第三批专项：STAFF 趋势限本人+statuses/时间范围+STAFF 数据域零泄漏（r3_batch3 B3-06/07） |
| DEF-030 | 前端美化移交（2026-09-14） | TR-C10（契约） | 创建用户手机号选填 vs @NotBlank 契约漂移：前端表单「手机号选填」无必填校验，后端 CreateSysUserDTO @NotBlank+唯一校验。裁决：后端 @NotBlank 维持（手机号为账号体系联系必填字段），前端校验补齐归 C 轨批次，后端侧仅决策日志留痕。 | 轻微 | 关闭（回归通过·留痕口径） | 见 §3.8（无代码改动） | 2026-09-14 第三批专项：缺/空 phone 均 400 零落库（r3_batch3 B3-08） |
| DEF-031 | 前端美化移交（2026-09-14） | TR-C5（R25 置顶配套） | 公告 GET /notices 缺 priority 过滤参数：管理端公告优先级筛选无服务端支撑（前端已按「不做假控件」原则移除控件，参数就绪后回补）。期望：列表支持 priority/isPinned 过滤。实际：无参数。 | 一般 | 修复（待回归） | 0a1935b §3.8 | 2026-09-14 第三批专项：priority/isPinned/type 三过滤生效且锚点命中（r3_batch3 B3-09） |
| DEF-032 | 第三批专项（2026-09-14，r3_batch3 B3-12） | TR-C1-13（D-端点2 批量创建） | 批量创建「部分成功」语义对参数校验类行失败不可达：BatchCreateHousesDTO.houses 带 @Valid 元素级联（area @NotNull 等）、BatchCreateUnitsDTO.names 元素级 @NotBlank/@Size——任一行违反 Bean Validation 即 Controller 层整批 400，进不了服务层逐行 try-catch；BatchCreateResultVO 的 reason 字段仅对服务层异常可达（08 §3.8 单测为服务层 Mock，E2E 不可复现）。期望：坏行逐行反馈、好行照常创建。实际：整批 400（楼栋/单元/房屋三端点同）。 | 一般（功能可用——整批拒绝偏保守方向，无数据风险；语义与设计意图不符） | 修复（待回归） | e32a48b §3.9 | — |
| DEF-042 | 前端接线复核（2026-09-14，ebc80fc 登记） | TR-C11-04（R51） | channel-levels PUT 无等级键白名单：任意字符串键可写入配置（sourceType 误用键亦放行），读回后前端并集渲染出现幽灵等级行。期望：仅接受 7 个真实业务等级键，未知键 400。实际：无校验。 | 一般（配置漂移防线缺失；前端已按真实全集渲染） | 关闭（回归通过） | 本批 §3.11 | 2026-09-15 第五批：未知键 400 + 七键放行 + 读回一致（单测 2 + E2E 3 断言） |
| DEF-043 | 前端接线复核（2026-09-14，ebc80fc 登记） | TR-C5-01/C5-02（R24/R25） | 公告 update 端点不重写 targets：编辑时 notice_target 不变（旧口径「目标范围不可变更」），前端接线后表单回显保存不生效——定向编辑静默丢失。期望：编辑定向生效（先删后插）。实际：目标行不变。 | 一般（编辑功能失真；create 正常） | 关闭（回归通过） | 本批 §3.11 | 2026-09-15 第五批：多目标重写/回收无残余/广播清空/越社区拒（单测 2 + E2E 5 断言） |
| DEF-033 | 第三批专项（2026-09-14，r3_batch3 B3-11） | TR-C1-12（D-端点1 楼栋级联） | 楼栋级联删除遇历史居住关系 FK 阻断返回 500：cascadeDelete 仅自底向上物理删 house_status_history→house→unit→building，不清理 residence_relation（历史已搬出行）/residence_application/lease_record 等引用 house_id 的 FK（均 RESTRICT）——房屋存在历史居住关系时级联删除触发 FK 异常 → HTTP 500「系统内部错误」而非业务码拒绝或级联清理。实测：直建关系→迁出（留历史行）→cascade=true 删除 → 500，楼栋未删（数据未损坏，事务回滚）。 | 一般（错误契约破损；在住保护有效、无数据损坏） | 修复（待回归） | e32a48b §3.9 | — |
| DEF-034 | 第三批专项（2026-09-14，r3_batch3 B3-13） | TR-C10-05（R47 操作日志）/TR-C2-12 | CREATE 类操作日志全量写入失败（#result 未绑定）：OperationLogAspect 的 @AfterReturning 未绑定返回值（无 returning 属性），resolveSpel 中 #result 恒为 null——凡注解使用 `#result.id` 的 SpEL（targetId/communityId）求值抛 NPE，整条日志被 catch 丢弃（后端 WARN「操作日志写入失败」留痕）。影响 10 个服务的 CREATE 注解（社区/楼栋/单元/房屋/公共资源/资源时段/房源/租约/系统用户/居住关系管理员直建），创建类关键变更零留痕；UPDATE/STATUS/REVIEW/DELETE 类不用 #result 不受影响（第二轮抽样恰好未覆盖 CREATE 类，故未暴露）。期望：R47 全类型可检索。实际：CREATE 类 0 条。 | 严重（R47 判据缺口：创建类关键变更大面积零留痕） | 修复（待回归） | e32a48b §3.9 | — |
| DEF-035 | 第四轮全量回归（2026-09-14，r3_batch3 B3-05 复跑） | TR-C8-02（R38 跟进工作台） | unsatisfied 列表 hasFollowup 过滤为页内内存过滤，分页语义失真：服务层先取当前页（默认 20 条）后过滤——N7 批量数据 803 条不满意评价下 hasFollowup=false 返回 0 条（首页 20 条无待跟进项时全滤空），total 被置为过滤后条数而非全集计数。期望：服务端过滤=全集分页（false 应返回全部待跟进的分页）。实际：页内过滤+total 失真（08 §3.8「未做 SQL 级过滤，当前页量级可控」的量级判断在 N7 场景不成立，纯净库大数据下显形）。 | 一般（小数据量功能正确；工作台待跟进清单大数据量漏项） | 修复（待回归） | e32a48b §3.9 | — |
| DEF-036 | 第四轮全量回归（2026-09-14，SP-04 TC-SP-019 排查） | TR-C6-03/TR-SP-04（R30 会话实时推送） | 同一用户多 WebSocket 连接时任一连接断开即注销整个在线状态：WebSocketSessionService.deregister 按 userId 直接 DEL `ws:session:{userId}`（register 是 SET 单 key）——用户开两个连接（如双标签页）后关闭其一，另一在线连接被判离线；此后 sendMessage/close 的 isOnline 判定为 false，/topic/feedback/{id} 广播被静默跳过，会话对端最长滞后一个轮询周期（30s）。实测：A/B 双连接同用户 → B close → A 的 topic 推送 100% 丢失（通知通道 /user/queue 不受影响——SimpleBroker 用户队列独立于 isOnline 判定）。期望：按会话计数（或仅最后连接断开才注销）。实际：单 key 删除断言偏宽。 | 一般（单连接场景正确；多标签页/多端登录的会话实时性降级，轮询兜底不丢消息） | 修复（待回归） | e32a48b §3.9 | — |

## 3. 回归测试记录（每轮修复后回归范围与结果，含「未引入新问题」结论）

### 3.1 步骤 8 修复窗口 · 第一批（2026-09-11，后端修复会话）

**修复范围**：致命 DEF-005~008 + 严重 DEF-003/004/009/010/011/012/014（已修复）；
严重 DEF-021 复核改判（非系统缺陷，见 §2 复核记录）；一般/轻微 11 项逐项标注见 §3.2。

| DEF | 修复方式 | 单测回归 | 备注 |
|-----|---------|---------|------|
| DEF-005/006（预约并发+半重叠） | Redisson 锁（资源+日期粒度）内区间重叠判定（lt start<已有end AND gt end>已有start，首尾相接放行）+ V10 唯一约束 uk_reservation_slot 兜底 + insert DuplicateKeyException 转业务冲突；刻意去 @Transactional（锁内单条 INSERT 自动提交后释放锁，避免先释放后提交竞态） | ReservationConflictFixTest 9 用例（边界对 6+终态不占用+并发竞态 10 路仅 1 成功+约束兜底） | V10 在独立库 V1~V10 全新 Flyway 流程实测通过（含历史同槽脏数据清理逻辑验证：保留最早一条、其余置 CANCELLED） |
| DEF-007/008（看房并发+半重叠） | 同 C7 机制（锁 viewing:lock:housing:{id}:{date} + V10 uk_viewing_slot + 区间重叠判定与 C7 同口径） | ViewingAppointmentConflictFixTest 8 用例（含 08:00-10:00 原误拦口径纠正） | 与 C7 同构 |
| DEF-003（公告读路径） | getById：ADMIN 越绑定社区 404（目标含 BUILDING 经归属社区解析）；居民限本人社区定向+全系统广播（经 residence_relation）；page：居民注入定向 ID 集合 OR 广播（NOT EXISTS 子查询）过滤 | NoticeDataScopeFixTest 7 用例 | 游客/未登录维持公开口径（未被缺陷覆盖，不扩大改动面） |
| DEF-004（communityId 越权） | resolveTargets 单目标分支补 checkTargetAccess（与 targets 列表同口径） | NoticeDataScopeFixTest 含 | create/update 双入口生效 |
| DEF-009（令牌同秒吊销） | isRevoked 判定 `<` 改 `<=`（iat==revokedAt 按已吊销；「吊销全部存量令牌」口径） | TokenRevocationFixTest 5 用例（含 Redis 降级 DB 路径） | 改密后同秒重登的新令牌也被拦（重新登录需隔秒），符合「存量令牌全部失效」语义，可接受 |
| DEF-010（居民数据社区过滤） | getById：ADMIN 越绑定社区（经 residence_relation）404；page：ADMIN 注入绑定社区内居民 ID 过滤（IN），无绑定空页 | ResidentScopeFixTest 6 用例 | SUPER_ADMIN/STAFF 不动（STAFF 走派单关系约束） |
| DEF-011（同态/终态重复） | transition 删除 `current.equals(target)` 静默放行——同态/终态重复动作一律 5302 且不产生处理记录 | WorkOrderStateMachineFixTest 10 用例 | R19/R20/R22 判据恢复 |
| DEF-012（流转集收紧） | ALLOWED_TRANSITIONS 对齐架构 §6.1：cancel 仅 PENDING/TO_ASSIGN；assign 自检限 PENDING/TO_ASSIGN/ASSIGNED（改派）；ACCEPTED 及之后不可取消/改派 | 同上 | 流转表保留 TO_CONFIRM→IN_PROGRESS（R22 退回，端点为 DEF-019 待裁决） |
| DEF-014（操作日志 AOP） | 新增 @OperationLog 注解 + OperationLogAspect（SpEL 解析 targetId/communityId/content；操作人取安全上下文；content 包 JSON 适配列类型；写失败仅 warn 不阻断业务）；9 个模块 40 个写方法标注（C1 六类 CRUD/状态、C4 全流转、C5 全流转、C2 审核、C3 租约、C6 办结、C7 流转/处置、C10 用户/绑定、居民冻结、C12 房源/看房）；pom 增 spring-aop+aspectjweaver（Boot 4 BOM 无 starter-aop，按 BOM 版本 7.0.7/1.9.25.1 直接引入） | OperationLogAspectTest 4 用例 + 独立实例 E2E（社区更新/派单/接单/公告发布 4 模块落库验证 + 检索端点可用） | E2E 验证含 ip/operatorType 全字段；居民创建工单/预约等自助动作不记（管理侧变更语义，与 R47「关键数据变更」口径一致——遗留边界供主会话复核） |

**单测全量回归**：211/211 通过（原 162 + 新增 49 修复回归用例），零失败；修复过程
使用独立端口 18080 + 独立库 community_residence_fixit 自测，8080 SP-03 跨日观察实例全程未触碰。

### 3.2 一般/轻微缺陷修/不修标注（待主会话口径裁决后执行）

> 修复会话逐项评估结论如下；「建议修」项在主会话确认口径后按序实施，
> 「建议不修」项如主会话不同意可复议。

| DEF | 严重度 | 修复建议 | 理由 |
|-----|-------|---------|------|
| DEF-001（无关系账号提工单） | 一般 | **建议修**（小改） | R9/R12 数据口径判据：create 补「存在有效居住关系」校验即可（经 residence_relation），改动面小、判据直达；与 DEF-010 修复共享关系查询基建 |
| DEF-002（预约同天误拦） | 一般 | **建议修**（小改） | R33 口径明确「同一时段」而非「同天」：放宽为仅拦截同时段重叠即可——但需与 V10 唯一约束联动评估（约束按槽不按天，无冲突）；注意拦截口径放宽后同天不同时段多次预约将放行，需主会话确认无「同天限次」隐藏需求 |
| DEF-013（租约跳级归档） | 一般 | **建议修**（小改） | 状态机一行收紧（删 ACTIVE→ARCHIVED），与 DEF-012 同模式，风险极低 |
| DEF-015（时段删除无引用保护） | 一般 | **建议修**（小改） | delete 前查未完成预约计数，5106 错误码已预留，与资源本体 5105 对称 |
| DEF-017（归档租约可改） | 一般 | **建议修**（小改） | update 补终态校验一行（ARCHIVED 拒改），R16 判据直达 |
| DEF-019（R22 不满意退回无端点） | 一般 | **建议修**（中改） | R22 明确需求项且流转表已留边（TO_CONFIRM→IN_PROGRESS）：新增 Controller 端点 + Service 方法 + 退回原因必填校验 + 通知服务人员；建议修（需求判据缺失，非优化项） |
| DEF-020（工单驳回不发通知） | 一般 | **建议修**（小改） | reject 补 notificationService.create 一行，与其他流转节点对称，R48 判据直达 |
| DEF-022（评价双维度聚合缺失） | 一般 | **建议修**（中改） | R39 明确「按服务人员、服务类别聚合」：新增 by-staff/by-category 两个聚合端点（经 work_order_assignment 关联），看板口径需主会话确认输出结构 |
| DEF-023（看房预约零通知） | 一般 | **建议修**（小改） | confirm/complete/violate/cancel 补通知调用（与资源预约对称），R48/R55 判据直达 |
| DEF-016（R15 手动重发无端点） | 一般 | **建议不修（延期）** | 自动提醒主链路正常，手动重发为辅助能力；实现需定义「重发=删旧插新」的事务语义与权限口径，属新功能开发而非缺陷修复，建议 60 阶段后按需求优先级排期（遗留评估：不影响 R15 验收主判据「到期提醒」） |
| DEF-018（停用类别在提交选项） | 轻微 | **建议不修（延期）** | 后端提交校验兜底有效（停用类别提交被拒），影响仅为选项列表展示净化；修复涉及居民端选项接口的过滤口径（管理端配置预览需要看全量还是净化列表需产品口径裁决），建议 C 轨 UI 改版批次一并处理（遗留评估：无越权/脏数据风险） |

> 补充：DEF-024（文档超限 500）在 08 缺陷清单登记后由本轮修复会话评估为**建议修**（小改：
> GlobalExceptionHandler 增补 MaxUploadSizeExceededException 映射 400 + 上限提示），
> 随主会话口径一并裁决。

### 3.3 第二批修复实施计划（用户裁决 2026-09-11：建议修 10 项全修）

> 处置口径：DEF-021 已复核采纳改判（测试脚本修正归第二轮）；DEF-016 弃置；
> DEF-018 延期至 C 轨 UI 批次。以下 10 项为第二批修复范围，修复会话按序
> 实施，每项完成后回填本表状态与提交号；全部合入后启动第二轮（DEF 全量
> 回归 + NF 压测 + P0/P1 回归 + DEF-016 对应观察项补测）。

| 顺序 | DEF | 修复方案要点 | 预估 |
|------|-----|-------------|------|
| 1 | DEF-001 | 工单提交校验：无有效居住关系账号提交工单拒绝（经 residence_relation 校验居民与社区关系，对齐 R9/R12；注意代建账号在入住申请通过前的边界） | 小 |
| 2 | DEF-002 | 预约冲突口径改为「同时段」（接口设计 §9.7.1.1 口径：同时段不可重复，同天不同时段放行；确认无同天限次需求——需求规格 R32~R33 无此约束，2026-09-11 已核实） | 小 |
| 3 | DEF-024 | GlobalExceptionHandler 增补 MaxUploadSizeExceededException 映射 → 400 + 「文档不能超过 10MB」；同时复核 max-file-size 与业务上限留 buffer（如 12MB/请求 64MB）使业务层 400 可达 | 小 |
| 4 | DEF-020 | 工单驳回补通知（reject 方法 notificationService.create，对齐其他流转节点模式，R48） | 小 |
| 5 | DEF-023 | 看房预约四类流转（confirm/complete/violate/cancel）补通知（对齐资源预约 confirm 模式，R55/R48） | 小 |
| 6 | DEF-013 | 租约状态机收紧：ALLOWED_TRANSITIONS 删除 ACTIVE→ARCHIVED（R16 线性流转） | 小 |
| 7 | DEF-017 | 归档租约禁改：LeaseService.update 增终态校验（ARCHIVED/MOVED_OUT 拒改，R16） | 小 |
| 8 | DEF-015 | 时段删除引用保护：存在未完成预约（待审核/已预约）的时段禁止删除（对齐资源本体 5105 模式，R6） | 小 |
| 9 | DEF-019 | 工单「不满意退回处理中」端点：TO_CONFIRM→IN_PROGRESS，居民发起须填原因（R22，流转表已留边） | 中 |
| 10 | DEF-022 | 评价双维度聚合端点：by-staff（经 work_order_assignment）/ by-category 两维度（R39） | 中 |

实施约束（同第一批）：单测全量回归；不动前端；不动 02/07 等测试回写文档
（08 是唯一回写文档）；不占用 8080（自测独立端口+独立库）；提交规范
fix(stage-50): 步骤 8 缺陷修复第二批——…

### 3.4 第二批修复实施记录（2026-09-11，后端修复会话）

按 §3.3 顺序 10 项全部完成，**无与方案要点的偏差**（逐项核对）：

| DEF | 实施摘要 | 单测回归（正/反例） | 备注 |
|-----|---------|-------------------|------|
| DEF-001 | WorkOrderService.create 补在住关系校验：residence_relation 按（提交人，类别归属社区，move_out_date IS NULL）计数，0 条 FORBIDDEN 拒绝 | WorkOrderBatch2FixTest：无关系拒（403）/有关系放行 2 用例 | 代建账号边界：R8 代建「成功后可登录」，登录≠居民端业务入口——与 R9「申请通过后获得入口」一致，代建账号须入住申请通过后才可提单，无特殊放行 |
| DEF-002 | 同天限次拦截改为「同居民同资源**时段重叠**」拦截（lt/gt 边界对，首尾相接放行），错误提示改「同一时段已有本人的预约」 | ReservationSameDayFixTest：同天不同时段放行/同时段重叠拒/首尾相接放行 3 用例 | 与 V10 唯一约束无冲突（约束按槽不按天）；全局重叠拦截（他人时段占用）不受影响 |
| DEF-024 | GlobalExceptionHandler 增 MaxUploadSizeExceededException → 400「单个文件不能超过 10MB」；multipart 上限与业务上限留 buffer（10MB→12MB、60MB→64MB）使业务层 400 可达 | MaxUploadSizeHandlerFixTest：异常映射 400+提示 1 用例 | >10MB 且 ≤12MB 走业务层原文案（「文档不能超过 10MB」/「图片不能超过 5MB」），>12MB 走新映射 |
| DEF-020 | reject 补 notificationService.create（居民收「工单已驳回：理由」） | WorkOrderBatch2FixTest：驳回通知 1 用例 | 与其他流转节点通知模式对齐 |
| DEF-023 | ViewingAppointmentService 四类流转（confirm/complete/cancel/violate）补 notifyApplicant；游客预约（无 userId）跳过通知不报错 | ViewingNotificationFixTest：四流转各 1 + 游客边界 1 共 5 用例 | R55 口径内边界：游客无账号，站内通知语义不适用 |
| DEF-013 | LeaseService ALLOWED_TRANSITIONS 删 ACTIVE→ARCHIVED 跳级边 | LeaseStateMachineFixTest：跳级拒/线性两步过 2 用例 | 与架构 §6.2 线性口径对齐 |
| DEF-017 | LeaseService.update 补 ARCHIVED 终态校验（STATE_TRANSITION_INVALID「已归档的租约不可修改」） | LeaseStateMachineFixTest：归档拒改/在住可改 2 用例 | MOVED_OUT 非终态（可续租前修正）不在拦截范围，以 R16 原文「归档后不可修改」为界 |
| DEF-015 | ResourceTimeslotService.delete 前查引用：按（resource_id + 模板起止时段）匹配占用中预约（PENDING/RESERVED），>0 抛 5106 | ResourceTimeslotDeleteFixTest：占用拒删/仅历史预约放行 2 用例 | 预约表无 timeslot_id（V1 事实），按模板形状匹配——同资源不同模板形状的同起止时段会保守拦截（宁可误拦不可漏拦的删除保护取向） |
| DEF-019 | 新增 returnBack：TO_CONFIRM→IN_PROGRESS（流转表第一批已留边），居民本人发起 + WorkOrderActionDTO @NotBlank 保证原因必填 + 时间线 RETURN + 通知当前服务人员；端点 PATCH /work-orders/{id}/return（RESIDENT） | WorkOrderBatch2FixTest：退回成功含通知/非 TO_CONFIRM 拒/非本人 403 共 3 用例 | — |
| DEF-022 | StatisticsService.evaluationAggregation(communityId)：byStaff（工单→最新派单人，改派取 id 最大 assignment）/ byCategory（工单→类别）两维度，每行 total/avgRating/satisfiedRate/name；端点 GET /api/v1/statistics/evaluations/aggregation（ADMIN+，支持社区筛选） | EvaluationAggregationFixTest：byStaff（含改派归属）/byCategory/空数据 3 用例 | 输出为维度行列表（含名称回填），未含服务人员/类别 ID 字段——如看板需要 ID 键再补（当前无前端消费方） |

**单测全量回归**：235/235 通过（第一批后 211 + 本批新增 24 修复回归用例），零失败。
8080 SP-03 实例全程未触碰；本批纯单测验证（无迁移变更），未起独立实例。

### 3.5 第二轮回归（2026-09-11，主测试会话，基线 3e27307 = c946b69 + eac158e 全部修复合入）

**执行环境**：main 3e27307 干净构建，8081 端口（SPRING_DATASOURCE_URL 指向
community_residence_test，V10 迁移由 Flyway 自动应用，历史同槽重复已清且唯一约束
uk_reservation_slot/uk_viewing_slot 在位核验）；8080 为并行 dev 实例未触碰。

**回归范围与结果**（脚本 `scripts/r2_def_regression.mjs` + 既有套件重跑）：

| 类别 | 范围 | 结果 |
|------|------|------|
| 致命 4 项（DEF-005~008） | SP-01 重跑：资源/看房各 3 轮 10 路并发 + 12 边界对 | **20/20 全过**——每轮仅 1 路成功且落库 1 条（R33/R55 拦截率 100%，出口红线达标；SP-003 第 3 轮首跑 0/10 系第一轮旧数据同日期占用，清理日期残留后复验通过） |
| 域功能 19 项 | r2_def_regression.mjs（DEF-001~004/009~015/017/019/020/022~024 + DEF-021 脚本口径复核） | **19/19 全过**（见 §2 各行回归结果列） |
| P0 100% + P1 回归 | 冒烟 11/11、SP-02 权限矩阵 84/84、C10 认证 10/10、状态机否定矩阵 84/84、C1~C12 十二域全量、SP-04 8/8、SP-05 4/4 | **16 套件 263/263 断言点零失败**（P1 抽 30% 要求以全量覆盖超额完成） |
| NF 性能 | JMeter 5.6.3（JDK 17）50 并发/60s ramp-up/10min 稳态 7 核心接口 | P95 73~700ms 全部 ≤2s、服务端零 HTTP 错误（详见 07 §2） |
| 定时任务 | LeaseExpiryReminderTask 01:30 补观察 | 直调任务 bean 全链：扫描 1010 条、新发提醒 1010（EXPIRING 491+EXPIRED 519）、租户+管理员通知落库、sys_task_log SUCCESS/processed=1010 |

**执行中发现并处理的脚本侧适配（非缺陷）**：①冒烟/C10/C1/C4 套件的
「绑定/冻结变更后立即登录」在新 DEF-009 同秒吊销口径下被连带拦——统一加隔秒
重登（修复会话已声明「重新登录需隔秒」口径）；②数据累加导致的脚本容量问题
（sys-users 分页 500、空置房/时段/看房档位动态查找、跳过被占日期）；③过时
断言更新（E7/TC-C7-004 同天拦截→同时段口径、状态机矩阵 TO_CONFIRM→process
退回边/TO_ASSIGN→reject 合法化）；④ SP-005 越权日志断言改读 8081 实例日志
（BACKEND_LOG 环境变量）。

**结论**：22 项待回归 DEF 全部回归通过转「关闭」；DEF-021 拒绝（脚本口径）、
DEF-016 弃置、DEF-018 延期至 C 轨（用户裁决维持）；**无新增缺陷（DEF-025 无）**，
修复未引入新问题。

### 3.6 第三批修复实施计划（前端美化移交发现，用户裁决 2026-09-14：全修）

> 来源：frontend-beautify 分支交接（BEAUTIFY_NOTES 后端适配清单 + BACKEND_HANDOVER_PROMPT 四块）。
> 用户裁决：B 项 7 缺陷 + D 项 4 端点 + C3 公告 is_pinned 补全全部实施，
> C1 接口设计三处修订采纳（以实现为准勘误）；测试库保留至开发完成，
> 全量回归前清理重建。

**B 项 · 预存在缺陷（DEF-025~031）**

| 顺序 | DEF | 问题 | 严重度 | 修复方案要点 |
|------|-----|------|--------|-------------|
| 1 | DEF-025 | ADMIN 调 GET /sys-users 403 → 社区管理员无法派单（R20 主链路断裂） | 严重 | 新增 ADMIN 可用的服务人员选项端点（如 GET /work-orders/assignable-staff，数据级权限限绑定社区）或放开 sys-users 查询对 ADMIN 的数据过滤——二选一，倾向前者（最小暴露面），决策日志留痕 |
| 2 | DEF-031 | 公告 GET /notices 缺 priority 过滤参数（R25 置顶配套） | 一般 | 列表接口补 isPinned/priority 筛选参数 |
| 3 | DEF-027 | 反馈 close() 不推 WS（对端滞后一周期）+ GET /feedbacks 缺 keyword | 轻微/一般 | close() 补 convertAndSend（FEEDBACK_STATUS 载荷）；列表补 keyword 模糊参数 |
| 4 | DEF-026 | 工单号 4 位随机撞唯一键 409 无重试 | 一般 | generateOrderNo 补循环重试（上限 N 次） |
| 5 | DEF-028 | unsatisfied 无 hasFollowup 参数（前端 N+1 兜底） | 一般 | 评价跟进列表补参数（服务端过滤） |
| 6 | DEF-029 | 统计无 STAFF 按日序列（前端 MOCK）+ 工单列表无时间范围/多状态分页 | 一般 | 统计补 STAFF 可访问按日端点（限本人数据）；工单列表补 startTime/endTime/status 列表参数 |
| 7 | DEF-030 | 创建用户手机号选填 vs @NotBlank 契约漂移 | 轻微 | 二选一：后端放开为可选（若业务允许空手机号）或前端补必填校验——按业务语义裁决（倾向后端 @NotBlank 维持、前端校验补齐归 C 轨批次，后端侧仅留痕） |

**D 项 · 补端点/约束**

| 顺序 | 项 | 内容 | 要点 |
|------|-----|------|------|
| 8 | D-端点1 | 楼栋事务级联删除 | DELETE /buildings/{id}?cascade=true 或独立端点，参照社区级联（stage-50-1b 事务模式）；默认保持现状拒删保护，cascade 显式触发 |
| 9 | D-端点2 | 楼栋/单元/房屋批量创建 | 事务性批量端点（POST /buildings/batch 等）；部分成功语义对齐 R8 批量导入先例（逐行反馈）；前端 310 请求循环可整体替换 |
| 10 | D-端点3 | 管理员直建居住关系 | POST /residence-relations（ADMIN 限绑定社区，直接建立关系不走审批；写操作日志） |
| 11 | D-端点4 | 预约防重唯一索引 | V11 迁移：resource_reservation 加 (user_id, resource_id, reserve_date, start_time, 占用态) 唯一约束——参照 V10 slot_occupied 生成列模式（占用中=1/其余 NULL）；补 DEF-002 并发穿透最后一块 |
| 12 | C3 项 | 公告表单字段补全 | 保存接口接收 priority/type/expireTime（落 notice 列或合理映射）；NoticeVO 暴露 is_pinned（V9 列已建、置顶排序已实现，仅差 VO 与接收）；priority 过滤即 DEF-031 |

**C1 项 · 接口设计.md 勘误（文档，随第三批一并提交）**：9.7.1.1 预约请求体
（timeslotId/participants → resourceId/reserveDate/startTime/endTime/
purpose/contactPhone/remark）、9.2.3.2 phone → residentPhone（补
relationType/houseLocation）、9.8 评价跟进单 content 字段 + followups
纯数组。C2/C4 待用户裁决后处置（见决策日志）。

实施约束（同前两批）：单测全量回归；不动前端（DEF-030 前端校验归 C 轨）；
08 唯一回写文档；不占 8080；新迁移从 V11 起。

### 3.7 C2 可约时段栅格化设计定案（用户裁决 2026-09-14，归第三批第 13 项）

**模型（Slot Grid）**：
- 每资源配置：`slot_unit` 预约最小单位（分钟，15/30/60 可选默认 30）+
  `capacity`（语义收窄重定义：**资源级·每时段可承载人数上限**）；
- 周循环模板保存时强制校验起止对齐 slot_unit（不对齐 400，源头保栅格纯净）；
- 预约请求起止须为 slot_unit 整数倍（服务端校验 400）；
- available-slots 返回标准化栅格数组 `[{start, end, booked, capacity, remaining}]`：
  一次范围查询该日该资源全部占用态预约 → 内存按栅格分桶计数；
- 可约判定（服务端权威）：创建预约在 Redisson 锁内对请求覆盖的每个时段
  逐一检查 booked < capacity（复用 DEF-005 锁模式）；
- 三道安全防线：栅格对齐校验 + 锁内逐时段容量检查 + V11 唯一约束兜底；
- 迁移与存量：V11 补 resource.slot_unit 列（默认 30）；模板 max_bookings
  退役（统一 capacity 口径，存量模板值迁移映射后废弃列或保留不用）；
  存量非对齐预约不清洗，按「覆盖即占用」分桶计入；新预约强制对齐；
- 日容量 = 该日开放时段数 × capacity（派生计算不落库，两套口径问题消解）；
- C12 看房预约不套用此模型（capacity=1 语义简单，文档注明口径差异即可）。

性能口径：占用计算 O(预约数+时段数) 单查询分桶，不引入 Redis 预聚合
（50 并发教学规模过度设计）；同日排序 ORDER BY start_time（顺带修无序问题）。

### 3.8 第三批修复实施记录（2026-09-14，后端修复会话）

按 §3.6/§3.7 顺序 13 项全部完成，逐项实施摘要（B 项 7 + D 项 4 + C3 项 1 +
C2 栅格化 1；DEF-030 后端侧仅留痕无代码改动）：

**B 项 · 缺陷修复**

| DEF/项 | 实施摘要 | 单测回归（正/反例） | 备注 |
|--------|---------|-------------------|------|
| DEF-025 | **方案 A**：新增 `GET /api/v1/work-orders/assignable-staff`（ADMIN+）：启用状态 STAFF 账号、最小暴露面（仅 ID+姓名，无用户名/手机号）；ADMIN 场景绑定社区内派过单的服务人员**优先置前**（软排序引导，非硬过滤——R20 判据仅要求启用状态，STAFF 数据权限走派单关系无社区绑定，2026-09-14 核实） | WorkOrderBatch3FixTest：仅启用 STAFF 返回/熟手优先排序 2 用例 | 方案选择理由：sys-users 放开数据过滤会暴露全量账号字段（用户名/手机号/角色/状态），选项端点最小暴露面且语义聚焦派单场景；决策日志留痕 |
| DEF-031 + C3 项 | ①NoticeService.page 补 priority/isPinned/type 过滤参数（wrapper 条件注入）；②CreateNoticeDTO 补接收 priority（@Pattern LOW/NORMAL/HIGH/URGENT）/type（@Pattern ANNOUNCEMENT）/expireTime（别名映射 endTime 列，与 endTime 并设时以 endTime 为准，缺省 publishTime+30 天）；③NoticeVO 暴露 priority/type/expireTime（is_pinned 前批已暴露）；④V12 迁移 notice 补 priority/type 列（默认 NORMAL/ANNOUNCEMENT） | NoticeBatch3FixTest：字段接收+落库/缺省值/endTime 优先级/过滤参数注入 wrapper 4 用例 | expireTime 不落新列——语义即 notice.endTime，避免双列漂移 |
| DEF-027 | ①close() 补 convertAndSend（/topic/feedback/{id}，FEEDBACK_STATUS 载荷 {feedbackId,status}，与 sendMessage 同口径：任一端在线即广播、失败轮询兜底）；FeedbackPushVO.data 放宽为 Object（双载荷类型）；②GET /feedbacks 补 keyword（标题/内容 like） | FeedbackBatch3FixTest：办结推送 FEEDBACK_STATUS/双方离线不推 2 用例 | 前端零改动（载荷结构向前兼容） |
| DEF-026 | generateOrderNo 补撞号循环重试：生成前 selectCount 查重，撞号重试上限 5 次（ORDER_NO_RETRY_LIMIT），仍撞抛 OPERATION_FAILED「工单号生成失败」；顺带 keyword 补 orderNo like 匹配（前端待补清单⑦） | WorkOrderBatch3FixTest：撞号重试成功/达上限拒绝 2 用例 | 4 位随机空间下单日万级提交才可能耗尽，教学规模不可达 |
| DEF-028 | unsatisfied 补 hasFollowup 参数（Boolean：true 已跟进/false 待跟进/null 全量），页内按 followupMapper.selectCount 过滤 | EvaluationBatch3FixTest：true/false/null 三分支 3 用例 | 服务端过滤消前端 N+1；未做 SQL 级过滤（当前页量级可控，避免 join 复杂化） |
| DEF-029 | ①新增 `GET /api/v1/statistics/staff/trend?days=N`（STAFF/ADMIN/SUPER_ADMIN，限本人被派单工单，默认 7 上限 90 日）：assignedTrend/completedTrend/processingTrend 日序列（按 created_at 归日，completed 含 TO_CONFIRM/CLOSED）+ 今日三值 + totalCompleted；②工单列表补 statuses（逗号分隔多状态 IN）/startTime/endTime（created_at 范围）参数 | WorkOrderBatch3FixTest：statuses 多选/时间范围 2 用例 | 趋势按工单提交时间归日（派单时间无冗余列，与看板 workOrderTrend7d 同口径） |
| DEF-030 | 后端 @NotBlank 维持（手机号为账号体系联系必填字段，uk_phone 唯一约束依赖），前端必填校验归 C 轨批次；决策日志 2026-09-14 留痕 | —（无代码改动） | — |

**D 项 · 补端点/约束**

| 项 | 实施摘要 | 单测回归 | 备注 |
|----|---------|---------|------|
| D-端点1 楼栋级联 | `DELETE /buildings/{id}?cascade=`（默认 false 维持现状拒删保护 5102）；cascade=true 参照社区级联事务模式自底向上物理删除：房屋状态历史 → 房屋 → 单元 → 楼栋；在住居民保护（任一房屋存在在住关系整体拒绝回滚）；新增 mapper physicalDeleteById/ByIds（绕过 @TableLogic 消 FK 引用） | BuildingBatch3Test：默认拒删/级联自底向上顺序/在住保护整体拒绝/空楼栋直删 4 用例 | 替代前端逐层编排非原子删除（R4-D2） |
| D-端点2 批量创建 | `POST /buildings/batch`（复用 CreateBuildingDTO 列表，上限 100）/ `POST /units/batch`（名称列表，上限 100）/ `POST /houses/batch`（复用 CreateHouseDTO 列表，上限 200）；部分成功语义对齐 R8 CSV 导入先例：逐行 try-catch 独立不整体回滚，BatchCreateResultVO 逐行反馈 rowNo/success/id/reason | BuildingBatch3Test：楼栋部分成功/单元社区推导/房屋 area 缺失行失败 3 用例 | 前端 310 请求循环可整体替换 |
| D-端点3 直建关系 | `POST /residence-relations`（ADMIN+，AdminCreateRelationDTO：residentId/houseId/relationType/moveInDate/remark）：ADMIN 限绑定社区（checkCommunityAccess），校验房屋 VACANT 且无在住关系，建关系（isPrimary=1）+房屋置 OCCUPIED，@OperationLog 写操作日志；租约不联动（TENANT 走 C3 独立登记，与审批流 approve 口径一致） | AdminCreateRelationTest：直建成功含房屋翻转/已入住拒/越社区 403 3 用例 | 不走审批流语义：台账补录场景 |
| D-端点4 唯一约束（V11） | resource_reservation 撤销 V10 uk_reservation_slot（全局同槽唯一），替换为 **uk_reservation_user_slot（user_id, resource_id, reserve_date, start_time, slot_occupied）**——用户级防重；含存量同键脏数据清理（保留最早一条，其余置 CANCELLED 备注来源） | 随 SlotGridFixTest 并发用例覆盖约束语义 | **与 §3.6 原方案的偏差（重要）**：原方案 (user_id,resource_id,reserve_date,start_time,占用态) 约束前提是「同槽全局唯一」仍成立；Slot Grid 定案后同槽允许多人至 capacity，全局约束与容量模型直接冲突（容量>1 资源第二人预约必撞约束被误拒）。约束收窄为用户级（拦同一居民并发重复提交），全局容量控制交由锁内逐格检查（防线②）。决策日志留痕 |

**C3 项**：随 DEF-031 合并实施（见上表）。

**C2 项 · Slot Grid 栅格化（08 §3.7 定案）**

| 模块 | 实施摘要 | 单测回归 |
|------|---------|---------|
| V11 迁移 | public_resource 补 slot_unit 列（INT 默认 30，注释 15/30/60）；同迁移承载 D-端点4 唯一约束变更（迁移原子性：约束降级与栅格列同属 Slot Grid 模型变更，一次迁移保证中间态不可见） | —（迁移由独立库 Flyway 流程验证，见下「回归环境」） |
| 模板对齐校验 | ResourceTimeslotService create/update 补 checkSlotAlignment：起止须为资源 slot_unit 整数倍（无秒级分量+分钟数整除），不对齐 400；存量非对齐模板保留（可约计算宽容分桶）仅拦新写 | 随 SlotGridFixTest |
| 资源配置 | PublicResource 补 slotUnit（DTO @Min15/@Max60 + 服务层枚举校验 15/30/60，缺省 30 保留既有值）；ResourceVO 暴露 slotUnit；capacity 注释语义收窄为「每时段可承载人数上限」 | — |
| available-slots 栅格分桶 | 模板按日期展开为 slot_unit 粒度栅格（模板起止向上对齐宽容处理），一次范围查询占用态预约后内存分桶（**覆盖即占用**：预约与栅格区间有交集即计入该格计数）；返回字段兼容数组（timeslotId 改为「模板ID×10000+当日分钟数」栅格标识、maxBookings=capacity、currentBookings=booked、status AVAILABLE/FULL）；同日按 startTime 排序（顺带修无序问题） | SlotGridFixTest：6 格拆分+排序+booked 计数/FULL 状态 2 用例 |
| 创建预约三道防线 | ①起止对齐校验 400（slot_unit 整数倍）+ 模板覆盖校验（既有）；②Redisson 锁内（复用 DEF-005 锁模式 resource+date 粒度）本人重复拦截（同资源时段重叠）+ **逐栅格容量检查**（请求覆盖的每个 slot_unit 格 booked<capacity，存量非对齐预约覆盖即计入）；③V11 uk_reservation_user_slot 兜底（DuplicateKeyException 转业务冲突）。confirm 的 checkCapacity 同步改逐格口径（排除自身） | SlotGridFixTest 12 用例：对齐校验 4（非对齐起/止拒、15 分钟粒度放行、60 分钟粒度拒 30 对齐）+ 容量 4（多人同槽至 capacity/满员拒/存量非对齐覆盖计入/跨格请求逐格检查）+ 边界 1（首尾相接放行）+ **并发 1（容量 3 同槽 5 路并发恰 3 成功 2 拒绝）**+ 分桶 2 |
| C12 口径注明 | 看房预约不套用 Slot Grid（capacity=1 语义简单，V10 uk_viewing_slot 全局唯一约束维持不变，与资源预约的容量模型差异已在 V11 迁移注释与 SlotGrids 类注释注明） | 既有 ViewingAppointmentConflictFixTest 不受影响（全量回归通过） |

**口径偏离点说明（对照 §3.7 定案）**：
1. **V10 约束降级为用户级**（见 D-端点4 行）——定案未预见的与容量模型的冲突，
   属实现必要偏离，决策日志留痕；
2. **timeslotId 字段复用为栅格标识**（模板ID×10000+当日分钟数）——定案要求
   返回标准化数组 [{start,end,booked,capacity,remaining}]，实施为保持前端零改动
   复用既有 VO 字段名（maxBookings=capacity、currentBookings=booked、
   remaining=maxBookings-currentBookings 可前端派生），语义等价；
3. **模板 max_bookings 退役为无操作**——核实 resource_timeslot 表无该列（V1
   事实，V2 种子亦无该数据），「存量值迁移映射」无对象，仅在注释与文档留痕。

**C1 项 · 接口设计.md 三处勘误**：9.7.1.1 创建预约请求体（timeslotId/
participants → resourceId/reserveDate/startTime/endTime/purpose/contactPhone/
remark + Slot Grid 对齐说明）、9.2.3.2 房屋居民列表（phone → residentPhone +
补 relationType/houseLocation）、9.8 跟进单（三字段 → 单 content 字段）+
followups（分页 → 纯数组）——均以实现为准修订。

**单测全量回归**：**272/272 通过（第二批后 235 + 本批新增 37 修复回归用例），
零失败**。新增测试类：SlotGridFixTest 12 + WorkOrderBatch3FixTest 6 +
FeedbackBatch3FixTest 2 + EvaluationBatch3FixTest 3 + NoticeBatch3FixTest 4 +
BuildingBatch3Test 7 + AdminCreateRelationTest 3；既有测试适配口径变更
（Reservation 冲突/容量口径改 selectList 逐格、availableSlots 分桶断言、
page/unsatisfied/delete 新签名）。V11/V12 迁移尚未在真实 MySQL 实例验证
（本轮纯单测自测，迁移执行验证与全量回归归主会话下一轮——测试库清理重建后
Flyway V1~V12 全新流程执行即覆盖）。8080 实例未触碰。

### 3.9 第三批专项回归（2026-09-14，主测试会话，基线 e807b5b = 0a1935b 第三批修复合入后）

**执行环境**：main e807b5b 干净构建，8081 端口（SPRING_DATASOURCE_URL 指向
community_residence_test，测试库按 03 口径 A 全新重建，**Flyway V1~V12 全新执行成功**
——V11/V12 迁移真实 MySQL 验证随本段达成，08 §3.8 遗留验证项闭环）+ 01 种子扩展
（社区 1/perf 池 200/test_ 账号 4/并发时段 70/看房时段 77 全自检通过）；
8080 为 Kimi 并行工作区 dev 实例未触碰。

**执行范围与结果**（脚本 `scripts/r3_slotgrid.mjs` + `scripts/r3_batch3.mjs`）：

| 范围 | 脚本/断言组 | 结果 |
|------|------------|------|
| C2 Slot Grid 栅格化专项（08 §3.7 定案全部断言点） | r3_slotgrid SG-01~09：slot_unit 三粒度/模板对齐正反例/available-slots 栅格分桶与库一致/锁内逐格容量/并发不超卖（容量 3 五路恰 3 成，两轮）/V11 用户级约束（同居民 10 路仅 1 成功+多人同槽可达+索引形状双向核对）/存量非对齐宽松计入/confirm 逐格口径/C12 看房全局唯一不回归 | **28/28 全过，无缺陷** |
| DEF-025 派单选项端点 | B3-01：ADMIN 可用/仅启用 STAFF/最小暴露面（id+realName）/熟手置前软排序/RESIDENT·STAFF·未登录拒 | ✅ |
| DEF-026 工单号重试+keyword | B3-02：20 单零 409+工单号精确命中 1+标题命中 | ✅ |
| DEF-027 反馈 keyword+办结推送 | B3-03/04：keyword 与库一致；FEEDBACK_STATUS {feedbackId,status} 在线 8s 内送达（SockJS+STOMP 裸客户端） | ✅ |
| DEF-028 hasFollowup | B3-05：true/false/null 三分支与 unsatisfied_followup 明细 100% 一致 | ✅ |
| DEF-029 STAFF 趋势+工单筛选 | B3-06/07：趋势限本人（与库内被派单计数一致）+他人单不计入+RESIDENT 403；statuses 多选 total 与库一致+时间窗 20/20+STAFF 列表零越权泄漏 | ✅（days 0/91 钳位为实现口径非缺陷） |
| DEF-030 手机号必填契约 | B3-08：缺/空 phone 均 400 零落库（@NotBlank 维持，前端归 C 轨） | ✅（留痕口径） |
| DEF-031+C3 公告字段 | B3-09/10：三过滤生效；priority/type 落库；endTime 优先；非法值 400；VO 暴露 17 字段含四新字段；存量默认 NORMAL/ANNOUNCEMENT | ✅ |
| D-端点1 楼栋级联 | B3-11：默认 5102 拒/在住保护 5104 整体回滚/空楼直删/居民 403——**迁出后级联遇历史关系 FK 阻断 500（DEF-033 登记）** | ✅（取证口径） |
| D-端点2 批量创建 | B3-12：全有效行 2/2 逐行反馈/超限拒/单元社区推导/权限——**坏行整批 400（DEF-032 登记，部分成功语义不可达）** | ✅（取证口径） |
| D-端点3 直建关系 | B3-13：成功翻 OCCUPIED+isPrimary/重复 5205 拒/越社区 404/TENANT 零租约联动/居民 403——**创建类操作日志零留痕（DEF-034 登记，严重）** | ✅（取证口径） |

**脚本适配留痕（非缺陷）**：①CreateNoticeDTO.isPinned 为 Integer（boolean true 序列化
被 Jackson 拒 400），脚本传 1；②staff/trend 返回 assignedTrend 为日期键对象非数组；
③days=0/91 钳位（Math.min(Math.max(days,1),90)）；④公告时间字段需 ISO T 分隔；
⑤create notice 返回 data=null，id 查库获取；⑥库列名勘误（feedback.resident_id/
lease_record.tenant_id/work_order 无 deleted 列/sys_operation_log 无 content LIKE 可用的
target 检索路径改 target_type+target_id）。

**结论**：第三批 13 项中 DEF-025~031 六项缺陷修复全部回归通过转关闭（DEF-030 留痕
口径）；Slot Grid 栅格化全断言通过（V10 降级/V11 用户级约束/分桶口径三偏离点均实证
无问题）；D-端点三项各暴露一项新缺陷（DEF-032 一般/DEF-033 一般/DEF-034 严重），
登记 §2 待用户裁决修复批次。全量回归（A 轨 16 套件+SP+NF+C 轨）在测试库清理重建后
的纯净基线上执行（用户裁决 2026-09-14）。
| DEF-037 | 第四轮 C 轨（2026-09-14，track_c_ui.py TC-C11-013~015 页面侧） | TR-C11-04（R51 渠道分级） | 通知渠道分级配置无前端入口：管理端全局配置页对 notify.channel.levels 仅以键值文本行原样渲染，无渠道勾选/分级交互控件；前端 api 层无任何 channel 封装（全源码检索「渠道/channel」零命中）。后端 GET/PUT /api/v1/notifications/channel-levels 已实现（D2 交付）且接口侧 A 轨通过——前端未接入。期望：渠道勾选前端入口。实际：仅键值行展示。 | 一般（后端能力完整，管理操作体验缺前端入口） | 新建→打开 | —（前端侧，归后续前端批次） | — |
| DEF-038 | 第四轮 C 轨（2026-09-14，track_c_ui.py TC-C2-006/007 页面侧） | TR-C2-04（R8 v1.2） | 居民代建与 CSV 批量导入无前端表单：居民管理页三 Tab 均无「代建居民」表单与 CSV 导入入口；后端 POST /api/v1/residents/admin-create 与 /import 已实现（D1 交付）且接口侧 A 轨通过——前端零调用。期望：代建表单+导入入口。实际：页面无入口。 | 一般（后端能力完整） | 新建→打开 | —（前端侧） | — |
| DEF-039 | 第四轮 C 轨（2026-09-14，track_c_ui.py TC-C5-006 页面侧） | TR-C5-03（R25 置顶） | 公告置顶无管理端设置入口：公告新建/编辑表单无「置顶」开关，提交体 buildRequest 不含 isPinned；V9 notice.is_pinned 列+置顶排序后端已实现（A 轨 B3-09/B3-10 过），前端无路径写入该列（列表置顶标记防御式读取恒不点亮）。期望：管理端可设置置顶。实际：无入口。 | 一般（后端能力完整；另公告定向粒度止于社区级，楼栋级 targets 前端未接——同源问题一并留痕） | 新建→打开 | —（前端侧） | — |
| DEF-040 | 第四轮 C 轨（2026-09-14，track_c_ui.py TC-C12-004 页面侧） | TR-C12-02（R53 筛选） | 游客端房源筛选缺「租售类型」「户型」两维控件：筛选条仅社区/状态/租金/关键字四控件；后端 V9 已加 layout/rentType 查询参数（A 轨通过），前端未接（源码注释「接口无户型参数」系改版前旧结论，参数已就绪）。期望：四维筛选。实际：两维缺失。 | 一般（后端能力完整；游客端 IHousing.layout/rentType 幽灵字段问题一并留痕） | 新建→打开 | —（前端侧） | — |
| DEF-041 | 第四轮 C 轨（2026-09-14，DEF-018 闭环验证） | TR-C4-01（R17） | DEF-018 闭环验证不通过（双侧根因）：居民端提交工单页类别选项含停用类别（实测 8 项含停用的「绿化养护2171」）——①后端 ServiceCategoryService.treeByCommunity() 查询不过滤 is_active=0；②前端 loadCategories() 直接平铺全部返回亦无过滤；③另发现 applyDto() 更新忽略 isActive 字段，停用类别目前只能 SQL 产生（管理端无停用入口）。期望：停用类别不出现在提交选项。实际：仍出现。 | 轻微→一般（升级理由：DEF-018 原延期裁决「随 C 轨处理展示净化」，现证实需后端改动而非纯前端过滤） | 修复（待回归） | e32a48b §3.9 | — |

### 3.10 第四轮全量回归（2026-09-14，纯净基线 e807b5b）

**执行环境**：测试库口径 A 清理重建（用户裁决：第三批残留只供开发、回归前必清）
→ Flyway V1~V12 全新 → 01 种子 → 02 N7 批量 → Redis seq=2004。

**回归范围与结果**（详见 07 §5）：

| 类别 | 范围 | 结果 |
|------|------|------|
| A 轨 16 套件 + r2/r3 | 冒烟/SP-02/C10/状态机/C1~C12/DEF 全量回归/第三批专项复跑 | **全绿**（脚本适配 6 处留痕：空置房动态扫描×2、BACKEND_LOG、占号前置、资源 id 动态查、B3-05 取证口径） |
| SP 专项 | SP-01（20/20）/ SP-04（8/8，019 加 2s 在线注册等待规避 DEF-036 时序）/ SP-05（4/4）/ SP-06（随 r2） | **全绿** |
| NF 压测 | JMeter@JDK17 50 并发 10min（500ms pacing），50843 样本 | **7 接口 100%、P95 16~620ms ≤2s**（N1/N2/N7 达标） |
| C 轨 UI | track_c_ui.py 16 条 + 视觉抽查 8 页 | 9 过 / 6 失败（DEF-037~041 登记） |

**第四轮新发现缺陷**：DEF-035（unsatisfied 页内过滤失真，r3 复跑）、
DEF-036（同用户多连接注销在线状态，SP-04 排查）、DEF-037~040（C 轨前端
入口缺失四项）、DEF-041（DEF-018 闭环验证不通过）——合计 7 项，加上第三批
专项的 DEF-032/033/034，当前打开缺陷 10 项（严重 1 + 一般 8 + 轻微 1）。

**结论**：第四批修复范围建议 = 严重 DEF-034（出口阻塞）+ 后端侧一般
DEF-032/033/035/036/041；前端侧 DEF-037~040 归前端批次（后端能力已交付，
不阻塞后端出口判据）；修复后需第五轮局部回归（10 项 + 受影响套件抽样）。

### 3.8 第四批修复实施计划（全量回归发现，用户裁决 2026-09-14）

> 裁决口径：后端侧 7 项全修（DEF-034 出口阻塞必修 + DEF-032/033/035/036/041）；
> 前端接入 4 项（DEF-037~040）归前端批次（后端已交付、前端未接线——
> Kimi 工作区或主会话前端任务执行，非后端缺陷）。修复后第五轮局部回归
> （第四批回归 + C 轨失败项复验），随后步骤 9/10 报告起草。

| 顺序 | DEF | 修复方案要点 | 预估 |
|------|-----|-------------|------|
| 1 | DEF-034（严重，出口阻塞） | OperationLogAspect @AfterReturning 补 returning = "result" 绑定；#result SpEL 恢复求值；端到端验证 10 服务 CREATE 类落库 | 小 |
| 2 | DEF-041 | 服务类别 tree 接口补 isActive 过滤（后端侧，对齐 R17「停用类别不再出现在提交选项」——DEF-018 纯前端方案证伪，后端收口） | 小 |
| 3 | DEF-033 | 楼栋级联删除处理历史居住关系：级联范围补 residence_relation 等业务引用清理（对齐社区级联的 26 表口径），或遇在住关系整体拒绝并明确错误码（二选一，按社区级联先例取向：**在住保护拒绝**——与 D-端点1 的「在住居民保护整体拒绝」口径一致） | 小 |
| 4 | DEF-032 | 批量创建部分成功语义修复：@Valid 整批校验改为逐行校验（循环内 manual validate 或去掉类级 @Valid 改方法内 Validator），对齐 R8 CSV 导入先例 | 小 |
| 5 | DEF-035 | hasFollowup 改 SQL 级过滤（query wrapper 条件），total 全集计数，消页内内存过滤 | 小 |
| 6 | DEF-036 | WebSocketSessionService 改连接计数（INCR/DECR，归零才注销）或维护会话集合——对齐 register/deregister 语义；验证双连接关一推送不丢 | 小 |

**前端批次（DEF-037~040，非后端）**：渠道勾选 UI / 代建与 CSV 导入表单 /
公告置顶与定向 UI / 房源两维筛选——四处均为第三批已交付端点的前端接线，
另行分发前端任务（主会话或 Kimi 工作区），C 轨复验随第五轮。

### 3.9 第四批修复实施记录（2026-09-15，后端修复会话）

按 §3.8 第四批计划顺序 6 项全部完成（DEF-034 → 041 → 033 → 032 → 035 → 036）：

| DEF | 实施摘要 | 单测回归（正/反例） | 备注 |
|-----|---------|-------------------|------|
| DEF-034（出口阻塞） | ①@AfterReturning 补 `returning = "result"` 绑定，resolveSpel/resolveLong/resolveContent/resolveCommunityId 全链路透传 result，`#result` SpEL 恢复求值；②**端到端复验中揭露的第二层缺陷**：Unit/House/ResourceTimeslot 三服务的 CREATE 注解 communityId 表达式引用 `#dto.communityId`，但其 DTO 无 communityId 字段（社区由楼栋/单元/资源推导）——SpEL EL1008E 求值失败整条日志仍被丢弃（DEF-014 时埋下、切面单测未覆盖），改用 `#result.communityId`（三 VO 均含该字段；TimeSlotVO 补暴露 communityId 列）；③**端到端验证**：独立实例 18080 + 独立库 community_residence_b4（V1~V12 全新 Flyway 通过），10 个服务 CREATE 逐一调用后操作日志 **12/12 断言全过**（10 目标 + RESIDENT 代建反例回归 + BUILDING communityId SpEL 解析断言），第二轮未暴露的原因（抽样未覆盖 CREATE）就此闭环 | OperationLogAspectTest 扩展：#result SpEL 求值正例（ResultHolder 42L）/result=null 防御性边界（不向调用方抛异常）2 用例 + 既有 4 用例适配新签名 | 修复后 CREATE 类日志全量落库（E2E 实证 12 条）；含 #dto.communityId 修正的 3 处注解 |
| DEF-041 | ①treeByCommunity 补 `is_active=1` 过滤（R17「停用类别不再出现在提交选项」——DEF-018 纯前端方案证伪，后端收口）；②新增管理端全量树 `GET /communities/{id}/service-categories/all`（ADMIN+，含停用，供配置管理——管理端需看见停用项才能重新启用）；③applyDto 补 isActive 写入（原 update 忽略致停用状态只能 SQL 产生） | ServiceCategoryActiveFilterTest：居民树过滤（wrapper 断言含 is_active）/管理端全量树不过滤 2 用例；E2E：创建 isActive=0 类别 → 居民树不含、全量树含 | E2E 于独立实例实证 |
| DEF-033 | **口径选择：在住保护拒绝 + 历史引用清理（混合，对齐社区级联先例）**——cascadeDelete 补齐 house_id 引用链清理：residence_application / residence_relation（历史已搬出行）/ lease_record→lease_reminder（租约链）/ housing→housing_timeslot+viewing_appointment（房源链），删除顺序子表在前；在住关系（move_out_date 为空）维持整体拒绝 5104（D-端点1 既有口径），历史关系随级联清理不再 FK 阻断 500 | BuildingBatch3Test 扩展：历史关系级联清理（四链全删验证）/引用链清理顺序（inOrder）2 用例 + 既有用例适配；E2E：直建→迁出→级联 200（原 500）+ 在住保护反例业务码拒绝 | 口径理由：社区级联 delete 同款语义（在住保护 + 历史行随社区消亡清理）——楼栋是社区子集，删除语义应对称；纯拒绝会让「整栋拆迁/重建」场景不可用（历史台账挡住结构维护），纯清理不加在住保护会误删在住居民关系（数据风险不对称） |
| DEF-032 | 三个批量 DTO 去元素级 @Valid/@NotBlank 级联（Controller 整批 400 根因）；服务层逐行校验：楼栋/房屋用 RowValidator（Bean Validation Validator 逐行 validate，坏行反馈首条违约提示）、单元用显式非空/长度检查——坏行逐行反馈原因、好行照常创建，对齐 R8 CSV 导入先例 | BuildingBatch3Test 重写三用例：楼栋坏行（名称空×2）1/3 成功、单元空串+超长 1/3 成功、房屋 area 缺失 1/2 成功；E2E：三端点坏行均 200 + 逐行 reason | 类级约束（@NotEmpty/@Size 上限）保留整批拒绝（列表级参数属调用方错误） |
| DEF-035 | unsatisfied 的 hasFollowup 改 SQL 级过滤：wrapper.exists/notExists 子查询（`unsatisfied_followup.evaluation_id = work_order_evaluation.id`），total 由 selectPage 全集计数（分页语义恢复）；删除页内内存过滤与 hasFollowupOf | EvaluationBatch3FixTest 重写三用例：true→EXISTS、false→NOT EXISTS、null→无条件（wrapper SqlSegment 断言） | N7 大数据量场景 false 返回全部待跟进分页（原首页滤空 0 条） |
| DEF-036 | WebSocketSessionService 改连接计数：register INCR+续期 TTL、deregister DECR 归零才 DEL key（多连接关一不断言离线）；isOnline 改读计数值（>0 在线），非数字残留值（历史 SET 单值格式）兼容按在线处理 | WebSocketSessionCountTest 6 用例：双连接关一保持在线/最后连接断开才注销/INCR+TTL 续期/**双连接并发 register×2+deregister×2 恰第二次才注销**（AtomicLong 并发计数断言）/旧格式兼容/Redis 不可用降级 false | TTL 2h 兜底不变；/user/queue 通知通道本就不受 isOnline 影响 |

**单测全量回归**：**284/284 通过（第三批后 272 + 本批净增 12：切面 2 + 类别 2 +
楼栋级联 2 + WS 计数 6，另重写/适配既有用例若干），零失败**。新增测试类：
ServiceCategoryActiveFilterTest（2）、WebSocketSessionCountTest（6）、
OperationLogAspectTest 扩展（+2）、BuildingBatch3Test 扩展（+2）。

**端到端验证（独立实例 18080 + 独立库 community_residence_b4，V1~V12 全新
Flyway 流程）**：两脚本全过——①DEF-034 专项：10 服务 CREATE + RESIDENT
代建反例共 12 日志断言 ALL PASS；②DEF-032/033/041 顺带：批量三端点坏行
逐行反馈、楼栋级联历史关系 200/在住保护业务码拒绝、类别树停用过滤 +
管理端全量树 ALL PASS。验证后实例停止、独立库已删除、脚本已清理，
8080/8081 全程未触碰。

**第四批补充（前端接线复核发现，2026-09-14 主会话合并 fba9012 后登记）**：

| 顺序 | DEF | 修复方案要点 | 预估 |
|------|-----|-------------|------|
| 7 | DEF-042 | channel-levels PUT 等级键白名单：仅接受 7 个真实业务类型键（WORK_ORDER/NOTICE/FEEDBACK/RESERVATION/RESIDENCE/LEASE/SYSTEM），未知键 400——前端已按真实全集渲染，后端补防线 | 小 |
| 8 | DEF-043 | 公告 update 端点重写 targets：编辑时 notice_target 先删后插（对齐 create 落库模式），存量公告编辑定向生效——前端已如实回显待后端收口 | 小 |
| 9 | DEF-044 | 房源详情页 rentTypeLabels 旧口径（SHARED 不在后端白名单）：前端详情页映射表对齐 V9 白名单（RENT/SALE）——归前端微改，随第五轮复验 | 轻微 |

数据残留裁决：W2 验证产生的 9 个冻结居民（id 10~18）不单独清理——归 50
阶段步骤 12 收尾统一清理（测试库重建口径），dev 库同批处置。

前端接线四个 DEF-037~040 状态：代码已合入（fba9012），复验归第五轮
局部回归（连同 C 轨 6 失败项复验与 CREATE 类日志逐服务断言）。

### 3.11 第五批修复实施记录（2026-09-15，主测试会话，DEF-042/043 两项）

按 §3.9 末表顺序 7/8 两项完成（第 9 项 DEF-044 为前端微改归第五轮复验批次）：

| DEF | 实施摘要 | 单测回归 | E2E（8081 实例） |
|-----|---------|---------|-----------------|
| DEF-042 | NotificationService.updateChannelLevels 补等级键白名单 VALID_LEVELS（7 键：WORK_ORDER/NOTICE/FEEDBACK/RESERVATION/RESIDENCE/LEASE/SYSTEM），未知键 400「非法通知等级」——**口径核实**：13 个 create() 调用点的 type 实参集恰为这 7 键（RESOURCE_RESERVATION/RESIDENCE_APPLICATION/LEASE_RECORD/VIEWING_APPOINTMENT/TASK 均为 sourceType 来源标识非等级），与前端 NOTIFICATION_LEVELS 渲染集一致，白名单无歧义 | ChannelLevelsTest 新增 2 用例（七键全放行/未知键拒）；既有 2 用例键值修正（原用虚构键 IMPORTANT，白名单后不再可达——测试侧口径漂移留痕） | 未知键 400/非法渠道仍 400/七键写入读回一致 3 断言全过，验后勾选清零还原 |
| DEF-043 | NoticeService.update 补 targets 先删后插（对齐 create 落库模式）；广播边界与 create 同口径（targets 空且非超管 400「社区管理员必须指定目标社区」；超管编辑为广播清空全部目标行）——原「目标范围不可变更」旧注释口径废除 | NoticeBatch3FixTest 新增 2 用例（重写先删后插 times(2) 插入/广播清空 never 插入） | 越社区编辑 404 拒（数据级过滤保护有效）/多目标编辑落 2 行（COMMUNITY+BUILDING）/回收编辑重写 1 行无残余/超管广播清空目标行/非草稿编辑仍拒 5 断言全过，验后公告已删 |

**单测全量回归**：**288/288 通过（第四批 284 + 本批新增 4），零失败**。
E2E 于 8081 测试实例实测（重启加载新代码后），测试数据已清理（notice 84/85 删除）。

第五批后剩余待办：第五轮局部回归（DEF-034~036 后端三项 + DEF-037~040 前端
接线复验 + DEF-041 + DEF-044 + CREATE 类日志逐服务断言 + C 轨 6 失败项复验）。
