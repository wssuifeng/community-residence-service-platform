# 冒烟测试记录（步骤 6）

> 编写时点：环境与数据就绪后、全面执行前。
> 冒烟范围 = 主流程可跑通的最小集合；**任一项失败即整体不通过，
> 打回开发修复后重测，不进入步骤 7**。

## 1. 冒烟范围（主流程清单：建议 E1/E2/E4/E6/E7/E10b 六条 + 登录认证 + 启动健康检查）

| # | 范围项 | 执行方式 |
|---|--------|---------|
| 0a | 启动健康检查（swagger 可达 + 未认证拒绝） | 脚本 |
| 0b | 登录认证（superadmin/admin1/staff1/resident1 + 冻结账号拒登） | 脚本 |
| 1 | E10b 权限初始化：超管建社区→建管理员→绑定→新管理员开展工作 + 两类越全局级拒绝 | 脚本 |
| 2 | E1 社区初始化：社区→楼栋→单元→房屋→资源→时段全链创建 | 脚本 |
| 3 | E2 注册入住：注册→登录→入住申请→审核通过→居住关系建立→居民端可提交工单 | 脚本 |
| 4 | E4 报修工单：建类别→提交→派单→接单→处理→完成→居民确认 + 时间线 | 脚本 |
| 5 | E6 反馈会话：提交→受理建会话→双向消息→办结→办结后只读 | 脚本 |
| 6 | E7 资源预约：查可约时段→提交→冲突拦截→审核通过 | 脚本 |

执行脚本：`scripts/smoke_test.mjs`（Node 24，可重复执行——账号/手机号随机、
空置房与预约日期动态挑选；执行后端 http://localhost:8080，测试库
community_residence_test，被测基线 main `8595609`）。

## 2. 执行记录（逐项：用例引用 / 实际结果 / 结论）

执行日期：2026-09-10（环境重建后首轮执行，共 4 次迭代修正脚本断言口径后全绿）。

| # | 检查项 | 实际结果 | 结论 |
|---|--------|---------|------|
| 0a | 启动健康检查 | swagger /v3/api-docs HTTP 200；无 token 调用业务接口 HTTP 401（GET /communities 为 R12 游客公开口径，不适用认证断言） | ✅ |
| 0b | 登录认证 | superadmin/admin1/staff1/resident1 四账号登录成功（JWT expiresIn=7200）；冻结账号 test_resident_frozen 拒登（code=5201） | ✅ |
| 1 | E10b 权限初始化 | 超管建社区+建管理员+绑定成功；新管理员登录后可查本社区；越全局级建系统用户 403；管理员建社区 403（v1.1 全局级口径） | ✅ |
| 2 | E1 社区初始化 | 社区/楼栋/单元/房屋(VACANT)/资源/时段全链创建成功（建社区仅超管——v1.1 口径，管理员 403 属正确行为） | ✅ |
| 3 | E2 注册入住 | 注册→登录→申请→审核通过(APPROVED)→居住关系建立→本社区类别提交工单成功 | ✅ |
| 4 | E4 报修工单 | 工单全链：提交→派单→接单→处理中→待确认→已完成(COMPLETED)；时间线 6 条记录完整 | ✅ |
| 5 | E6 反馈会话 | 提交→受理建会话→双向消息（2 条）→办结→办结后发消息被拒（code=5004） | ✅ |
| 6 | E7 资源预约 | 预约创建(PENDING)→同天冲突被拒（code=5002）→审核通过；available-slots 查询正常 | ✅ |

**冒烟期发现（非打回项，登记 DEF 转 A 轨裁决）：**
- DEF-001（一般）：无居住关系的注册账号可用任意社区启用类别提交工单
  （HTTP 200）——R9「申请通过后获得居民端功能入口」/R12 居住私有口径疑似
  应拒（TR-C2-04 / TR-C4-01 关联用例执行时复核）；
- DEF-002（一般）：资源预约实现为「同一居民同资源同天仅一约」，比接口设计
  §9.7.1.1「同一居民同一时段不可重复预约」更严——同天不同时段的合法预约
  被误拒（R33 重叠拦截口径，SP-01/TR-C7 关联用例执行时复核）。

## 3. 冒烟结论（通过进入步骤 7 / 不通过打回——附打回项与责任）

**结论：通过（11/11 检查点全绿，2026-09-10）**，进入步骤 7 第一轮 A 轨执行。

- 冒烟脚本前 3 次迭代中的失败项均经核实为**脚本断言口径错误**（游客公开
  路径误用作认证断言、v1.1 建社区全局级口径、绑定社区后需重新登录生效、
  reason 必填、账号唯一约束），不构成系统缺陷；
- 2 个真实观察项（DEF-001/002）按「执行暴露即记 DEF，不打回」指令登记
  08 文档，留 A 轨相关用例裁决严重度与修复必要性；
- 冒烟产生的过程数据保留（Q5 轮次间累加口径）。
