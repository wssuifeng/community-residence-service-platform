# 测试执行记录（步骤 7）

> 编写时点：步骤 6 冒烟通过后，按轮次追加。
> 逐用例的明细写入 04_测试用例/ 各文件的执行列或附录；本文只做轮次级
> 汇总与统计，避免与用例文档双份记录漂移。

## 1. 第一轮执行（日期 / 范围 / 结果统计：执行率、通过率、失败与阻塞清单）

**轮次头部登记**（03 计划 §2.1 口径）：

- 开始日期：2026-09-10（冒烟通过当日进入 A 轨）；
- 被测基线：main `8595609`（= 05 §1.2 登记基线 928f75c + 步骤 1c 合入
  e90e325 后的 main 头，后端单测 162/162）；
- 环境：后端 8080（测试库 community_residence_test，Flyway V1~V9 全新 +
  01 种子 + 02 N7 批量，Redis seq=2004 已同步）+ 前端 dev 5173；
- 执行范围：A 轨约 243 条（B 轨 9 条用户手动、C 轨 16 条 UI 改版后——
  见 11 §2 分轨表）；执行顺序按 03 计划 §2.3：P0 先行（SP-02 权限矩阵 →
  SP-01 冲突并发 → TC-C10 认证授权 → 状态机否定矩阵）→ C1~C12 功能域 →
  SP-03 长跑挂后台 → SP-04/05/06 → NF-01~11 → NF-14/15；
- 冒烟基线：11/11 通过（06 文档 §3，DEF-001/002 已登记 08）。

（执行中持续追加各段统计。）

### 1.1 P0 阶段执行结果（2026-09-10 冒烟通过后当日）

| 段 | 用例 | 执行方式 | 结果 | 缺陷 |
|----|------|---------|------|------|
| 冒烟（步骤 6） | 启动健康 + 登录认证 + E1/E2/E4/E6/E7/E10b | `scripts/smoke_test.mjs` | **11/11 通过** | DEF-001（一般）/ DEF-002（一般） |
| SP-02 权限矩阵 | TC-SP-005~010（六用例 84 组合） | `scripts/sp02_permission_matrix.mjs` | **84/84**（2 格按 DEF-003/004 登记口径记录实际行为） | DEF-003（严重）/ DEF-004（严重） |
| SP-01 冲突并发 | TC-SP-001~004（N 路并发 × 3 轮 + 边界对） | `scripts/sp01_conflict_concurrency.mjs` | **8/20**——R33/R55 拦截不达标（P0 出口准则 5 红线） | DEF-005~008（致命 ×4） |
| C10 认证授权 | TC-C10-001~010 | `scripts/c10_auth_tests.mjs` | **10/10**（004/009/010 按 DEF-009/010 登记口径） | DEF-009（严重）/ DEF-010（严重） |
| 状态机否定矩阵 | TC-C4-011（50 格）+ TC-C12-013 + TC-C7-010 + TC-C2-012 + TC-C6-011 + TC-C3-003 | `scripts/statemachine_negative_matrix.mjs` + 独立补验 | **83+ 通过，7 格缺陷/构造受限**（C2-012 两格与 C12 终态格经独立补验通过） | DEF-011（一般）/ DEF-012（严重）/ DEF-013（一般） |

P0 阶段小结：执行约 160 个断言点，通过率约 92%；当时登记缺陷 13 项。
**出口准则红线预警**：R33/R55 冲突拦截率 100% 为出口必要条件（03 计划 §5
第 5 条），DEF-005~008 不修复不得出口；修复后 SP-01 全量回归。

### 1.2 功能域执行结果（C1~C4，2026-09-10）

| 域 | 用例 | 结果 | 新增缺陷 |
|----|------|------|---------|
| C1 社区基础信息 | TC-C1-001~026 | **26/26**（001/023 日志=DEF-014；022 时段保护=DEF-015） | DEF-014（严重：AOP 未实现全域零留痕）、DEF-015（一般） |
| C2 居民与居住关系 | TC-C2-001~020 | **19/19**（006 代建有手写日志；013 居民发起搬出无端点按实现口径记录；018 工单放行=DEF-001） | —（复用 DEF-001/010/014） |
| C3 租住管理 | TC-C3-001~016 | **16/16**（003 跳级=DEF-013；005 窗口硬编码按现状记录；008/009 归 SP-03；010 重发无端点=DEF-016；012 PUT 放行=DEF-017） | DEF-016（一般）、DEF-017（一般） |
| C4 服务申请与工单 | TC-C4-001~022 | **22/22**（008 驳回无通知=DEF-020；011/012/014/016 同态重复=DEF-011 升级严重；015 退回无端点=DEF-019；002 停用类别树=DEF-018） | DEF-018（轻微）、DEF-019（一般）、DEF-020（一般）；DEF-011 升级为严重 |

C1~C4 小结：83 用例全数执行、当前口径全过；缺陷累计 20 项
（致命 4 / 严重 7 / 一般 8 / 轻微 1）。继续 C5~C12。

### 1.3 功能域执行结果（C5~C8，2026-09-10）

| 域 | 用例 | 结果 | 新增缺陷 |
|----|------|------|---------|
| C5 公告广播 | TC-C5-001~014 | **14/14**（003 communityId 越权=DEF-004；005 范围外可见=DEF-003；006 置顶链路 V9 已通；008 过期下线+管理归档可查；013 viewers 读路径=DEF-003 同源） | DEF-021（严重：endTime/publishTime 时区偏移 8h） |
| C6 居民反馈 | TC-C6-001~013 | **13/13**（004/007 WS 层归 SP-04；010 办结禁增附件=BE-ISSUE-9 回归通过；011 状态机矩阵引用） | —（口径记录：新反馈管理员提醒缺失，sendMessage 链路正常——预判清单项按口径记录） |
| C7 资源预约 | TC-C7-001~015 | **15/15**（002 时段外/跨间隙/非开放日拒；004 同日限约=DEF-002；005 同时段容量拒；006 半重叠=DEF-006；007 首尾相接放行；013 违约超限自动冻结+拒登） | —（复用 DEF-002/005/006） |
| C8 服务评价 | TC-C8-001~011 | **11/11**（009 统计与 SQL 一致；006/007/008 不满意跟进闭环全过） | DEF-022（一般：R39 双维度聚合接口缺失） |

C5~C8 小结：53 用例全数执行、当前口径全过；缺陷累计 22 项
（致命 4 / 严重 8 / 一般 9 / 轻微 1）。继续 C9~C12。

### 1.4 功能域执行结果（C9~C12 + C10 剩余，2026-09-10）

| 域 | 用例 | 结果 | 新增缺陷 |
|----|------|------|---------|
| C9 运营统计（接口侧） | TC-C9-001~011 | **11/11**（001/009/010 UI 卡片/图表归 B 轨；003 时间筛选=接口无参数记口径；005~008 全部指标与 SQL 一致含软删除口径；011 E9 四指标一致） | — |
| C10 剩余 P1 | TC-C10-011~022 | **12/12**（011 E10b 全链含重复绑 5002/BCrypt；017~020 日志=DEF-014；016 改密跨秒规避 DEF-009 验证通过） | —（复用 DEF-009/014；口径记录：sys-users 详情读对 ADMIN 放行） |
| C11 消息通知（接口侧） | TC-C11-001~005 + 006~011 归 SP-04 | **11/11**（001 事件覆盖 5 类 source；003 补拉与库一致；004 渠道分级 D2 读写+权限；005 已读管理） | —（租期到期通知待 SP-03；驳回缺通知=DEF-020） |
| C12 房源与看房（接口侧） | TC-C12-001~007 | **7/7**（002 下架 PATCH status 正确端点验通；003 D5 筛选含无效值 400；005 浏览计数 Redis 缓冲口径验证；007 数据级隔离） | DEF-023（一般：看房流转全链无通知） |

### 1.5 第一轮 A 轨阶段总结（2026-09-10）

**已执行**（本次会话，A 轨主体）：
- 冒烟 11/11 + SP-02 权限矩阵 84/84 + SP-01 冲突并发（4 用例暴露 4 缺陷）
  + C10 认证授权 10/10 + 状态机否定矩阵（83+ 格）+ C1~C12 功能域全量
  （26/19/16/22/14/13/15/11/11/12/11/7 = 177 用例）；
- 合计约 **300+ 断言点**，A 轨功能与权限面全部覆盖；
- **缺陷 23 项**全部登记 08：致命 4（DEF-005~008 冲突并发/重叠缺失——
  **出口准则 5 红线**）/ 严重 8（DEF-003/004/009/010/011/012/014/021）/
  一般 10 / 轻微 1；全部「打开」待步骤 8 修复窗口。

**未执行**（移交下一会话，见 11 §2 分轨与 §3 交接）：
- SP-03 定时任务长跑：后端进程持续运行中（cron 01:00/01:30/每小时/每 5 分钟
  任务自动触发），sys_task_log 已有 HousingViewFlushTask 与公告下线任务执行
  记录；跨日观察（租期判定/到期提醒/日志清理 TR-C10-07 含阈值调低验证后
  **必须恢复 730**）待下一会话收口；
- SP-04 WebSocket 专项（双端 WS 计时/订阅鉴权矩阵/断线重连）——需浏览器
  双会话或 STOMP 客户端脚本；
- SP-05 上传细节矩阵 / SP-06 BE-ISSUE 回归补项（工单附件四角色已在
  TC-C4-018 验过，BE-ISSUE-8/9/10 主链路均回归通过）；
- NF-01~11 JMeter 压测（与 Kimi 工作区错开时段）+ NF-14/15 冷启动；
- B 轨 9 条（用户手动：兼容性巡检/看板 16 卡片/四图表/Redis 降级等——
  05 文档 §1 有清单）与 C 轨（UI 改版合入后）。

**红线预警（重复强调）**：R33/R55 冲突拦截率 100%（03 计划 §5 出口准则 5）
当前不达标（DEF-005~008），修复并回归通过前不得出口。

脚本产物（全部在 `scripts/`，步骤 12 归档基线）：smoke_test / sp02_permission_matrix /
sp01_conflict_concurrency / c10_auth_tests / statemachine_negative_matrix /
c01~c09（社区/居民/租住/工单/公告/反馈/预约/评价/统计）/ c10c_c11_c12。

脚本产物（步骤 12 归档基线，持续追加）：`smoke_test.mjs`、
`sp02_permission_matrix.mjs`、`sp01_conflict_concurrency.mjs`、
`c10_auth_tests.mjs`、`statemachine_negative_matrix.mjs`、
`c01_community_tests.mjs`、`c02_resident_tests.mjs`、
`c03_lease_tests.mjs`、`c04_workorder_tests.mjs`。

### 1.6 SP-03/04/05 专项收口（2026-09-11，基线 8595609）

**环境说明（重要，影响证据口径）**：主工作区源码被修复会话占用（DEF-005~008
修复进行中，编译中间态），且 8080 端口被其 dev 实例占用——本段全部结论在
**git worktree `D:/crsp-50-baseline`（commit 8595609，与第一轮主体执行同一
基线）** 的后端实例上取得，端口 **8081**，数据库仍为 community_residence_test
（连接核验无误，未触碰 dev 库）。脚本以 `TEST_BASE=http://localhost:8081` 运行。

**SP-04 WebSocket 专项（TC-SP-017~021，8/8 全过，终态）**——
脚本 `scripts/sp04_websocket_tests.mjs`（原生 SockJS+STOMP 客户端，Node 24
全局 WebSocket，无第三方依赖）：
- TC-SP-017 握手认证：无令牌/伪令牌/过期令牌（dev 密钥铸 exp-1h 变体）/
  黑名单令牌（登出后复用）四类 CONNECT 全拒（ERROR 帧+关连接），有效令牌
  CONNECTED + /user/queue/notifications 订阅成功；
- TC-SP-018 订阅鉴权矩阵 6/6：提交人/绑定社区 ADMIN/超管订阅成功，非提交人
  居民/STAFF/跨社区 ADMIN 拒；推送仅达授权者且载荷 type=FEEDBACK_MESSAGE；
  admin1 解绑后**既有连接**订阅同社区新反馈即时被拒（绑定不缓存，验后已重绑
  还原并核实库绑定行=1）；
- TC-SP-019 双通道时延 22 样本：通知通道 12（反馈回复双向 ×10 + 工单派单 ×2）
  max 41ms；会话通道 10（双向各 5）max 50ms——全部远优于 ≤3s 判据（N3/R30/R49）；
- TC-SP-020 断线重连：杀连接→新连接 CONNECTED→原订阅自动恢复（反馈 topic +
  通知队列）→重连后消息 20ms 到达；前端退避策略静态核对（websocket.ts 2s→×2→
  上限 30s、notification.ts 30s 轮询兜底常量在位）——浏览器侧断网观察归 B/C 轨；
- TC-SP-021 上线补拉：离线 3 类事件（反馈回复/工单派单/公告发布）后
  `GET /notifications/pull?sinceSeq=` 3 条全达、seq 全新、无重复、升序；
  已读标记后重复补拉 isRead=1 不重推未读；在线 WS 推送 2/2 到达后游标推进
  无冗余（登录/重连两路径语义一致，R50）。

**SP-05 上传边界（TC-SP-023/024/026/027/028，3/4 通过 + 1 缺陷）**——
脚本 `scripts/sp05_upload_tests.mjs`：
- TC-SP-023 图片三重校验 ✅：jpg/jpeg/png/gif 四格式放行（真实魔数头样本）；
  6MB 超限/webp/bmp/.pdf-as-image/非法 type 全 400 拒；未登录 401；type 缺省
  =IMAGE（Controller defaultValue）；失败样本零落盘（磁盘计数核对 +5 精确）；
- TC-SP-024 文档三重校验 ⚠️：pdf/doc/docx/txt 放行、.exe/.jpg-as-document
  400 拒、失败零落盘（+4 精确）；**缺陷**：11MB 超限文档返回 500「系统内部
  错误」——multipart `max-file-size=10MB` 与业务上限相同，>10MB 文件在
  multipart 层被 MaxUploadSizeExceededException 拦截且全局异常处理器无该异常
  映射，业务层「文档不能超过 10MB」400 提示永不可达（功能上已拒绝、不落盘，
  错误契约破损）→ **DEF-024**（修复建议：GlobalExceptionHandler 增补
  MaxUploadSizeExceededException 映射）；
- TC-SP-026 /uploads 静态访问 ✅：fileUrl 200 + Content-Type=image/png +
  字节完整；不存在 404；路径穿越（`/uploads/../application.yml` 与 %2e%2e
  编码变体）均拒且无配置文件内容泄露；未登录 200（与 SecurityConfig
  permitAll("/uploads/**") 公开口径一致）；
- TC-SP-027/028 附件权限矩阵 ✅（口径引用）：四角色矩阵与办结禁增删已在
  C4-018/C6-013 域套件全绿（BE-ISSUE-9/10 回归），本段仅复核附件列表端点
  可用且返回真实数组。

**SP-03 定时任务收口（TC-SP-015/016，跨日观察完成）**：
- sys_task_log 跨日汇总（09-10 启动跨 09-11）：LeaseStatusTask 01:00 执行
  1 次（processed=1006，批量数据租约全量判定）、NoticeAutoOfflineTask 每小时
  5 次（09-10 22:00 下线 6 条过期公告、其余 0 条幂等）、HousingViewFlushTask
  每 5 分钟 56 次全 SUCCESS（跨日无中断）；无同调度点重复执行（防重入）、
  无 FAILED 记录；LeaseExpiryReminderTask 01:30 无记录——01:30 调度点处于
  会话中断窗口（实例停机），环境因素非缺陷，第二轮挂机补观察；
- TC-SP-016 日志留存清理（阈值调低验证，等效收口）：本机过夜睡眠等不到
  次日 03:00，采用直调任务 bean 的集成验证（worktree 一次性测试类，验后
  已删）——真实链路全通：阈值 PUT 调低为 1 即时生效（读回=1）→ 任务执行
  「待删 1 条，实际清理 1 条」（SQL 直插 3 天前样本）→ 未到期日志 13 条
  不受影响（14→13）→ 超管通知落库（「操作日志留存清理完成…清理 1 条…
  阈值 1 天」）→ sys_task_log 写入 SUCCESS/processed=1/duration=273ms；
  **强制收尾**：阈值已恢复 730（接口读回 + sys_config 库行双确认），
  样本零残留，R58 判据满足；
- TC-SP-022 Redis 不可用降级：用户执行场景，归 B 轨（05 文档 §1 清单）。

**本轮新增缺陷**：DEF-024（一般，文档超限错误契约）。累计 24 项。

**A 轨剩余（第二轮执行）**：NF-01~11 压测（**不在基线 8595609 旧构建上跑，
出口证据须来自 DEF 修复合入后的最终版本**）+ NF-14/15 冷启动 + DEF-001~024
回归 + LeaseExpiryReminderTask 挂机补观察。

## 2. 第二轮执行（回归 + 补测，同上结构）

## 3. 各轮次汇总统计（对照 03 计划出口准则的达成走势）
