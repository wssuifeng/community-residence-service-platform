# 测试执行入口（新会话交接 + 用例分轨）

> 本文档是 50 阶段步骤 6/7 的**执行总入口**：新测试会话从这里开工，
> 按分轨表执行与回写。文档布局纪律补充（_中控 §2 映射表登记）：本文
> 为 11 号位专用入口文档（新会话上下文管理需要，非一次性过程文档——
> 执行结论回写 07/08，本文只维护入口信息与分轨表，不记录执行细节）。

## 1. 新会话开工清单（按序执行）

1. 读 `docs/00_总控.md` → `docs/50_系统测试/_中控.md`（确认当前步骤
   指针）→ `AGENTS.md`（种子账号/启动方式/纪律）；
2. 读本文 §2 分轨表 + `03_系统测试计划.md` §2/§5（轮次与准则）；
3. 读 `05_测试环境与数据.md`（环境/库/切库方式）与 `scripts/README.md`
   （脚本执行顺序）；
4. **步骤 6 冒烟前置（一次性）**：测试库重建（脏库警告见 05 §2：
   执行 `03_cleanup.sql` 口径 A → `00_create_test_db.sql` → 启动后端
   指向测试库让 Flyway 全新跑 V1~V9 → `01_seed_extension.sql` →
   按需 `02_bulk_data.sql` → Redis seq 同步，见 scripts/README）；
   注意：**Kimi 并行工作区共用 MySQL/Redis 实例，切库用环境变量
   SPRING_DATASOURCE_URL，绝不写开发库 community_residence**；
   启动后端前确认 8080 端口空闲（Kimi 工作区已改用其他端口）；
5. 执行步骤 6 冒烟（06 文档范围）→ 通过后按分轨表执行第一轮；
6. 执行结论回写：逐用例结果→`02 RTM` 执行结果列 + `07` 轮次汇总；
   缺陷→`08` 登记 DEF-NNN；**不要**把执行细节写进本文档。

## 2. 用例分轨表（268 TC 三轨）

> 轨道划分依据：自动化会话（新测试会话）执行 A 轨；用户手动执行 B 轨；
> UI 改版（Kimi 批次）合入后执行 C 轨。B/C 轨合计约 25 条，其余全在 A 轨。

### A 轨 · 自动化会话执行（约 243 条，第一轮主体）

纯后端接口 + 数据库断言 + WS 协议层 + JMeter + Playwright(Edge) 自动化。
执行顺序建议（按 03 计划 §2.3）：
1. **P0 先行**：SP-02 权限矩阵（76 组合）→ SP-01 冲突并发 →
   TC-C10-001~010 认证与数据授权 → 各模块状态机非法流转否定矩阵；
2. 各功能域正向/异常/边界（C1→C12 顺序）；
3. SP-03 定时任务长跑（**第一轮启动即挂后台跨日观察**，含 SP-016
   日志清理任务——阈值短时调低验证后**必须恢复 730**）；
4. SP-04 WebSocket / SP-05 上传 / SP-06 缺陷回归；
5. NF-01~11（JMeter 压测单独时段，与 Kimi 工作区错开）；
6. NF-14/15 冷启动（用独立库口径，勿动测试现场）。

### B 轨 · 用户手动执行（约 9 条）

| 用例 | 内容 | 备注 |
|------|------|------|
| TC-NF-013 | 兼容性手动巡检（Q3 裁决：以手头浏览器为准） | 05 文档 §1 有页面清单与检查点 |
| TC-SP-022 | Redis 不可用降级（停 Redis 验证轮询兜底，验后恢复） | 涉及停共用实例，用户执行更稳 |
| TC-C9-001 | 运营看板 16 卡片逐一断言（断言面为 UI 卡片清单） | 与 C 轨可同批 |
| TC-C9-009/010 | 四图表渲染 + 导出图片 | UI 依赖 |
| TC-C9-011 | E9 答辩演示口径全流程 | UI 依赖 |
| TC-SP-025 | 文件上传前端侧校验（before-upload 拦截） | UI 依赖 |
| TC-NF-005 | 消息 ≤3s（可选自动化，计时含渲染时刻则归 UI） | 优先自动化协议层 |
| 待补 | Kimi UI 改版后视觉回归抽查 | 改版合入后由用户验收 |

（用户执行时结论同样回写 02 RTM / 07；DEF 照常登记 08）

### C 轨 · UI 改版合入后执行（约 16 条，第二轮一部分）

**前置**：Kimi UI 改版批次合入 main 且 Playwright 脚本适配新页面结构。
- 依赖浏览器界面交互的用例：反馈会话 UI 双端（TC-C6-004 计时含 DOM
  渲染时）、工单附件前端链路（TC-C4-021）、渠道勾选前端入口
  （TC-C11-013~015 的页面侧）、代建/批量导入前端表单（TC-C2-006/007
  页面侧）、公告定向/置顶前端（TC-C5-005/006 页面侧）、房源筛选前端
  （TC-C12-004 页面侧）——这些用例的**接口侧断言已在 A 轨覆盖**，
  C 轨只补页面交互与展示层；
- 全量视觉/布局回归抽查（用户验收口径，并入 B 轨末项）。

### C/D 轨 · 第五轮局部回归新增范围（深度审查前置，2026-09-16）

> 本节只登记执行入口，不把静态审查写成已通过。`DEF-045～DEF-068` 需按证据等级执行；已有 `DEF-034～036/037～041/044` 仍按第五轮原待办复验。

| 分组 | DEF | 局部回归范围与最小断言 |
|---|---|---|
| 公告范围/通知 | 045/056/057/064 | 仅 BUILDING 目标发布通知；匿名列表/详情、communityId 筛选、居民跨社区可见性；viewers/recordView 越权；同用户并发回执幂等与 viewCount |
| 认证与私有业务 | 058/059/060 | 居民 A/B、无关系居民、非派单 STAFF、跨社区 ADMIN 对评价、反馈、资源预约的读写矩阵；DB 零越权行 |
| 看房与公开入口 | 061/067 | 匿名预约详情脱敏/核验、STAFF 取消拒绝、ADMIN 处置、游客单 null 边界；available-slots 反向/超大日期范围限制 |
| 配置/契约 | 047/048/049/053/054 | 未知配置 key、空配置值、敏感值脱敏；CSV 空输入 4xx；配置开关行为差分；status 参数契约；代建账号与入住关系两步页面提示 |
| 预约边界 | 050/066 | 过去/最大提前天数/跨日/结束早于开始；迁出日期早于入住、今天、未来时关系/租约联动一致 |
| 部署/通道 | 051/052/063/068 | 非白名单 Origin、附件跨账号读取、Redis seq 故障恢复/并发、生产 profile 默认密钥启动保护 |
| 公开字段与前端真实性 | 046/055/065 | 游客公开字段矩阵；渠道/代建/公告/房源 C 轨页面操作；服务人员端不再显示取模伪造属性 |
| 关系分页 | 062 | N7 或等价数据下 ADMIN 绑定社区分页 total/页码/记录与数据库全集一致 |

**执行纪律**：当前构建限制为 Maven Wrapper 依赖下载失败、`npm ci` 输出 `Exit handler never called!`，且前端无自动化测试；因此 A 轨可执行项需先恢复独立后端/数据库/Redis，C 轨必须留浏览器截图和 Network/DB 证据，所有新增条目完成后回写 `08`，不得在本入口文档填“通过”。

## 3. 交接快照（2026-09-14 第三/四轮后更新）

- 被测基线：第二轮 `3e27307`；**第三/四轮 `e807b5b`**（= 第三批修复 0a1935b 合入，
  单测 273/273；第三批专项 + 全量回归均在此基线执行）；
- **第三轮（2026-09-14 第三批专项）已收官**：r3_slotgrid 28/28（Slot Grid 全断言
  无缺陷，V10 降级/V11 用户级约束/分桶口径三偏离点实证无问题）+ r3_batch3 14/14
  （DEF-025~030 六项修复回归关闭；DEF-031 关闭）；Flyway V11/V12 真实 MySQL 验证
  随纯净重建闭环（08 §3.9）；
- **第四轮（2026-09-14 全量回归·纯净基线）已收官**：测试库口径 A 重建（V1~V12 +
  01 种子 + 02 N7 + Redis seq=2004）；A 轨 16 套件 + r2 19/19 + r3 复跑全绿；
  SP-01 20/20 + SP-04 8/8 + SP-05 4/4；NF 压测 50843 样本 7 接口 100%、
  P95 16~620ms（N1/N2/N7 达标）；C 轨 16 条 UI 用例 9 过 6 失败（08 §3.10）；
- **当前打开缺陷 10 项**（08 §2）：严重 DEF-034（CREATE 类操作日志 #result 未绑定，
  **出口阻塞项**）；后端一般 DEF-032/033/035/036/041；前端一般 DEF-037~040
  （后端能力已交付的前端入口缺失）；轻微→一般 DEF-041 含 DEF-018 延伸——
  第四批修复范围建议见 08 §3.10 结论；
- 剩余动作：第四批缺陷修复（用户裁决范围）→ 第五轮局部回归（10 项 + 受影响套件
  抽样）→ 步骤 9 结果分析 → 步骤 10 测试报告 → 步骤 11 评审 → 步骤 12 收尾；
  B 轨 9 条用户手动（执行窗口开放，不阻塞出口评估）；压测机端口耗尽（无节流
  1557/s 时）与 JMeter JDK 解析问题已按 500ms pacing + JDK17 直启处置，口径
  说明入报告；
- 环境现状：8081 测试实例 + 5173 前端 dev（Vite proxy→8081）由测试会话管理；
  8080 曾为 Kimi 工作区 dev 实例（本轮期间自行消失，未触碰）；测试库含第四轮
  过程数据 + 社区 2「R4-库存楼」20 套空置房（防套件数据耗尽）；脚本资产新增：
  r3_slotgrid.mjs / r3_batch3.mjs / track_c_ui.py（C 轨适配版）+ c_shots/ 截图 17 张；
- 已知执行期裁决口径（沿用第二轮）：D3 服务人员账号归超管；R26 下线=WITHDRAWN；
  R14 到期判定=查询期；上传校验=扩展名白名单+大小；/uploads 公开访问；
  本轮新增：days 参数钳位（0/91→1/90）为实现口径非缺陷；批量创建坏行整批 400
  =DEF-032；isPinned 为 Integer 契约（boolean 序列化 400）。
