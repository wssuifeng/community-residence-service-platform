# 功能用例 · C7 公共资源预约管理

> 依据：01_测试需求分析.md §3.7（TR-C7-01~06 + 第三批新增提案 TR-C7-07 Slot Grid 栅格化，2026-09-14，待 01 回填）、需求规格 R32~R36 + E7、接口设计 9.7、架构设计 §6.3（资源预约 6 态状态机）。
> 编号规则：TC-C7-NNN；每条至少覆盖一个 TR；执行结果列由步骤 7 回填。
> 口径说明：并发冲突专项（N 路并发提交）在「非功能与专项用例」文件的 TR-SP-01 设计，本文件仅覆盖单线程的时段外拒绝、冲突拒绝与首尾相接/半重叠边界，不重复设计；TC-C7-022/023（容量并发/唯一约束并发）为 Slot Grid 第三批新增的容量与用户级防重专项，虽为并发类型但挂 TR-C7-07 在本文件执行（脚本 r3_slotgrid.mjs，锁模式与 SP-001 同型）。
> 状态枚举（权威口径：Flyway 迁移脚本注释 + 架构设计 §6.3）：PENDING 待审核 / RESERVED 已预约 / COMPLETED 已完成 / REJECTED 已拒绝 / CANCELLED 已取消 / VIOLATED 已违约。
> 种子数据：V6 健身房（GYM，容量 10）时段模板为周一至周五 09:00-12:00、14:00-18:00；居民 resident1/Resident123456（已入住）、社区管理员 admin1/Admin123456、超管 superadmin/Admin@123456。

### TC-C7-001 时段内预约申请正常提交
- 关联：TR-C7-01　类型：功能　优先级：P1
- 前置：后端 8080 / 前端 5173 已启动；居民 resident1 已登录且居住关系有效（已入住）；种子健身房资源与周一至周五时段模板存在。
- 数据：资源=健身房（V6 种子）；reserveDate=下一个工作日（周一至周五，避开周末）；startTime=09:00、endTime=10:00（完全落在 09:00-12:00 模板内）；purpose=晨练；contactPhone=13800000001。
- 步骤：
  1. 居民端进入「资源预约」页，选择健身房与目标日期；
  2. 调用 GET /api/v1/resources/{resourceId}/available-slots?startDate=…&endDate=… 查看可约时段；
  3. 调用 POST /api/v1/resource-reservations 提交预约（resourceId/reserveDate/startTime/endTime/purpose/contactPhone）；
  4. 调用 GET /api/v1/resource-reservations/{id} 查询该预约详情。
- 预期：
  1. 可约时段列表按日期+开始时间升序返回，目标日期含 09:00-12:00、14:00-18:00 两个 AVAILABLE 时段及容量/占用计数；
  2. 提交返回 code=200，预约落库且状态为「待审核」（PENDING）；
  3. 详情返回的资源、日期、时段、用途、联系电话与提交一致；
  4. 「时段须落在该资源可预约时段配置内」满足时提交后状态「待审核」（R32 验收标准）。

### TC-C7-002 时段外预约请求被拒并提示可预约范围
- 关联：TR-C7-01　类型：异常与边界　优先级：P1
- 前置：同 TC-C7-001；已获取健身房时段模板（周一至周五 09:00-12:00、14:00-18:00）。
- 数据：三组非法请求——A：startTime=08:00、endTime=09:00（早于模板）；B：startTime=11:00、endTime=14:00（跨越 12:00-14:00 模板间隙）；C：reserveDate=下一个周六或周日（无可约模板）。
- 步骤：
  1. 以请求 A 调用 POST /api/v1/resource-reservations；
  2. 以请求 B 重复提交；
  3. 以请求 C 重复提交；
  4. 每次失败后调用 available-slots 核对系统返回的可用时段信息。
- 预期：
  1. 请求 A 被拒绝（业务错误，非 500），提示时间不在资源可预约时段内；
  2. 请求 B 被拒绝（跨模板间隙视为时段外）；
  3. 请求 C 被拒绝（非开放日无模板）；
  4. 「时段外请求被拒绝并提示；提交后状态『待审核』」——仅时段内请求进入待审核，三组请求均未产生预约记录（R32 验收标准；时段外提交不入状态机，架构 §6.3 口径）。

### TC-C7-003 起止时间非法与必填校验
- 关联：TR-C7-01　类型：异常与边界　优先级：P1
- 前置：同 TC-C7-001。
- 数据：endTime 早于 startTime（如 10:00-09:00）；endTime 等于 startTime（09:00-09:00）；缺少 resourceId 或 reserveDate。
- 步骤：
  1. 提交 endTime < startTime 的预约；
  2. 提交 endTime = startTime 的预约；
  3. 分别缺省 resourceId、reserveDate 提交。
- 预期：
  1. 步骤 1、2 返回参数校验错误，提示结束时间必须晚于开始时间；
  2. 步骤 3 返回 @NotNull 校验错误（400 类业务错误码）；
  3. 三种请求均未产生预约记录。

### TC-C7-004 同一居民同资源同日重复预约被拒（单线程）
- 关联：TR-C7-02　类型：异常与边界　优先级：P0
- 前置：resident1 已对健身房在某工作日成功提交一条「待审核/已预约」状态的预约（可复用 TC-C7-001 产物）。
- 数据：同居民、同资源、同日期、另一时段（如 14:00-15:00）。
- 步骤：
  1. resident1 对同一资源同一日期再次调用 POST /api/v1/resource-reservations（不同时段）；
  2. resident1 改约另一日期提交，验证仅同日受限。
- 预期：
  1. 第一次重复提交被拒绝，提示同一天已预约该资源，不可重复预约；
  2. 另一日期的预约正常受理（状态待审核）；
  3. 库中该居民该资源该日期的有效预约（待审核/已预约）仅 1 条。

### TC-C7-005 容量满的同时段预约被拒（单线程冲突拒绝）
- 关联：TR-C7-02　类型：异常与边界　优先级：P0
- 前置：容量 1 的测试资源（如「会议室」）及时段模板已由管理员配置；两个测试居民账号 A、B 均已入住该社区。
- 数据：依赖：05 测试环境与数据 · 新建容量=1 的公共资源与时段模板 + 第二个已入住测试居民账号 B（resident1 可作 A）。
- 步骤：
  1. 居民 A 对容量 1 资源提交某日 09:00-10:00 预约并经管理员确认（已预约）；
  2. 调用 available-slots 查看该时段状态；
  3. 居民 B 对同资源同日同时段（09:00-10:00）提交预约。
- 预期：
  1. 步骤 2 中该时段占用计数达容量上限，状态标记为 FULL；
  2. 居民 B 的提交被拒绝（预约冲突类错误码），提示该时段预约已满；
  3. 「同一资源同一时段的重叠预约 100% 拦截」——单线程场景下库中该时段有效预约数不超过容量（R33；并发场景归 TR-SP-01）。

### TC-C7-006 半重叠时段预约拦截（单线程边界）
- 关联：TR-C7-02　类型：异常与边界　优先级：P0
- 前置：同 TC-C7-005（容量 1 资源）；居民 A 已成功预约某日 09:00-10:00（已预约状态）。
- 数据：居民 B 请求同资源同日 09:30-10:30（与 A 的时段半重叠）；对照组：同资源同日 10:30-11:00（不重叠，落在模板内）。
- 步骤：
  1. 居民 B 提交 09:30-10:30 预约；
  2. 居民 B 提交对照请求 10:30-11:00。
- 预期：
  1. 半重叠请求被拒绝（R33：重叠预约 100% 拦截；若实现按起止时刻精确匹配计数而放行半重叠，记缺陷）；
  2. 对照请求（与既有预约无时间重叠）正常受理为待审核；
  3. 库中无与 A 时段重叠的有效预约。

### TC-C7-007 首尾相接时段预约放行（单线程边界）
- 关联：TR-C7-02　类型：异常与边界　优先级：P0
- 前置：容量 1 资源（同 TC-C7-005）；居民 A 已预约某日 09:00-10:00；居民 B 账号可用。
- 数据：居民 B 请求同资源同日 10:00-11:00（与 A 预约首尾相接、不重叠）。
- 步骤：
  1. 居民 B 提交 10:00-11:00 预约；
  2. 管理员确认 B 的预约；
  3. 查询该资源当日预约列表。
- 预期：
  1. 首尾相接（结束时刻=下一预约开始时刻，区间不重叠）请求正常受理，不被误判为冲突；
  2. 确认成功，B 预约状态为已预约；
  3. 当日两条预约（A 09:00-10:00、B 10:00-11:00）并存且均有效。

### TC-C7-008 审核通过与拒绝流转及通知（R34）
- 关联：TR-C7-03　类型：功能　优先级：P1
- 前置：存在两条居民提交的待审核预约（PENDING）；管理员 admin1 已登录（绑定该社区）。
- 数据：预约 1（正常用途）；预约 2（用途可疑/时段冲突），拒绝理由=「时段安排冲突，请改约」。
- 步骤：
  1. 管理员调用 PATCH /api/v1/resource-reservations/{id1}/confirm 审核通过预约 1；
  2. 管理员调用 PATCH /api/v1/resource-reservations/{id2}/reject，不填 reason 提交；
  3. 补填 reason 后重新提交拒绝；
  4. 居民端登录被预约居民账号，查看通知中心与本人预约列表。
- 预期：
  1. 预约 1 状态流转「待审核→已预约」，确认操作留痕（操作日志可见）；
  2. 缺省 reason 的拒绝请求被参数校验拒绝（拒绝须填理由）；
  3. 预约 2 状态流转「待审核→已拒绝」，理由留存于预约记录；
  4. 「拒绝/取消通知对方」——两位居民均收到对应站内通知（确认通知/未通过通知，含日期与理由），通知可溯源到预约单；
  5. 全状态流转留痕：两次流转均可在操作日志检索到（操作人/时间/对象）。

### TC-C7-009 用户取消预约（待审核/已预约两态）与容量释放
- 关联：TR-C7-03　类型：功能　优先级：P1
- 前置：居民 A 名下有一条待审核预约（PENDING）、一条已预约预约（RESERVED，容量 1 资源，占用计数已 +1）；居民 B 账号可用。
- 数据：取消理由=「临时有事」。
- 步骤：
  1. 居民 A 调用 PATCH /api/v1/resource-reservations/{id}/cancel 取消待审核预约；
  2. 居民 A 取消已预约预约（RESERVED）；
  3. 居民 B 对容量 1 资源同日同时段提交预约；
  4. 居民 B 尝试取消居民 A 名下另一条预约（越权对照）。
- 预期：
  1. 待审核预约流转「待审核→已取消」，理由留存；
  2. 已预约预约流转「已预约→已取消」，同时段占用计数释放；
  3. 释放后居民 B 可成功预约该时段（容量回位）；
  4. 「用户可取消（→已取消）」仅限本人：居民 B 取消他人预约被拒绝（403），A 的预约状态不变；
  5. 取消操作通知社区管理员（R34：取消通知对方），管理员通知中心可见且可溯源。

### TC-C7-010 非法状态流转拒绝
- 关联：TR-C7-03　类型：异常与边界　优先级：P1
- 前置：构造覆盖 6 态的预约记录各一条：待审核/已预约/已完成/已拒绝/已取消/已违约。
- 数据：依赖：05 测试环境与数据 · 预置或通过操作产生 6 种状态的预约各一条（已完成/已违约可由 TC-C7-011/012 产物复用）。
- 步骤：
  1. 对已拒绝预约调用 confirm（已拒绝→已预约）；
  2. 对已取消预约调用 reject（已取消→已拒绝）；
  3. 对已完成预约调用 cancel（已完成→已取消）；
  4. 对待审核预约调用 violate（待审核→已违约，跳过已预约）；
  5. 对已违约预约调用 complete（已违约→已完成）。
- 预期：
  1. 步骤 1~5 全部返回状态流转非法错误（明确错误码与提示，如「预约状态不允许从 X 流转到 Y」）；
  2. 各预约状态保持不变，库中无非法流转痕迹；
  3. 合法流转全集（待审核→已预约/已拒绝/已取消；已预约→已完成/已取消/已违约）与架构 §6.3 状态机一致。

### TC-C7-011 预约到期登记已完成
- 关联：TR-C7-04　类型：功能　优先级：P1
- 前置：某居民存在一条已预约（RESERVED）且使用日期已过的预约；admin1 已登录。
- 数据：remark=「活动正常结束」。
- 步骤：
  1. 管理员调用 PATCH /api/v1/resource-reservations/{id}/complete 登记完成；
  2. 查询该预约状态与操作日志。
- 预期：
  1. 状态流转「已预约→已完成」；
  2. 完成登记留痕（操作人/时间/备注可查）；
  3. 该时段占用计数释放（available-slots 复核容量回位）。

### TC-C7-012 违约登记与违约记录可查
- 关联：TR-C7-04　类型：功能　优先级：P1
- 前置：居民 A 有一条已预约（RESERVED）预约；admin1 已登录；A 当前无违约记录。
- 数据：违约理由=「未按时到场，资源浪费」。
- 步骤：
  1. 管理员调用 PATCH /api/v1/resource-reservations/{id}/violate 标记违约；
  2. 调用 GET /api/v1/residents/{residentId}/violations 查询 A 的违约记录；
  3. 居民 A 登录查看通知中心。
- 预期：
  1. 状态流转「已预约→已违约」；
  2. 违约记录列表含该条记录（违约类型 RESERVATION_NO_SHOW、关联预约、处置说明、处理人、时间），「违约处置记录可查」（R35 验收标准）；
  3. 居民 A 收到违约处置站内通知，可溯源到该预约单；
  4. 未达违约上限（默认 3 次）时 A 的账号状态不变（仍 ACTIVE，可正常登录与预约）。

### TC-C7-013 违约超限自动冻结与限制生效
- 关联：TR-C7-04　类型：异常与边界　优先级：P1
- 前置：测试居民 A 已有 2 条违约记录（violation_record）；一条可标记违约的已预约预约；全局配置 violation 上限为默认 3 次。
- 数据：依赖：05 测试环境与数据 · 为测试居民 A 预置 2 条历史违约记录（SQL 直插 violation_record 或复用前两轮执行产物）。
- 步骤：
  1. 管理员对 A 的第三条预约执行 violate；
  2. 查询 A 的违约记录总数与账号状态（库内直查 resident.status）；
  3. A 重新登录系统；
  4. A 尝试提交新的资源预约。
- 预期：
  1. 第三次违约登记后违约计数达上限，A 的账号状态自动变更为 FROZEN（冻结），处置留痕（日志可见冻结原因与阈值）；
  2. 「被处置账号受相应限制」（R35 验收标准）：A 登录被拒或仅保留受限功能（冻结账号拒登口径），新预约提交被拒绝；
  3. 违约处置通知送达 A（或其登录前入库待补拉）；
  4. 违约记录列表 3 条齐全，处置全程留痕可查。

### TC-C7-014 预约记录查询与组合检索（R36）
- 关联：TR-C7-05　类型：统计核对　优先级：P2
- 前置：库内已有多社区、多资源、多状态的预约数据（种子 + 前序用例产物）；resident1 与 admin1 分别登录。
- 数据：组合条件——资源=健身房、状态=已预约、日期范围=本周；对照样本：另一社区的一条预约。
- 步骤：
  1. resident1 调用 GET /api/v1/resource-reservations 查询本人记录（分页）；
  2. resident1 尝试翻看他人预约详情（越权对照，取另一居民的预约 id 调 GET /{id}）；
  3. admin1 以 resourceId+status+startDate+endDate 组合检索；
  4. SQL 直查库：SELECT id,user_id,resource_id,reserve_date,status FROM resource_reservation 按同样条件统计，与接口结果比对。
- 预期：
  1. 居民端仅返回本人预约（含状态），分页与排序正常；
  2. resident1 查看他人预约被拒绝（数据级权限：限本人）；
  3. 管理员组合检索结果准确：仅含绑定社区内、满足全部条件的记录，「检索条件组合可用」；
  4. 「查询结果与预约数据一致」（R36 验收标准）：接口 records 的 total 与 SQL 直查计数 100% 相符，逐条核对无多漏。

### TC-C7-015 E7 全流程：预约→审核→使用→登记/违约→处置留痕
- 关联：TR-C7-06　类型：功能　优先级：P1
- 前置：独立测试居民账号（避免污染其他用例数据）；admin1、superadmin 可用；容量 1 测试资源已配置。
- 数据：主路径预约（正常使用）+ 分支路径预约（违约）；违约上限保持默认 3 次。
- 步骤：
  1. 居民选资源/日期/时段 → available-slots 确认可约 → 提交预约（待审核）；
  2. 管理员审核通过 → 居民收到确认通知（已预约）；
  3. 到期使用后管理员登记已完成；
  4. 同一居民再次预约另一时段 → 管理员标记违约 → 查询违约记录与通知；
  5. 全流程结束后核对操作日志：每步流转（提交/确认/完成/违约）均有留痕。
- 预期：
  1. 主路径：待审核→已预约→已完成，三步流转与通知（确认通知）齐全；
  2. 分支路径：待审核→已预约→已违约，违约记录入库、违约处置通知送达、留痕可查；
  3. 「选资源/时段→冲突校验→提交→审核通过（通知）→使用→登记完成/违约→处置留痕」全链路无断点（E7 验收口径）；
  4. 操作日志覆盖全部关键变更，操作人/时间/对象/内容完整。

### TC-C7-016 slot_unit 三粒度配置与非法值拒绝（Slot Grid）
- 关联：TR-C7-07（提案）　类型：功能/异常与边界　优先级：P2　执行：自动化 scripts/r3_slotgrid.mjs
- 前置：admin1 已登录；三个测试资源可创建（或现场创建后调整）。
- 数据：依赖：05 测试环境与数据 · Slot Grid 专用资源池（R-15/R-30/R-60 三资源 + 容量可调的测试资源，产物用后清理）。
- 步骤：
  1. `POST /api/v1/resources` 分别创建 slot_unit=15 / 30 / 60 的三个资源（capacity 任意合法值）；
  2. `GET /api/v1/resources/{id}` 核对三个资源的 slotUnit 回显；
  3. 创建资源不传 slotUnit，核对详情回显；
  4. `POST /api/v1/resources` 传 slotUnit=20 提交；传 slotUnit=10 与 slotUnit=90 分别提交；
  5. `PUT /api/v1/resources/{id}` 将某资源 slotUnit 从 30 改为 15，再改回不传（null）；
  6. 前端资源管理页（如已接入）核对 slotUnit 配置项可见。
- 预期：
  1. 三资源创建成功；
  2. ResourceVO 暴露 slotUnit 字段且值与请求一致（V11 列，08 §3.8）；
  3. 缺省 slotUnit=30（SlotGrids.DEFAULT_SLOT_UNIT，V11 列默认值）；
  4. 三组均 400 拒绝——@Min(15)/@Max(60) 拦 10/90，服务层枚举校验（ALLOWED_SLOT_UNITS={15,30,60}）拦 20，提示「预约最小单位取值 15/30/60」；
  5. 更新生效为 15；不传时**保留既有值**（更新不回退默认，08 §3.8「未传保留既有值」口径）；
  6. 配置项展示正常（UI 断言归 C 轨复核，脚本仅断言 API）。

### TC-C7-017 模板起止对齐校验正反例（Slot Grid 防线①源头）
- 关联：TR-C7-07（提案）　类型：异常与边界　优先级：P1　执行：自动化 scripts/r3_slotgrid.mjs
- 前置：admin1 已登录；slot_unit=30 资源 R-30、slot_unit=15 资源 R-15、slot_unit=60 资源 R-60 就绪（TC-C7-016 产物）；各资源已有/可创建周模板。
- 数据：六组模板请求（见步骤）。
- 步骤：
  1. 对 R-30 调 `POST /api/v1/resources/{R-30}/timeslots` 创建 10:00-11:30 模板（对齐）；
  2. 对 R-30 创建 10:10-11:00 模板（起点不对齐）；
  3. 对 R-30 创建 10:00-11:10 模板（止点不对齐）；
  4. 对 R-15 创建 10:00-10:15 模板（15 分钟粒度对齐）；
  5. 对 R-60 创建 10:30-11:30 模板（30 分钟对齐但 60 分钟粒度下不对齐）；
  6. `PUT /api/v1/timeslots/{id}` 将 R-30 某既有对齐模板改为 10:20 起（更新路径对齐校验）；
  7. SQL 直查（或经接口）确认存量非对齐模板（若测试库 V10 前遗留存在）仍可查询、未被清洗。
- 预期：
  1. 创建成功；
  2. 400 拒绝（提示「时段起止必须为预约最小单位（30 分钟）的整数倍」）——checkSlotAlignment 源头拦截；
  3. 400 拒绝（止点 11:10 非 30 倍数）；
  4. 创建成功（15 分钟粒度下 10:15 对齐）；
  5. 400 拒绝（60 分钟粒度下起点 10:30 不对齐）；
  6. 更新路径同样 400（create/update 双入口均校验，08 §3.8）；
  7. 存量非对齐模板保留不清洗（仅拦新写），可约计算走宽容分桶（08 §3.7 迁移与存量口径）。

### TC-C7-018 available-slots 栅格结构断言与库内计数一致（Slot Grid）
- 关联：TR-C7-07（提案）　类型：统计核对　优先级：P1　执行：自动化 scripts/r3_slotgrid.mjs
- 前置：slot_unit=30、capacity=2 的资源 R-T 就绪，配周一模板 09:00-12:00；三个已入住测试居民账号（resident1 + 账号池 2 个）。
- 数据：目标日期 D=下一个周一；两居民分别预约 D 的 09:00-09:30 与 09:30-10:00（状态 PENDING，占用口径）。
- 步骤：
  1. 调 `GET /api/v1/resources/{R-T}/available-slots?startDate=D&endDate=D`；
  2. 断言数组结构：按日期+startTime 升序；模板 09:00-12:00 展开为 6 个 30 分钟栅格（09:00-09:30 … 11:30-12:00）；
  3. 断言字段兼容口径：每格含 timeslotId（=模板ID×10000+当日分钟数，栅格标识非数据库主键）、date、startTime、endTime、maxBookings=capacity（2）、currentBookings、status（AVAILABLE/FULL）；
  4. 断言 booked 计数：09:00 格 currentBookings=1（预约 1 覆盖）、09:30 格 currentBookings=1（预约 2 覆盖）、其余格 0；remaining=maxBookings-currentBookings；
  5. SQL 直查该日该资源占用态（PENDING/RESERVED）预约明细，按「覆盖即占用」分桶（预约与栅格区间有交集即计入该格）重算每格 booked，与接口逐格比对；
  6. 将两预约改约同一格（如 09:00-09:30 两条并存，capacity=2 下合法），重查接口断言该格 currentBookings=2、status=FULL。
- 预期：
  1. 200 返回栅格数组（同日按 startTime 排序，V11 顺带修复的无序问题不回归）；
  2. 6 格粒度正确（slot_unit 拆分）；
  3. 字段结构符合 08 §3.8 实施记录（timeslotId 栅格标识口径为权威：模板ID×10000+当日分钟数，如模板 7 的 09:00 格 = 70000+540=70540）；
  4. 计数与构造数据一致、remaining 派生正确；
  5. 接口计数与 SQL 直查 100% 相符（「覆盖即占用」分桶口径，R36 数据一致性在栅格维度的延伸）；
  6. 满格 status=FULL（booked ≥ capacity 判定）。

### TC-C7-019 创建预约锁内逐时段容量边界（Slot Grid 防线②）
- 关联：TR-C7-07（提案）　类型：异常与边界　优先级：P0　执行：自动化 scripts/r3_slotgrid.mjs
- 前置：slot_unit=30、capacity=2 的资源 R-T（周一模板 09:00-12:00）；三个已入住测试居民账号 A/B/C。
- 数据：目标日期 D。
- 步骤：
  1. 居民 A、B 分别预约 D 09:00-09:30（同槽两人，capacity=2 内）；
  2. 居民 C 预约 D 09:00-09:30（第 3 人，超容量）；
  3. 居民 C 改约 D 09:30-10:00（相邻格未满）；
  4. 构造跨格请求：居民 C 预约 D 09:30-11:00（覆盖 09:30/10:00/10:30 三格，其中 10:00 格已由他人占满——先由 A、B 各占一条 10:00-10:30 预约）；
  5. available-slots 复核各格状态。
- 预期：
  1. A、B 均成功（同槽多人至 capacity 是 Slot Grid 模型的核心能力，V10 全局同槽唯一已降级，08 §3.8 口径偏离点 1）；
  2. C 被拒：业务码 5401（RESERVATION_CONFLICT，提示「时段 09:00~09:30 已约满」），零落库；
  3. C 成功（容量按格独立判定）；
  4. 跨格请求被拒：任一覆盖格满即拒（锁内逐格循环检查，提示指向首个满格），零落库；
  5. 09:00 格 FULL（2/2）、09:30 格 currentBookings=1、10:00 格 FULL、10:30 格视步骤 4 前状态——接口与库一致。

### TC-C7-020 存量非对齐预约宽松计入（Slot Grid 兼容口径）
- 关联：TR-C7-07（提案）　类型：异常与边界　优先级：P1　执行：自动化 scripts/r3_slotgrid.mjs
- 前置：slot_unit=30、capacity=2 的资源 R-T（周一模板 09:00-12:00）；目标日期 D。
- 数据：依赖：05 测试环境与数据 · SQL 直插一条非对齐占用态预约（resource_id=R-T、reserve_date=D、start_time=10:10、end_time=10:50、status=PENDING，模拟 V11 前存量），用后清理。
- 步骤：
  1. 调 available-slots（D）核对非对齐预约的计入：10:00 格与 10:30 格 currentBookings 均 +1（预约 10:10-10:50 与两格均有交集）；
  2. 居民 A 预约 D 10:00-10:30（与存量非对齐预约同格重叠，此时该格 booked=1 < capacity=2）；
  3. 居民 B 预约 D 10:00-10:30（该格 booked 将达 2，但存量非对齐已占 1 格 + A 占 1 格 → 满）；
  4. SQL 直查该格占用明细核对分桶来源。
- 预期：
  1. 两个栅格均计入（「覆盖即占用」：r.startTime < slotEnd && r.endTime > slotStart，08 §3.7/§3.8 宽松计入口径）；
  2. 预约成功（容量判定同样把非对齐存量计入 booked）；
  3. 被拒（5401，满格判定含存量非对齐占用）——新预约容量判定与展示口径一致；
  4. 占用明细 = 存量非对齐 1 条 + A 1 条，分桶正确。

### TC-C7-021 预约请求对齐校验（Slot Grid 防线①请求侧）
- 关联：TR-C7-07（提案）　类型：异常与边界　优先级：P1　执行：自动化 scripts/r3_slotgrid.mjs
- 前置：slot_unit=30 的资源 R-30（模板覆盖 09:00-12:00）；resident1 可用。
- 数据：目标日期 D；三组请求（见步骤）。
- 步骤：
  1. resident1 预约 D 09:10-10:00（起点非 30 倍数）；
  2. resident1 预约 D 09:00-10:10（止点非 30 倍数）；
  3. resident1 预约 D 09:00-09:30（对齐对照）。
- 预期：
  1. 400 拒绝（INVALID_PARAM，提示「预约起止时间必须为预约最小单位（30 分钟）的整数倍」）——预约请求侧对齐校验，与 TC-C7-017 模板侧为两道独立防线；
  2. 400 拒绝（止点判定）；
  3. 成功（对齐放行）。
  注：非对齐请求在进入模板覆盖校验之前即被拦（防线①a 先于①b）。

### TC-C7-022 容量并发不超卖（Slot Grid 并发专项）
- 关联：TR-C7-07（提案）　类型：并发　优先级：P0　执行：自动化 scripts/r3_slotgrid.mjs（与 SP-001 同型脚本模式）
- 前置：slot_unit=30、capacity=3 的资源 R-C（周一模板含目标时段）；**5 个**已入住测试居民账号（05 账号池，确保 user_id 互不相同——V11 用户级约束下同一账号并发会被防重拦截，混淆容量判定）。
- 数据：目标日期 D、同槽 09:00-09:30。
- 步骤：
  1. 5 路并发（同一栅格，5 个不同居民）同时提交 D 09:00-09:30 预约（Node 脚本 Promise.allSettled，模式复用 SP-001/DEF-005 回归脚本）；
  2. SQL 直查该资源该日该槽占用态（PENDING/RESERVED）预约数；
  3. 调 available-slots 核对 09:00 格 currentBookings 与 status；
  4. 重复执行 3 轮（每轮换日期或槽位，避免数据累加干扰）。
- 预期：
  1. 每轮恰好 3 路成功、2 路被拒（5401 满格），无第 4 条落库；
  2. 落库恰 3 条占用态（Redisson 锁内逐格容量检查生效，锁模式与 DEF-005 修复同型）；
  3. currentBookings=3、status=FULL；
  4. 3 轮结果稳定（无轮次间波动）。
  备注：**失败先区分脚本适配与产品缺陷**（项目纪律）——并发用例首轮失败时先核对账号池独立性（同账号并发触发用户级防重≠容量超卖）、日期/槽位残留数据、令牌有效性，再判产品缺陷。

### TC-C7-023 V11 用户级唯一约束并发兜底与多人同槽可达
- 关联：TR-C7-07（提案）　类型：并发　优先级：P0　执行：自动化 scripts/r3_slotgrid.mjs
- 前置：slot_unit=30、capacity=2 的资源 R-U（模板覆盖目标时段）；resident1 账号；另一已入住居民账号 B。
- 数据：目标日期 D、槽 09:00-09:30。
- 步骤：
  1. 同一居民（resident1）对同资源同日期同起止 10 路并发重复提交（同账号并发）；
  2. SQL 直查该组合占用态预约数；
  3. 多人同槽正向验证：resident1 与居民 B 先后（或 2 路并发）提交同资源同日期同起止；
  4. SQL 直查 uk_reservation_user_slot 约束存在性（SHOW INDEX FROM resource_reservation）与 uk_reservation_slot 缺失（V10 全局约束已撤销的回归确认）；
  5. 并发重复的 2 路失败响应核对业务码（锁内拦截 vs 约束兜底的提示区分）。
- 预期：
  1. 仅 1 路成功，其余 9 路被拒（锁内本人重复拦截 5002 DATA_EXISTS「同一时段已有本人的预约」+ V11 uk_reservation_user_slot 兜底 5401「该时段已被预约」DuplicateKeyException 转业务冲突）；
  2. 落库恰 1 条；
  3. 两居民均成功——V10 全局同槽唯一降级为用户级后的正向能力（capacity ≥2 资源不同居民同槽可达，08 §3.8 D-端点4 口径偏离点）；
  4. uk_reservation_user_slot 在（user_id, resource_id, reserve_date, start_time, slot_occupied）；uk_reservation_slot 不存在（V11 撤销确认，防约束残留误拒多人同槽）；
  5. 业务码与提示如实记录（两道防线的响应可区分）。
  备注：**失败先区分脚本适配与产品缺陷**——同账号并发用例须用同一令牌复用连接池，注意令牌刷新与登录态过期导致的 401 干扰。

### TC-C7-024 confirm 的 checkCapacity 逐格口径（容量满后确认不误拒）
- 关联：TR-C7-07（提案）　类型：异常与边界　优先级：P1　执行：自动化 scripts/r3_slotgrid.mjs
- 前置：slot_unit=30、capacity=2 的资源 R-T（周一模板）；admin1 与三个居民账号 A/B/C 可用。
- 数据：目标日期 D；A、B 各持一条 09:00-09:30 PENDING 预约（TC-C7-019 步骤 1 产物或现场构造）。
- 步骤：
  1. admin1 依次 confirm A、B 的预约（该格 booked 将达 capacity=2）；
  2. 确认过程中核对：confirm B 时该格已有 A 的 RESERVED（占用态）+ B 自身 PENDING（占用态）——若 checkCapacity 不排除自身则 booked=2 ≥ capacity 误拒；
  3. A、B 均流转 RESERVED 后，居民 C 提交同格预约（对照）；
  4. available-slots 复核该格 status。
- 预期：
  1. A、B 的 confirm 均成功（checkCapacity 逐格检查排除自身——自身 PENDING 已计入占用，不重复计，08 §3.8「confirm 的 checkCapacity 同步改逐格口径（排除自身）」）；
  2. 无误拒（对照修复前「精确匹配计数」口径下可能的误拒场景）；
  3. C 被拒（5401 满格）；
  4. status=FULL、currentBookings=2。

---

## TR → TC 映射（Slot Grid 第三批增量）

| TR | TC |
|----|----|
| TR-C7-07（提案） | TC-C7-016~024 |
